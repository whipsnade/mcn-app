# 2026-08-07

## Gate D/E Task 1 复审修复（2 P1 + 1 P2：字符串幂等键 / content_formats / 上传状态联合）

### 背景与目标

Codex 对 Task 1（前端 API、Artifact DTO 与事件状态契约）复审提出 3 个问题：
① 历史签名 `sendMessage(sessionId, content, idempotencyKey?: string)` 的字符串
第三参数被新 options 签名当作对象读取，Idempotency-Key 静默丢弃，破坏既有幂等
语义；② KOL scope 遗漏 `content_formats`（后端 KolSelectionScope 用于图文/视频
报价选择）；③ `ApiAgentUpload.status` 用宽泛 string，未收窄为后端实际三态。
严格 TDD 修复，不进入 Task 2，不做任何视觉/E2E/真实 UAT，不提交代码。

### 主要改动（关键文件）

**P1 字符串幂等键兼容**（`src/api/agent.ts`）
- `sendMessage` 改为 TypeScript overload 双签名（历史 `idempotencyKey?: string` 与
  新 `options?: AgentMessageOptions`），实现体统一接收 `AgentMessageOptions | string`
  并在入口确定性归一化：string → `{ idempotencyKey: value }`；object → 原样；
  undefined → `{}`。
- 行为不变：无第三参数 body 仍只有 `{content}`；options 正确发送
  parent_run_id/artifact_version_ids/upload_ids（空数组不进入 body）；
  options.idempotencyKey 正确发送 Header。

**P1 scope.content_formats**（`src/api/agentArtifacts.ts`）
- `KolSelectionPayload.scope` 增加可选 `content_formats?: string[]`（注释说明：
  新版本恒返回、历史版本可能缺失；不放进 filters/item）。

**P2 上传状态联合**（`src/api/agent.ts`）
- 新增 `export type AgentUploadStatus = 'uploaded' | 'parsed' | 'failed'`；
  `ApiAgentUpload.status` 由 `string` 收窄为 `AgentUploadStatus`（镜像后端
  UploadRead/数据库约束）。

### 测试（先红后绿）

- `agent.test.ts` +3：legacy string 幂等键 → `Idempotency-Key: 'legacy-key'` 头；
  空数组 options 不进 body；上传 DTO fixture 用 `satisfies ApiAgentUpload` 防字符串
  推断 + `@ts-expect-error` 断言非法状态字面量（如 'drafting'）被类型系统拒绝。
- `agentArtifacts.test.ts` +2 断言：`kolValueV3Fixture.scope.content_formats` 为
  `['视频']`（v3 fixture 带真实值）；旧 kol_score_v2 fixture 不带该字段仍可读取
  （`toBeUndefined()`）。
- 红阶段：运行时 1 failed（legacy-key 头缺失）+ 类型 5 errors（TS2559 无共同属性 /
  TS2578 @ts-expect-error unused / TS2353+TS2339 content_formats 不存在）。
- 绿阶段：全部通过。

### 验证结果

- `npm run test` → **224 passed**（27 files；因 package.json test 脚本固定包含
  `src`，该输出为完整前端测试集，非仅三个聚焦文件）。
- `npm run lint`（tsc --noEmit）→ 通过（含新增 overload 与 @ts-expect-error 校验）。
- `npm run build` → 构建成功（chunk 大小警告为既有提示，非错误）。
- `git diff --check` 干净；`git status --short` 仅既有 Task 1 文件（无新增文件）。
- 未运行 E2E/浏览器/视觉/真实 UAT；未提交、推送或合并。

### 遗留事项

- 等待 Codex 复审，不自行进入 Task 2。
- 本轮仅修复 3 项；Task 2–7 功能未动；Task 1 其余契约（事件归并、v3 判别联合、
  campaign 新章节）保持上一轮实现不变。

---

## Gate D Task 2：移除 Reviewer 执行卡与默认折叠 Thinking

### 背景与目标

直接发布运行时已不再创建 Reviewer 流程；前端执行卡必须停止显示审核相关状态，
同时保留历史 SSE 回放的容错。Thinking 在运行、完成和中断时都应默认折叠，且状态
变化与新增 delta 均不得推翻用户手动展开的选择。

### 主要改动（关键文件）

- `src/components/agent/AgentRunCard.tsx`：移除 Reviewer 图标、状态与文案；历史
  `reviewing` 仅显示为“历史执行状态”。新增 `completed_with_warnings` 的“部分完成”
  状态，以及逐项 Artifact 发布结果（准备中、已发布、发布校验失败、发布失败）。
  Run 卡的既有终态收起机制未改动。
- `src/components/agent/AgentThinking.tsx`：展开状态统一以 `false` 初始化，移除所有
  根据 running/completed/interrupted 自动收放的 effect；后续事件只更新文本与状态。
- `src/components/agent/AgentThinking.test.tsx`：新增三种初始状态折叠与“用户展开后
  delta/终态变化仍保持展开”的回归覆盖；同步 `AgentRunCard` 与 `ChatArea` 测试期望。

### 验证结果

- 红阶段：新增断言在旧实现上失败（running Thinking 自动展开、复核区仍出现）；
  聚焦测试还暴露调用方对旧自动展开行为的过期期望，已同步更新。
- `npm run test -- src/components/agent/AgentRunCard.test.tsx
  src/components/agent/AgentThinking.test.tsx src/components/ChatArea.test.tsx
  src/state/agentEvents.test.ts` → **225 passed**（脚本覆盖全部 `src` 测试）。
- `npm run lint` → 通过；`npm run build` → 构建成功（仅既有 bundle 体积警告）；
  `git diff --check` → 干净。

### 遗留事项

- 未执行 E2E、真实模型/DataTap UAT 或视觉验收；这些属于后续 Task 5–7。
- 历史 review SSE 事件继续由 reducer 的默认分支安全忽略，执行卡不再消费 `run.review`。

---

## Gate D Task 3：三类 BI、历史 Version 与 Excel 入口

### 背景与目标

右侧工作区须始终以已发布 Artifact Version 为唯一数据源：BI 与 Excel 下载绑定同一
精确版本，新产物只提示更新而不打断用户当前 Tab 或历史版本选择。活动报告需补齐
直接发布后的 Excel 入口和第九/十章节，达人报告须识别 v3 价值评分而不篡改历史 v2。

### 主要改动（关键文件）

- `src/components/artifacts/ArtifactWorkspace.tsx`：将 `campaign_report_v2` 加入可导出
  类型，继续使用已有 `artifact_id + selectedVersion` 调用；未读提示从纯圆点补为“更新”
  可视文本（标记为 `aria-hidden`，不改变 Tab 的既有可访问名称与定位）。
- `src/components/artifacts/CampaignArtifactView.tsx`：新增“归属、自然传播与受众”章节，
  对 null/缺失字段明确显示“数据受限/未提供”；仅在 `data.roi != null` 时加入
  “ROI 与转化”第十章节，避免把缺失 ROI 绘制为零值。
- `src/components/artifacts/KolSelectionArtifactView.tsx`：按判别联合展示
  `kol_value_score_v3` 的投放性价比指数、效果分与价格效率分，并在评分说明固定
  “效果与匹配度 70 + 价格效率 30”；`kol_score_v2` 仅保留历史综合评分，不伪造价格分。

### 验证结果

- 红阶段：活动 Excel 按钮缺失、ROI 章节不存在、v3 投放性价比指数缺失，三项新测试
  均按预期失败。
- `npm run test -- src/components/artifacts/ArtifactWorkspace.test.tsx
  src/components/artifacts/CampaignArtifactView.test.tsx
  src/components/artifacts/KolSelectionArtifactView.test.tsx
  src/components/artifacts/BrandArtifactView.test.tsx` → **227 passed**（脚本覆盖全部 `src`）。
- `npm run lint` → 通过；`npm run build` → 构建成功（仅既有 bundle 体积警告）；
  `git diff --check` → 干净。

### 遗留事项

- 未执行浏览器 E2E、真实 UAT 或视觉验收；分别由 Task 5–7 处理。
- 品牌视图既有八个业务章节与历史版本绑定逻辑保持不变；本次没有自动切换任何 Tab 或版本。

---

## Gate D Task 4：上传 Evidence、Artifact 引用与失败 Run 重试

### 主要改动

- 新增 `AgentUploadComposer`：只接受 CSV/XLSX，展示上传中、已解析和失败状态；移除
  仅清除本次本地选择，不删除后端审计记录。
- `ChatArea`：发送时只透传 `parsed` 的 `upload_ids`；请求成功后才清空输入与附件，
  失败时两者保留；切换 Session 清空本地未发送附件。
- `useAgentWorkspace`：每次普通发送生成新的 `Idempotency-Key`；新增 retry 调用，
  接受后端新的 Child Run id 并回拉会话详情，旧 Run 卡保持终态。
- `App`：将 message options 与 Run retry 接入实际工作区调用。

### 验证结果

- `npm run test -- src/components/agent/AgentUploadComposer.test.tsx
  src/components/ChatArea.test.tsx src/hooks/useAgentWorkspace.test.tsx` → **228 passed**。
- `npm run lint` → 通过；`npm run build` → 构建成功（仅既有 bundle 体积警告）；
  `git diff --check` → 干净。

### 遗留事项

- Artifact Version 引用的显式选择 chip 仍由右侧 Artifact 工作区触发；发送 API 已可
  接收精确 `artifactVersionIds`，本任务未新增重复的版本选择入口。
- E2E 与真实 UAT 尚未执行，归入 Task 5–7；视觉验收明确未执行。

---

## Gate D Task 5：直接发布运行时的非视觉 Playwright 回归

### 背景与目标

将端到端覆盖从已退役的 Reviewer SSE 流程迁移到直接发布契约；验证用户可见
Run/Artifact 行为、上传引用、缓存详情和导出，不以截图或像素结果作为验收依据。

### 主要改动（关键文件）

- `e2e/agent-runtime.spec.ts`：首条主流程改为两个相互独立的 Run，使用
  `artifact.publish.completed` 逐项结果（品牌发布成功、活动发布校验失败）与
  `run.completed_with_warnings`；断言 Thinking 初始收起、无审核/复核文案。澄清流程
  断言模糊请求零 MCP、回答创建 child Run；新增 CSV multipart 上传并只把已解析的
  `upload_ids` 带入新 Run 的网络断言。
- `e2e/artifact-workspace.spec.ts`：未读更新后保持当前 BI Tab；缓存命中的达人详情
  直接渲染；品牌既有导出用例外，新增活动报告与达人名单两类 published Version 的
  Excel 下载断言。
- `src/components/artifacts/ArtifactWorkspace.tsx`：补齐 `kol-details` 缓存命中时
  `result.detail`（`kol_detail_v2`）的直接渲染分支。此前仅处理 `artifact_id` 或
  `run_id`，导致后端返回缓存 payload 时详情弹窗不出现。
- `playwright.config.ts`：关闭 trace 与 failure screenshot；本 Gate 的自动化只保留
  DOM/ARIA、网络/SSE 与下载工件验证。

### 验证结果

- 调试阶段先后定位并修复两个测试问题：文件输入与上传区域有同名 label（改用文件
  输入的 role 定位）；上传标签是单一“文件名 · 已解析”文本节点（改为完整文本断言）。
  未查看失败时自动生成的旧视觉工件，之后已在配置中关闭。
- 缓存详情 E2E 先红后绿，暴露并验证 `ArtifactWorkspace` 遗漏的 `result.detail`
  分支。
- `npx playwright test e2e/agent-runtime.spec.ts e2e/artifact-workspace.spec.ts` →
  **51 passed**（desktop/tablet/mobile；最终 `test-results/.last-run.json` 为
  `status: passed`、`failedTests: []`）。
- `npm run lint`（tsc --noEmit）→ 通过；`git diff --check` → 干净。

### 遗留事项

- 未执行视觉/像素验收，也未查看截图；这是本 Gate 的明确排除项。
- 真实供应商 UAT、完整后端/前端验证、迁移与切换文档仍由 Task 6–7 执行。

---

## Gate E Task 6：真实供应商 UAT（中断记录）

### 背景与目标

按脚本在 `kol_insight_test`、mock 认证下，以真实模型与 DataTap 执行 Agent Runtime
业务场景；不打印密钥，不重跑以掩盖供应商波动。

### 验证结果

- `bash backend/scripts/run_real_agent_uat.sh` 强制确认 `APP_ENV=test`、
  `MYSQL_DATABASE=kol_insight_test`、`AUTH_MODE=mock` 后启动；真实模型/DataTap 仅
  连接测试库，输出未包含密钥或 DSN。
- 模糊品牌请求得到 `clarification_requested`、1 次决策、0 MCP、0 积分变动。
- 品牌分析完成：33 次决策、`brand_report_v3 v1 restricted lineage_ok`；真实调用包含
  settled、`failed_confirmed` 和 `result_unknown`，主交付仍完成。
- 活动首轮达到 `clarification_requested`，预设回答创建 child Run；该 child Run 在
  真实模型供应商反复重连、连续多个决策墙钟无终态后被 SIGINT 中断。未进行重跑。
  pytest fixture 已追加 QA 轮次、更新最新 JSON 并清理 `uat-*` 测试数据。

### 遗留事项

- 本轮 UAT **未通过完整 Gate E**：活动回答 Run 留为 `running`，达人、钻取、缓存、
  504/unknown/empty 结构化错误与 reviewer 零增量尚未获得同轮真实验证。
- `outputs/agent-runtime-uat-results.json` 由脚本按既有契约最小更新；
  `docs/qa/agent-runtime-uat-rounds.md` 已追加中断说明。后续必须在供应商可响应后
  另起一轮 UAT，不能将本轮的部分完成当作通过。

---

## Gate E Task 7：完整回归与切换文档（本地通过，生产切档仍阻断）

### 主要改动

- `docs/runbooks/agent-runtime-v3-cutover.md`：更新唯一迁移 head 为
  `0036_export_claim_token`；补充直接发布的部署顺序、上传/Artifact 权限边界、监控项、
  非视觉人工验收边界、UAT 阻断状态与安全回滚约束。
- `docs/runbooks/phase-2-runtime.md`：移除“新 Run 必经 Reviewer”旧说法，改为确定性
  lineage 门禁后的逐项直接发布与 `completed_with_warnings` 聚合，并链接最新部署顺序。

### 验证结果

- `backend/.venv/bin/alembic heads` → 唯一 `0036_export_claim_token (head)`。
- `backend/.venv/bin/ruff check app tests` → 通过。
- `backend/.venv/bin/pytest -q` → **1507 passed, 12 skipped**。
- `npm run test` → **228 passed**；`npm run lint` → 通过；`npm run build` → 通过（仅既有
  bundle 体积警告）。
- `npm run test:e2e` → **51 passed**（desktop/tablet/mobile；只断言 DOM/ARIA、网络/SSE
  与下载，未进行视觉/像素验收，也未查看截图）。

### 遗留事项

- 不可据此宣布 Gate E 或生产切档通过：真实 UAT 的活动回答 Run 被供应商重连中断，且活动、
  达人、钻取、unknown 恢复核对与 Reviewer 零写入仍无同轮真实证据。
- 独立代码审查发现并修复的前端/事件问题见下节；不推送、不合并。

---

## Gate E Task 8：独立审查修复（直发失败状态归并）

### 背景与目标

在 Gate D/E 审查中修复两个会直接影响用户可见运行状态的问题：完成或取消的 Run 不应展示
无效重试入口；发布汇总的失败项可能只携带 `draft_id`，必须回写到同一 Run 先前创建的 Draft。

### 主要改动（关键文件）

- `src/components/ChatArea.tsx`：仅 `failed` Run 显示“重试此 Run”；`completed`、
  `completed_with_warnings` 与 `cancelled` 不再触发后端拒绝的 retry API。
- `backend/app/agent_runtime/engine.py`：`artifact.draft.created/updated` SSE payload
  新增稳定 `draft_id`，与后续 `artifact.publish.completed.items[*].draft_id` 对齐。
- `src/state/agentEvents.ts`：草稿事件保存 `draftId`，发布汇总优先按 `artifact_id` 或
  `draft_id` 归并；空 `artifact_id` 的 failed 项更新既有草稿而非新增
  一张无模块的重复卡。
- `src/state/agentEvents.test.ts`、`src/components/ChatArea.test.tsx`、
  `backend/tests/agent_runtime/test_engine.py`：补充失败发布归并、失败 Run 重试和后端事件
  payload 的回归测试；`e2e/agent-runtime.spec.ts` 的 SSE fixture 同步真实事件身份与
  `message.completed` 在终态之前的顺序。
- `docs/runbooks/agent-runtime-v3-cutover.md`：明确 Draft/发布汇总的身份关联契约与
  切档监控核对项。

### 验证结果

- 修复前新增的两个前端回归用例均按预期失败；修复后
  `npm run test -- src/state/agentEvents.test.ts src/components/ChatArea.test.tsx` →
  **230 passed**。
- `cd backend && .venv/bin/pytest -q tests/agent_runtime/test_engine.py -k artifact_draft_created`
  → **1 passed, 44 deselected**。
- 测试库由滞后的 `0030_direct_publish_runtime` 升级至 `0036_export_claim_token` 后，
  `cd backend && .venv/bin/ruff check app tests && .venv/bin/pytest -q` → 通过；
  `npm run test` → **230 passed**，`npm run lint`、`npm run build` → 通过（仅既有 bundle
  体积警告），`npm run test:e2e` → **51 passed**，修正后的
  `npx playwright test e2e/agent-runtime.spec.ts` → **30 passed**。
- 独立复审已关闭 E2E `validation_failed` 身份字段偏差：该状态保留真实 `artifact_id`，
  空 `artifact_id` 只用于 `failed` 的 reducer 回归用例。

### 遗留事项

- 该修复不改变 Gate E 的真实 UAT 阻断结论；仍须在供应商恢复后完整重跑。

---

## Pi Agent Runtime 集成设计与方案 A 实施计划

### 背景与目标

现有自研 Runtime 在意图识别、模型循环与 MCP 编排层积累了较多代码控制，但真实报告的
数据覆盖和模型分析效果仍不如通用 Agent 直接调用 MCP。本轮仅做架构设计与实施规划：
用 Pi 接管模型研究循环，同时保留 FastAPI 的身份、状态、Evidence、确定性 Builder、
Artifact Version、Excel、BI 和审计能力。

### 主要改动（关键文件）

- 新增 `docs/superpowers/specs/2026-08-07-pi-agent-runtime-integration-design.md`：确认
  方案 A（Pi RPC POC）→方案 B（生产 Pi Agent Gateway）的边界、DataTap 直连透明 Hook、
  报告闭环、多轮证据复用、Skills、进程/并发、租户配置、License/积分、SSE、恢复与灰度。
- 新增 `docs/superpowers/specs/2026-08-07-marketing-mcp-gateway-future-design.md`：单独记录
  方案 C；当前暂缓，只有 Pi 链路稳定且用户再次批准后才能实施。
- 新增 `docs/superpowers/plans/2026-08-07-pi-rpc-poc.md`：把方案 A 拆为 9 个 TDD Task，
  包含 Pi 版本/API 探针、独立数据库门禁、RPC client、DataTap MCP Extension、Evidence
  旁路、内部 Builder/发布工具、六个 Skills、Run/Event 适配和六场景真实对比 Gate。
- 新增 `docs/superpowers/prompts/2026-08-07-pi-rpc-poc-codex-prompt.md`：为独立 Codex
  开发会话固定工作树、技能、TDD、数据隔离、同模型、直连透明 Hook、非视觉验收和 Gate
  停止条件，避免新会话重新解释或偏离已确认决策。
- 明确 POC 数据库固定 `kol_insight_pi_poc`，不处理积分、并发、管理端和正式切流；current
  与 Pi 只比较同一个模型。POC 通过后才基于实测 API 生成方案 B 的详细实施计划。

### 验证结果

- 设计中的所有关键选择已逐项获得用户确认。
- 对照当前代码确认可复用 `AgentRun/Attempt/Event`、`EvidenceWriter`、六类 Builder、
  `ArtifactPublicationService` 与现有 Excel/BI Version 契约。
- 查阅 Pi 当前官方 RPC、SDK、Skills、Extensions 和 tool flags 文档；识别出上游仓库/npm
  scope 迁移风险，已把“先探针、再锁精确版本”设为 Task 1 硬门槛。
- 三份新增文档通过 `git diff --check` 和占位词扫描；未修改业务代码、数据库或运行环境。

### 遗留事项

- 由新的 Codex 开发会话按 `2026-08-07-pi-rpc-poc.md` 只执行方案 A，并在每个 Task 后审查。
- Gate A 未通过前不得启动方案 B；方案 B 的文件级详细计划依赖 POC 验证出的 Pi API。
- Pi 链路稳定后必须提醒用户评估是否启动 Marketing MCP Gateway（方案 C）；未获明确批准
  不得实现。

---

## 方案 A Gate A Task 1：Pi RPC 版本与同模型探针

### 背景与目标

为方案 A 的首道 Gate 锁定当前官方 Pi 版本，验证 RPC 的严格 JSONL 边界、显式
Extension/Skill 加载及与当前 Runtime 完全相同模型的最小兼容性；不进入方案 B/C 或业务
集成。

### 主要改动

- 新增独立 `pi-runtime/` workspace，锁定
  `@earendil-works/pi-coding-agent@0.84.1`、MCP SDK、TypeScript、tsx 与 Vitest 的精确版本；
  npm 已弃用的 `@mariozechner` scope 不纳入 POC。
- 新增 `parseRpcLine` 严格 LF JSONL 解析及 TDD 测试，覆盖 trailing CR、字符串内 U+2028、
  空记录与非对象拒绝。
- 新增 `docs/qa/pi-runtime-version-probe.md`：记录无模型 RPC `get_state`/`abort`、资源 flags
  与同 endpoint/model/thinking 的临时 custom-provider 实测。

### 验证结果

- TDD 红灯：`protocol` 模块不存在时，测试按预期仅因 module missing 失败。
- 绿灯：`cd pi-runtime && npm test && npm run typecheck` → 1 文件、2 测试通过；类型检查通过。
- Pi 0.84.1 在隔离配置下确认 `--mode rpc`、`--no-session`、`--no-builtin-tools`、
  `--no-context-files`、`--no-extensions -e`、`--no-skills --skill`；无模型 stdout 仅两条
  JSONL response、stderr 为 0。
- 以当前 Runtime 的 `model:deepseek-v4-flash` 与 `high` thinking 实测，实际
  provider/model/thinking 一致，prompt 到达 `message_end`；上游没有返回 token usage 字段，
  已记录为不可用而非零值。

### 遗留事项

- 若后续 Task 需要精确 token usage 审计，应先明确“供应商未返回 usage”的可接受表示，或
  获得供应商返回该字段的确认；不得估算。

---

## 方案 A Gate A Task 1 Fix round 1：可审计资源与同模型探针

### 背景与目标

针对 Task 1 审查项，补齐 `--no-extensions -e`、`--no-skills --skill` 的真实加载证据，
审计 `get_state`/`abort` JSONL 请求与响应关联，并提供不复制 `.env` 的同模型 state probe。

### 主要改动

- `pi-runtime/src/rpc/audit.ts`：固定非敏感 RPC request ids、response 成功/关联形状与显式
  资源存在、自动发现资源不存在的断言。
- `pi-runtime/scripts/rpc-audit-probe.ts`：运行时生成有效 YAML frontmatter 的显式/自动
  Extension 与 Skill fixture；`get_commands` 确认只有显式 fixture 被加载。
- `pi-runtime/src/rpc/model-config.ts` 与 `scripts/model-state-probe.ts`：仅消费进程环境，
  将 `$TENCENT_PLAN_API_KEY` 引用写进临时 `0600 models.json`，并用 `get_state` 验证实际
  provider/model/thinking；无 prompt、无 `.env` 拷贝；两个 Pi 子进程均设置 10 秒硬超时，
  超时即 `SIGTERM` 并失败，避免探针无限等待。
- `rpc-probe.test.ts`：增加 invalid JSON、缺少 type、response 关联、资源隔离与安全模型配置
  测试。

### 验证结果

- 新测试先因缺少 audit 模块红灯；实现后 `npm test && npm run typecheck` → **8 passed**、
  类型检查通过。
- 初次真实 resource probe 失败定位为 Pi 要求 Skill frontmatter 中存在 `description`；补齐后
  `npm run probe:rpc` 验证显式 Extension/Skill 成功、自动资源未加载、三条 response 的 id/
  command/success 形状正确，stderr=0。
- `npm run probe:model` 在单次当前 Runtime 环境内验证
  `kol_insight_pi_poc` / `model:deepseek-v4-flash` / `high` 与 `get_state` 一致，stderr=0。

### 遗留事项

- 同模型 state probe 故意不发送 prompt；原始最小 prompt 兼容证据仍见同日 QA。保持 token
  usage 上游未返回的既有 concern，不估算数值。

---

## 方案 A Gate A Task 2：POC 配置隔离、单 Run 令牌与内部 DTO

### 背景与目标

Pi RPC POC 必须与现有开发/测试运行时完全隔离：只有显式开启、测试环境且数据库名精确为
`kol_insight_pi_poc` 时才能启动；内部 HTTP 调用还需使用不可跨 Run 复用的短期令牌。

### 主要改动

- `backend/app/core/config.py`：新增 POC 开关、内部签名密钥、内部 URL、Run/MCP 超时与最大
  决策次数配置，默认关闭。
- `backend/app/pi_runtime_poc/auth.py`：新增 fail-closed 的 `PiPocSettingsGuard`，同时核验
  开关、`test` 环境、精确 POC 数据库名与非空内部密钥；HMAC 令牌只包含 `run_id`、过期时间和
  随机 nonce，验签、Run 归属、篡改与过期失败均返回相同 401。
- `backend/app/pi_runtime_poc/schemas.py`：提供严格禁止额外字段的工具事件、内部工具请求/响应
  与 Run 上下文 DTO；原始工具 payload 使用 `Any`，不承载密钥。
- `.env.example`：补齐默认关闭的 POC 配置占位符，不包含任何实际密钥。任务简报所列
  `backend/.env.example` 在仓库中不存在，按现有 `Settings` 的相对 `.env` 读取规则更新根目录
  `.env.example`。

### 验证结果

- 严格 TDD：实现前聚焦测试因 `app.pi_runtime_poc` 缺失按预期 collection fail；实现后
  `cd backend && .venv/bin/pytest --confcutdir=tests/pi_runtime_poc tests/pi_runtime_poc/test_auth.py tests/pi_runtime_poc/test_config.py -q`
  → **7 passed**。
- `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc app/core/config.py`
  → **All checks passed**。
- 聚焦测试使用 `--confcutdir`，不加载根数据库 fixture，未连接开发库、测试库或 POC 数据库。

### 遗留事项

- Task 3 的 POC runner 必须在创建 Pi 子进程前调用 `PiPocSettingsGuard.assert_safe`，不得绕过
  本任务的启动门禁。

---

## 方案 A Gate A Task 3：严格 Pi RPC 子进程客户端

### 背景与目标

为方案 A POC 提供一个只处理 Pi 子进程与严格 JSONL RPC 边界的异步客户端。它不接入
DataTap、数据库、积分或正式 Runtime；调用方将在后续 Task 7 先执行 Task 2 的配置门禁。

### 主要改动（关键文件）

- `backend/app/pi_runtime_poc/rpc.py`：新增 `PiRpcClient`、配置 DTO 与协议/退出/墙钟超时异常。
  使用 `create_subprocess_exec` 的参数列表启动，不经 shell；stdout 用 bytes buffer 仅按 LF 分帧，
  去除行尾 CR 后严格校验 JSON 对象和 `type`。stderr 独立存入 64 KiB 环形诊断尾部。
- 同一客户端固定 `--mode rpc --no-session --no-builtin-tools --no-context-files`
  `--no-extensions` 后逐项 `-e`，以及 `--no-skills` 后逐项 `--skill`；每个 Run 创建并在关闭时
  删除专属 `PI_CODING_AGENT_DIR`，环境强制 `PI_OFFLINE=1` 与 `PI_SKIP_VERSION_CHECK=1`。
- `backend/tests/pi_runtime_poc/test_rpc.py`：以 fake subprocess 覆盖 prompt id 关联、跨 read 分片、
  CRLF 与字符串内 U+2028、stderr 隔离、非法 stdout、30 分钟默认墙钟（测试缩短）、abort 退出和
  close 清理。

### 验证结果

- 红灯：实现前聚焦测试按预期在 `app.pi_runtime_poc.rpc` 缺失处 collection fail。
- 绿灯：`cd backend && .venv/bin/pytest --confcutdir=tests/pi_runtime_poc tests/pi_runtime_poc/test_rpc.py -q`
  → **7 passed**；随后以根 conftest 所需的非敏感占位环境运行简报原始 pytest 命令亦为 **7 passed**，
  测试未使用数据库 fixture。
- `cd backend && .venv/bin/ruff check app/pi_runtime_poc/rpc.py tests/pi_runtime_poc/test_rpc.py`
  → **All checks passed**；`git diff --check` → 干净。

### 遗留事项

- Task 7 负责在调用 `PiRpcClient.start` 前接入 `PiPocSettingsGuard.assert_safe`，并将事件接至
  POC Run；本任务不自行越过该职责边界。
- Pi 上游 `abort` 的真实停止/退出时序仍由后续真实 POC 场景验证；本客户端已经在 abort 后等待
  子进程退出，避免遗留孤儿进程。

---

## 方案 A Gate A Task 3 Fix round 1：环境隔离与 abort 有限时收口

### 背景与目标

独立审查对 Task 3 初版（commit 443bde2）判未通过，列 3 项：① Critical——`start()`
`os.environ.copy()` 后整体传给 Pi 子进程，宿主数据库密码、DataTap token、模型 key、
内部签名 secret 会因环境继承进入 Pi；② Important——`abort()` 发送 abort RPC 后无限
`await _wait_task`；③ Minor——永不换行的 stdout 无最大 record bytes 上限，buffer 无限
增长。本轮严格 TDD 修复，完成后暂停在 Task 3，不进入 Task 4。

### 主要改动（关键文件）

- `backend/app/pi_runtime_poc/rpc.py`：
  - 新增 `_pi_environment()` 与 `_ENV_ALLOWLIST`（PATH/LANG/LC_ALL/LC_CTYPE/LANGUAGE/
    TMPDIR/HOME）：仅从宿主挑选非敏感键，合并 `PiRpcConfig.environment` 显式值，最后
    注入 Pi 必需键（PI_CODING_AGENT_DIR/PI_OFFLINE/PI_SKIP_VERSION_CHECK）；不再整体
    继承宿主环境。
  - `abort()` 改 `_await_exit_within()`：abort RPC 后给 `_ABORT_GRACE_SECONDS` 宽限，
    未退出仅对当前精确 PID 依次 `terminate()` → 再 `_TERMINATE_GRACE_SECONDS` 宽限 →
    仍未退出才 `kill()`；不使用进程名通配或 shell。
  - `_read_stdout()` 新增 `_MAX_RPC_RECORD_BYTES`（1 MiB）：单条未终结 record 超限即以
    `PiRpcProtocolError("rpc_record_too_large")` 失败收口。
- `backend/tests/pi_runtime_poc/test_rpc.py`：新增 6 例——宿主敏感变量不继承、仅显式
  配置并入、Pi 必需键恒注入、abort 后进程不退出→terminate、stubborn 进程 terminate 后
  仍不退出→升级 kill、永不换行 stdout 超限→协议错误；FakeProcess 增 `stubborn` 模式。

### 验证结果

- 红灯：实现前 5 个新测试按预期失败（`5 failed, 8 passed`）。
- 绿灯：
  - `cd backend && .venv/bin/pytest --confcutdir=tests/pi_runtime_poc tests/pi_runtime_poc/test_rpc.py -q`
    → **13 passed in 0.06s**。
  - 带非敏感占位配置复跑原始命令 → **13 passed**；全目录 `tests/pi_runtime_poc`
    → **20 passed**。
  - `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc`
    → **All checks passed**；`git diff --check` → 干净。
- 既有正确内容保持：bytes buffer 按 LF `b"\n"` framing（不用 readline）、仅剥离行尾 CR
  （不损坏字符串内 U+2028）、stdout 只接受严格 JSONL、stderr 保留 64 KiB 有界 ring
  buffer、`create_subprocess_exec` 参数列表不经 shell、固定 flags、30 分钟默认墙钟。

### 遗留事项

- 本修复为 fix round 1，等待独立审查通过后再放行 Task 4；不自行进入。
- Pi 上游 abort 的真实停止/退出时序仍由后续真实 POC 场景验证；当前 abort 语义已从
  「无限等待退出」改为「宽限 → terminate → kill 的有限时收口」。

---

## 方案 A Gate A Task 4：透明 DataTap MCP Extension 与 Evidence 旁路

### 背景与目标

让 Pi 直接、透明地调用 DataTap MCP，并把每次工具调用以零积分旁路沉淀为
AgentStep + AgentToolCall + EvidenceItem，同时保留严格安全边界：工具名/参数/原始
结果/错误原样透传、只加 `_runtime_metadata`、不自动重试/拆分/改参/换工具/熔断、
token 只存在于 HTTP Authorization 头。

### 主要改动（关键文件）

- `pi-runtime/src/redaction.ts`：递归脱敏（key 匹配 authorization|api_key|token|
  secret|password|cookie → `***`，替换已知 secret 字符串），仅用于审计/事件，返回
  深拷贝不修改原值。
- `pi-runtime/src/extensions/datatap-mcp.ts`：`discoverDatatapTools`（原样工具目录）、
  `callDatatapTransparent`（start 审计 → 一次 MCP call → settle/fail → 仅加顶层
  `_runtime_metadata`）、`withRuntimeMetadata`。不持有 token。
- `pi-runtime/src/http/client.ts`：`PiPocHttpClient`（start/settle/fail），token 只在
  `Authorization: Bearer`，绝不进入请求体。
- `pi-runtime/src/extensions/poc-runtime.ts`：组合入口薄壳（MCP SDK 连 DataTap +
  注册 Pi 工具 + 审计旁路），token 从环境变量读取。
- `backend/app/pi_runtime_poc/schemas.py`：`PiToolStarted` 加 `call_id`；新增
  `PiToolStartedResponse`/`PiToolSettledResponse`；`PiToolFailed` 增 `status`。
- `backend/app/pi_runtime_poc/service.py`：`PiEvidenceIngestService` start/settle/fail
  零积分状态机；`logical_call_id = sha256(run_id + Pi call_id)`（不依赖参数 hash 熔断）；
  settle 写不可变 EvidenceItem；failed/unknown 不产生 available Evidence。
- `backend/app/pi_runtime_poc/router.py` + `app/api/router.py`：`/api/v1/internal/pi-poc/
  runs/{run_id}/tool-calls/{start|settle|fail}`；未启用 POC 时 404 隐藏。
- 测试：`pi-runtime/tests/{redaction,datatap-mcp,http-client}.test.ts`、
  `backend/tests/pi_runtime_poc/test_evidence_ingest.py`。后端测试用 SQLite 内存库
  （`MEDIUMTEXT` 编译为 TEXT），绝不连接任何 MySQL 库。

### 验证结果

- `cd pi-runtime && npm test` → **22 passed**（4 文件）；`npm run typecheck` → 通过。
- `cd backend && .venv/bin/pytest --confcutdir=tests/pi_runtime_poc tests/pi_runtime_poc/test_evidence_ingest.py -q`
  → **8 passed**；`TENCENT_PLAN_API_KEY/... ` 占位环境跑 `tests/pi_runtime_poc` → **28 passed**。
- `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc app/api/router.py`
  → **All checks passed**。
- app 加载确认 3 个 internal pi-poc 路由注册正常。

### 安全/数据隔离

- 测试明确用 `sqlite+aiosqlite:///:memory:`，不连接 kol_insight / kol_insight_test /
  kol_insight_pi_poc 任何 MySQL 库。
- token 只存在于 `Authorization` 头；`datatap-mcp.test.ts` 断言审计 body 无 token。
- 未提交/读取/修改任何 `.env`；无方案 B/C、积分、License、管理端、并发改动。

### 遗留事项

- Task 7 runner 负责在调用 `PiRpcClient.start` 前执行 `PiPocSettingsGuard.assert_safe`，
  并以环境变量显式注入 DataTap token / 内部 token / run_id 给 Pi Extension。
- 真实 DataTap 连通与工具调用时序由 Task 8/9 的真实 POC 验证。

---

## 方案 A Gate A Task 4 复审修复：透明异常旁路与可复现 SQLite 测试

### 背景与目标

复审发现 MCP transport 抛异常时未登记失败，且脱敏函数没有接入审计旁路；同时 SQLite
隔离测试依赖未在开发依赖中声明，干净虚拟环境无法执行。

### 主要改动

- `datatap-mcp.ts`：MCP 抛异常时先以审计投影调用 `failToolCall`，再原样抛回 Pi；错误
  outcome 的审计同样经过投影，返回 Pi 的原始 payload/error 不变。
- `poc-runtime.ts`：只对审计投影递归脱敏 DataTap token 与临时 Run token。
- `backend/pyproject.toml`：将 `aiosqlite` 声明为开发依赖，令 SQLite 内存隔离测试可复现。
- `datatap-mcp.test.ts`：补充异常旁路和“审计脱敏、Pi 原样返回”的红绿回归覆盖。

### 验证结果

- 红灯：新增两项透明异常/脱敏测试均失败，分别证明未登记异常失败与审计泄漏敏感字符串。
- 绿灯：`npm test -- datatap-mcp.test.ts redaction.test.ts` → **12 passed**；
  `npm run typecheck` → 通过。
- `pytest --confcutdir=tests/pi_runtime_poc tests/pi_runtime_poc/test_evidence_ingest.py -q`
  → **8 passed**；Ruff → 通过；均未连接 MySQL。

### 遗留事项

- Task 4 仍须在真实 `kol_insight_pi_poc` 数据库执行 Task 8/9 的整链路验证；当前 SQLite
  用例仅证明 ORM 状态机与幂等行为。

---

## 方案 A Gate A Task 5：受控历史、Builder 与确定性发布内部工具

### 背景与目标

把既有可信工具（历史读取、六类 Builder、确定性发布）以白名单形式桥接给 Pi：
Pi 只能看到 get_session_context / search_evidence / read_tool_result / read_artifact /
六个 build_*_draft / publish_artifacts，禁止 bash/shell/文件编辑/任意 HTTP/Draft 直写/
计算/记忆；DataTap 仍由 Task 4 Extension 直连，内部 Registry 不注册 AgentMcpTool。
服务端从 Run token 对应 Run 强制注入身份，伪造身份键一律剥离或 Schema 拒绝。

### 主要改动（关键文件）

- `backend/app/pi_runtime_poc/internal_tools.py`（新增）：`PI_POC_ALLOWED_TOOLS` 白名单、
  `PIPOC_PROFILE`（仅 history+artifact 分类）、`GetSessionContextTool`（受限会话上下文）、
  `PublishArtifactsTool`（复用 ArtifactPublicationService，幂等）、
  `build_pi_internal_registry`（只注册 11 个白名单工具，无 MCP/计算/Draft 直写）。
- `backend/app/pi_runtime_poc/service.py`：加 `worker_id` 参数与 `execute_internal_tool`
  （token → Run → Registry.execute；publish 前确保当前 worker 活跃租约；
  UnknownToolError → 404）。
- `backend/app/pi_runtime_poc/router.py`：新增 `POST /runs/{run_id}/internal-tools`
  （body 用既有 PiInternalToolRequest，extra=forbid 拒绝伪造身份键）。
- `pi-runtime/src/extensions/internal-tools.ts`（新增）：白名单常量、
  `isAllowedInternalTool`、`PiInternalToolsClient`（白名单外直接拒绝、不发网络请求）。
- `pi-runtime/src/http/client.ts`：加 `executeInternalTool`（token 只在 Authorization）。
- 测试：`pi-runtime/tests/internal-tools.test.ts`（5 例）、
  `backend/tests/pi_runtime_poc/test_internal_tools.py`（9 例，SQLite 内存库隔离）。

### 验证结果

- 红灯：前端 internal-tools 模块缺失 collection fail；后端 internal_tools 模块缺失
  `ModuleNotFoundError`。
- 绿灯：
  - `cd pi-runtime && npm test` → **27 passed**（5 文件，含 84081b9 恢复后的 29 passed 复跑）；
    `npm run typecheck` → 通过。
  - `cd backend && .venv/bin/pytest --confcutdir=tests/pi_runtime_poc tests/pi_runtime_poc/test_internal_tools.py -q`
    → **9 passed**；`tests/pi_runtime_poc` 全目录 → **37 passed**。
  - `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc app/api/router.py`
    → **All checks passed**（--fix 清理 20 个冗余 noqa 后零剩余）。
  - app openapi 确认 `/api/v1/internal/pi-poc/runs/{run_id}/internal-tools` 端点注册。

### 安全/数据隔离

- 后端测试明确用 SQLite 内存库，不连接任何 MySQL 库；import 链到达 mcp.py 时以
  非敏感占位环境变量通过 Settings（engine lazy 不连接）。
- 伪造 user_id/session_id/run_id/worker_id：Registry 剥离保留键 + HTTP Schema
  extra=forbid 拒绝（双重防护）。
- 未提交/读取/修改任何 `.env`；无方案 B/C、积分、License、管理端、并发改动。

### 遗留事项

- `tests/agent_artifacts` 套件绑定根 conftest（kol_insight_test），简报禁止连接，
  且 --confcutdir 下 db_session fixture 不可用，本环境不运行；复用验证由
  test_internal_tools（brand build → publish → export 全链路）覆盖。
- Task 7 runner 负责完整 Run 生命周期与上下文重建；Task 8/9 在真实
  kol_insight_pi_poc 库整链路验证。

---

## 方案 A Gate A Task 5 复审修复：发布租约与 Builder 受控反馈

### 背景与目标

独立审查发现内部工具桥会在发布时自行抢占 Run 租约，绕过 Runner 的当前执行权；同时
Builder 返回摘要未包含 Pi 根据 gaps 补查所需的完整受控 availability、coverage 和
Evidence lineage 引用。

### 主要改动（关键文件）

- `backend/app/pi_runtime_poc/service.py`：`publish_artifacts` 仅接受当前 worker 已持有的
  未过期 Run 租约；无租约统一 409 `pi_run_lease_not_held`，不再由工具请求抢占租约。
  该职责留给 Task 7 Runner。
- 同一服务在六类 Builder 成功后，从当前 Run 所有的最新 Draft Revision 读取并有界投影
  `availability`、章节 `coverage`、`limitations` 与 Evidence `artifact_path/source_path` 引用；
  不返回正式 payload 或原始 Evidence。
- `backend/tests/pi_runtime_poc/test_internal_tools.py`：新增无租约发布拒绝、显式持有租约
  的成功发布、受控 Builder feedback 与真实跨用户 Evidence 归属测试。

### 验证结果

- 红灯：无租约发布用例原先未抛错；Builder feedback 原先缺少 `availability`。
- 绿灯：两项回归分别通过；真实跨用户 Evidence 归属用例通过。

### 遗留事项

- Task 7 Runner 必须在启动 Pi 前取得并持续续租；Task 5 内部工具桥只校验、不抢占。
