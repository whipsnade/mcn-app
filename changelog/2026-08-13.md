# 2026-08-13

## Scenario 2 生产链路修复：离线实现与回归

### 背景与目标

本日处理真实功能 UAT 中 Scenario 2 的失败链路。只读取脱敏 operator report 作为事实，未修改或清理旧 Run、旧 UAT 数据及历史 Evidence；本轮没有调用真实模型、DataTap、钱包，也没有创建真实 UAT round。

失败 Run `3c3e6a2d-f020-453a-aa05-4e7bdb5e58b5` 的已知事实：

- Run 已为 `completed`，只有 Attempt 1；模型请求 40 次，DataTap ToolCall 21 次。
- ToolCall 为 `settled=4`、`failed=14`、`unknown=3`；约 10 次 `succeeded_empty` 已扣费但没有写入 Evidence。
- 三次 `result_unknown` 共保留 30 积分；事件总量 27,199 条。
- `brand_report_v3`、Publication Version、Excel/BI 绑定均为 0。
- 模型反复调用 `build_brand_report_draft`，因无可用 Evidence 失败，最后只输出失败说明。
- 后端 terminal 只检查 assistant message，Gateway 在 worker 正常退出后直接发送 `completed`。

### 主要改动

- Run 创建按已审核 capability pack/profile 固化 capability allowlist、pack version、manifest digest 到不可变 RuntimeSnapshot；不再从 Profile、用户文本、模型输出或 Builder 调用推导 required artifact。terminal、Recovery、ACK-loss 和 force-complete 只读取该快照。
- Gateway/adapter 与后端共同执行严格 `mcp_result_v1` 判别联合：`available` 必须是非空 `structuredContent`，`empty`/`unavailable` 禁止结构化载荷；仅允许唯一且整体合法 JSON 的审核 MCP text block，普通文本、resource、图片和临时路径不能成为 Evidence。
- 明确 success/empty/unavailable/failure/unknown 的结算语义，持久化 Pi upstream request/logical call id；reconciliation 二次执行 allowlist schema 校验，unknown 不自动释放、不自动扣除。
- 抽出统一 CompletionValidator，覆盖正常 terminal、terminal ACK 丢失 Recovery、force-complete 和恢复重试；当前 Run 已自主生成产物时严格校验 Publication/Version/lineage 与归属，required artifact 不再是新 Run 的服务端必达门禁，unresolved MCP 仍受平台门禁约束。
- 保留跨 Attempt 持久化 LoopGuard 的错误指纹/Evidence 集合版本/阈值观测，但取消 Builder/Search 业务熔断和强制终态；高层 `max_decisions` 仍是唯一通用决策 fuse。
- thinking/message delta 按 4 KiB、32 fragments、50ms 合并；语义边界、worker finally、取消/中止、provider/decision failure、child exit 及 terminal 前均 flush/drain，保留默认有界 Gateway buffer。
- 修正离线 UAT harness 清理 `agent_tool_call_reconciliations` 的错误键列，避免测试基础设施残留影响后续 topology。

### 红灯与根因

- 先用脱敏的真实 adapter 结果形状补充非空 structured result、唯一合法 JSON text、空结果、多个 text、普通文本、UI resource、图片、路径、过大载荷、明确失败和 unknown 的测试；修复前结果包络不完整时无法安全进入后端 Evidence/结算链路。
- completion 负例先覆盖「有 assistant 但无 brand_report_v3」「历史 Version」「running Step/ToolCall」「unknown」「无效 lineage」；修复前 assistant-only terminal 会错误完成 Run。
- builder/search 重复错误、跨 Attempt、delta 高事件量和所有终止边界先补红；修复前无持久化 guard，Gateway 依赖大缓冲吸收洪泛。
- 另发现直接使用具体 output schema 的普通 `AgentMcpTool` 不应被 DataTap service 的 `{result: string}` 包装规则全局误判；最终改为由 canonical envelope 加具体审核 output schema 做最终信任边界，DataTap Pi allowlist 路径仍保留其专用 wrapper 校验。

### 红绿灯修订

- 首次后端非集成复跑暴露 4 个问题：旧 takeover 断言仍要求 unknown 强制失败、terminal 测试替身缺少 warnings 字段、两个 Pi POC 桥接测试缺少 worktree 依赖；均已定位并修正/安装锁定依赖，未放宽生产契约。
- 最终后端非集成全量：`2054 passed, 9 skipped`；`ruff check app tests` 通过。
- `pi-gateway` Vitest：25 files、196 tests passed；typecheck/build 通过。
- 完整 fake topology 离线 Pi UAT：`27 passed in 415.46s`；仅使用本地 fake model/MCP，未调用真实模型、DataTap、钱包，未创建真实 UAT Run。
- `backend/.venv/bin/ruff check app tests`：`All checks passed!`。
- `git diff --check`：通过。
- 独立代码审查目标：`Critical 0 / Important 0`；最终审查重点确认没有 Profile→required contract 隐藏映射、assistant-only terminal、unknown 自动 replay/release 或 Builder/Search 业务终止。
- 离线 UAT 期间临时创建的 worktree `backend/.venv` 软链接已删除，主仓库虚拟环境未触碰；本轮测试生成的 12 个 xlsx 已移至 `/private/tmp/scenario2-repair-artifacts/`。

### 遗留事项与风险

- 真实 Scenario 2 尚未重跑；本记录不代表 B7 PASS、生产就绪或任何真实 UAT 通过。
- 失败 Run 的 30 积分 reserved 本轮未直接写库处理，待后续独立 reviewer 决定是否通过正式 admin reconciliation 收口。
- 真实 adapter/provider 的未覆盖字段形状仍需在重新授权的功能 UAT 中确认；若上游违反已审核 output schema，系统应保持 fail-closed。
- 本轮停止状态只能是 `READY_FOR_FUNCTIONAL_SCENARIO_2_RERUN_REVIEW`；不得复用旧授权或继续 Scenario 3–7。

## 架构转向：Pi 透明 MCP 结果与 Artifact Skill（覆盖本文件前述旧方向）

### 背景与目标

用户在本日明确作出新的架构决策：DataTap MCP 是自研、受控的内部服务，Pi 主链路不再承担 Evidence Bridge 的二次解析、归一化、业务 Schema 复核或可信判定。本节覆盖前述同日的 `mcp_result_v1`/Evidence Bridge 实施方向；历史事实保留，旧数据、旧 Evidence 和旧 Run 不改写。本轮仍只做离线开发与验证。

### 主要改动（关键边界）

- 标准 Pi adapter 将 MCP Tool Result 原样交给模型；accounting hook 只观察调用并发送严格的 metadata-only finalize，不读取或替换 `event.content`，不把业务 payload、text/resource/image、临时路径或模型上下文带入 IPC/控制面。
- Gateway 保留 tenant/run/tool 身份、allowlist、secret 隔离、预留、logical_call_id、幂等及 settle/release/unknown 状态；ACK 丢失不改写模型结果、不创建 Attempt 2。unknown 不自动 replay、release 或扣除。
- Run Snapshot 只固化服务端审核 capability pack/profile 形成的候选 `allowed_artifact_contracts`、pack version 与 manifest digest；用户文本、模型输出和 Builder 调用不决定 contract。历史 required 字段只作为旧 Snapshot 兼容读取，Pi worker 不接收也不消费 required contract。
- Artifact Skill 的 `build_artifact_draft` 接收模型自主选择的 `artifact_type`、严格 payload 和可选的当前 Run ToolCall ID；不读取 Evidence。父 Artifact Version 仅允许同一 Session 的已发布 lineage 引用，不复制跨 tenant/session/Run 的 payload。Publication、Version、lineage 与 BI/Excel 同版约束继续有效。
- 统一 `CompletionValidator` 覆盖正常 terminal、ACK-loss Recovery、Recovery 与 force-complete；无 Artifact 可以正常文字完成，Evidence 数量不构成完成门禁；实际发布的 Artifact 仍需满足当前 Run 和 Snapshot allowlist 的完整链路。

### 红灯 → 绿灯与验证结果

- direct MCP 结果、metadata-only accounting、unknown/ACK-loss、Builder 无 Evidence、非法 Artifact Schema、父 Version 钻取和 completion gate 先红后绿；完整 fake topology 离线 Pi UAT 串行 `27 passed in 410.68s`。
- 后端全量（从 `backend/` 执行）`2103 passed, 22 skipped in 616.73s`；Ruff `All checks passed!`。
- Pi Gateway 全量 Vitest `26 files / 177 tests passed`，typecheck/build 通过；补充覆盖 SDK 仅 `isError=true` 的 confirmed Tool Error 分类；pi-runtime Vitest `9 files / 47 tests passed`、typecheck 通过；桥接 POC `6 passed`。
- 前端 Vitest `40 files / 305 tests passed`，tsc 与 Vite build 通过；build 仅保留既有 chunk size warning。
- 静态审查确认 Pi production path 不调用 `parse_mcp_result_details`、`_validate_mcp_output`、`EvidenceWriter.write`，不注册 `search_evidence`/`read_tool_result` 报告必经链路；legacy/current Evidence 模块仍兼容保留。

### 遗留事项与边界

- 未调用真实模型、真实 DataTap 或钱包，未创建真实 UAT Run，未进入 Scenario 3–7；历史失败 Run 的 reserved=30 未处理，旧 UAT/evidence 未修改。
- 真实 Scenario 2 尚未重跑；本节不代表 B7 PASS、生产就绪或 direct smoke review 已批准。
- pi-runtime 本地依赖安装时 npm audit 报告依赖树存在 2 个 moderate、2 个 high advisory；这不改变本轮离线验证范围，需由依赖治理单独评估。

### 当前停止状态

`READY_FOR_DIRECT_MODEL_MCP_SMOKE_REVIEW`

## 架构冻结与提交（Direct MCP 架构冻结 + 真实 Smoke 执行会话）

### 冻结前检查

- worktree `real-mcp-evidence-bridge-repair`、分支 `codex/real-mcp-evidence-bridge-repair`、
  HEAD `e00690fb6916d3ce6c0f264f6fb4ceeeeb9f667e`；49 tracked 修改 + 5 untracked 新文件，
  `+1267/-2116`，`git diff --check` 干净，无 .env/密钥/二进制/xlsx 混入。
- 三条并行只读审查 lane：范围扫描（0 越界：admin/service.py +86 属计划内 Pi unknown
  metadata-only 对账）、TS 侧审查、后端审查。逐文件对照设计/计划，全部改动落在
  Pi 透明 MCP 结果、metadata-only accounting、Artifact Builder、统一 CompletionValidator
  与 supersede 文档范围内。

### 红灯与修复（TS 计费分类 fail-safe 收紧回归）

- TS 审查发现 1 Important：未知 adapter error code 由 fail-safe `result_unknown`（保持预留）
  收紧为 `failed_confirmed`（释放预留）——handler 把 `hasErrorMarker` 一并传入分类器的
  `isError` 参数，违反设计 §2.2「已外发但无法确认 → unknown 保持预留」，存在外发后漏扣
  10 积分的窄缝。
- TDD 修复：先红（`some_future_error` 在 isError=true/false 两态下均须 `result_unknown`，
  2 failed 复现）→ `classifyMcpFailure` 兜底仅对「标准 MCP isError 且无 adapter error
  code」归 failed_confirmed；handler 只把 `event.isError === true` 传入 → 绿灯。
- 回归：pi-gateway 全量 Vitest 26 files / 179 tests 绿，typecheck 通过。
- 审查 Minor（跨端 upstream_request_id 字符集不对称、重复计算 requestIdFrom、
  build_artifact_draft wire 描述允许空对象但后端 fail-closed）记录在案，无安全影响。

### 后端审查结论（8 项专项全 PASS，Critical 0 / Important 0 / Minor 3）

- Pi production path 无 `parse_mcp_result_details`/`_validate_mcp_output`/`EvidenceWriter.write`/
  `mcp_result_v1`；finalize 严格 metadata-only（extra=forbid + 白名单 + 字段级校验）；
  `build_artifact_draft` 仅从 RuntimeSnapshot allowlist 取 artifact_type、payload 走
  `extra=forbid` Schema、归属 fail-closed、无 Evidence 可建 Draft；`session_analyst_v1` 只提供
  候选能力、历史 required 字段仅兼容读取不被 Pi worker 消费；CompletionValidator 三入口
  （terminal/Recovery/force-complete）统一复用，不依赖 Evidence/required artifact，对 running
  Step/ToolCall/permit/active Draft fail-closed；durable-before-send 与 unknown 不 replay 保持；
  legacy/current 路径完整保留。
- 3 个 Minor 记录在案（direct_model_payload 判定来源 create 硬编码 vs publish 动态查 backend；
  `freeze_model_direct` 的 source_tool_call_ids 参数当前无调用方；admin 对同一 Run 重复查询），
  均为实现脆弱性/冗余，不影响 C0/I0 目标，本轮不扩改动面。

### 提交拆分（线性，无 amend/rebase）

1. `refactor(pi): pass standard mcp results directly to model`（29 文件）——backend pi_gateway
   元数据化 finalize/失败分类、TS Gateway 透明 Tool Result 旁路计费、Pi recovery 不再实例化
   legacy AgentMcpTool、admin Pi unknown metadata-only 对账、TS 计费 fail-safe 修复。
2. `feat(artifacts): build allowlisted drafts from model payload`（20 文件）——`build_artifact_draft`
   统一 Builder、agent_artifacts direct-model lineage/发布链、统一 CompletionValidator
   （移除 required artifact，三入口复用）。
3. `test(pi-uat): cover direct mcp result architecture`（1 文件）——离线 UAT 脚本切
   build_artifact_draft 直提 payload 路径。
4. `docs: supersede mcp evidence bridge design`（4 文件）——新设计/计划 + 旧设计 superseded
   标记 + 本 changelog。

## UAT 契约纠偏：audited baseline 固化与 Scenario 2 再授权契约（纯文档）

### 背景与目标

- 真实 Direct Model + MCP Smoke（round `DIRECT_MODEL_MCP_SMOKE_20260813T103101Z_c01ec1ba`）
  已执行：Run completed、Attempt 1、模型请求 3 次、生产 dispatch 恰 1 次、钱包净支出 10、
  数据库 Evidence 增量 0（新架构预期）、Artifact 0（本 Smoke 不要求）。结论：
  `DIRECT_MODEL_MCP_SMOKE_FUNCTIONALLY_ACCEPTED_WITH_PROTOCOL_DEVIATION`——直连对照调用
  2 次超出授权上限 1 次，如实记录，不表述为严格零偏差。
- 本轮把 `c01ec1ba` 固化为新的 audited Direct MCP baseline，纠正旧 B7/UAT 文档中过时的
  Evidence Bridge / mcp_result_v1 / required artifact 现行规则，定义 Web Functional
  Scenario 2 的单场景授权与验收契约。纯文档，不改代码/测试/迁移，不调用外部服务。

### 主要改动（关键文件）

- 新建 `docs/qa/2026-08-13-direct-model-mcp-smoke-review.md`：Smoke 事实、偏差披露、
  接受结论与边界声明（不含完整 MCP payload/凭证）。
- `docs/superpowers/plans/2026-08-12-real-b7-uat-authorization-plan.md`：状态头更新；
  §0/§1.1/§1.2/§7.3 execution gate 改锚 `c01ec1ba…`（`68deca58…`/`61576f7…`/`f7ab159…`
  降为历史事实）；新增 §0.4 架构纠偏说明（验收证据 ≠ 数据库 Evidence）；§4.2 内部工具
  改现行 6 个（历史 13 个标注废止）；L2-01/02/03/04/05/06 按「场景 PASS 断言 ≠ Runtime
  门禁」重写；L2-17 改名去 Evidence；新增 §8 单场景授权契约（预算紧急上限、输入自主
  边界、启动规则）。
- `docs/qa/2026-08-12-pi-b7-uat-authorization-pack.md`：状态头更新；§2.3/§2.4/§3-11
  按「验收证据 ≠ 数据库 Evidence」纠偏；§5.3 execution gate 改锚 `c01ec1ba…`；新增
  §5.4 单场景授权模板（WEB_FUNCTIONAL_SCENARIO_2）。
- `docs/runbooks/pi-agent-gateway.md`：状态头更新为
  `READY_FOR_WEB_FUNCTIONAL_SCENARIO_2_REAUTHORIZATION`；migration head 纠偏为 0044
  （0043 标历史）；`max_decisions` 澄清为紧急上限而非工具规划策略（Scenario 2 建议 60）。
- `docs/qa/pi-agent-gateway-local-uat.md`：2026-08-13 更新（三）——架构转向说明与 Smoke
  接受记录，历史内容不重写。
- `docs/superpowers/plans/2026-08-09-pi-agent-gateway-plan-b.md`：顶部 SUPERSEDED 说明
  （历史实施计划不改写正文）。
- `docs/superpowers/plans/2026-08-13-pi-direct-mcp-result-artifact-skill-plan.md`：交付
  状态更新（Smoke 接受 + 新基线 + 下一状态）。

### 验证结果

- 身份核验：branch `codex/real-mcp-evidence-bridge-repair`、HEAD `c01ec1ba…`、工作树干净、
  `e00690fb` 线性祖先。
- `rg` 自检：`68deca58`/`61576f7`/`mcp_result_v1`/`required artifact` 等逐条分类
  （历史事实保留并标注 / 现行规则已纠正 / 验收证据未误删）。
- secret 扫描（API key/JWT/Bearer/私钥/DSN/手机号）0 命中；`git diff --check` 通过。
- 纯文档：`git diff --name-only` 仅 Markdown/changelog。
- 独立文档审查：Critical 0 / Important 0 / Minor 3；3 个 Minor（旧 B7 L1-SMOKE 口径
  「2 次模型请求/max_decisions=2/50」未逐处标注历史）已追加修复——§6.2/§6.3、授权包
  §3-13/§5.3、计划 §0 事实判读段逐处标注「历史 B7 流程口径」并指向 §8/§5.4 现行契约。

### 遗留事项

- 状态停止于 `READY_FOR_WEB_FUNCTIONAL_SCENARIO_2_REAUTHORIZATION`；真实 Scenario 2
  必须由用户按授权包 §5.4 模板逐字重新授权，execution HEAD 由授权消息确认。
- 历史失败 round、历史 reserved=30、旧 evidence 目录均未触碰。
- 本记录不构成 Scenario 2 PASS、B7 PASS 或生产就绪。

## 提交 1：Direct Artifact 模型输入 DTO + 服务器组装边界 + 结构化错误反馈

### 背景与目标

Scenario 2 失败根因之二：`BuildArtifactDraftTool` 把模型 payload 直接当完整
`BrandReportV3` 发布 payload 校验，模型必须手写 schema_version/module/data_status/
methodology/canonical_data/field_lineage，其中 canonical/lineage 须精确覆盖 data
全部叶子——模型在 60 次决策内 80 次调用全部失败。本提交实现修复第一步：
把「模型输入」与「发布 payload」解耦，服务器确定性组装，并给出结构化字段级错误。

### 主要改动

- 新增 `app/agent_artifacts/model_inputs/`（提交 1）：
  - `__init__.py`：`INPUT_SCHEMA_VERSION="direct_model_input_v1"`、
    `MODEL_INPUT_BY_ARTIFACT_TYPE`（brand_report_v3→BrandReportV3Input、
    campaign_report_v2/v3→CampaignReportV3Input（共享，schema_version 参数化）、
    kol_selection_v3→KolSelectionV3Input、insight_board_v1→InsightBoardV1Input）、
    `assemble_model_payload` 分发、`model_input_contract`（后续提交注入 Skill 上下文；
    `model_input_schema` 取 DTO `model_json_schema()` 单一事实源）、
    `SERVER_OWNED_PAYLOAD_KEYS`。
  - `brand.py`：BrandMethodologyInput + BrandReportV3Input（复用 payloads 类，
    extra=forbid+frozen）+ `assemble_brand_payload`（推导 data_status、组装
    methodology、`publish_canonical(data, refs=[], partial_paths=非 complete 章节
    叶子, module="brand")` 生成精确覆盖全部叶子的 canonical/lineage）+ concise_example。
  - `campaign.py`/`kol_selection.py`/`insight.py` 同构；kol/insight 非
    CanonicalPayloadMixin，组装输出无 canonical_data/field_lineage。
- `BuildArtifactDraftTool._execute`：snapshot allowlist 后解析模型输入 DTO →
  顶层保留键显式拒绝（`server_owned_field_rejected`，JSON 列路径与说明）→
  DTO model_validate（ValidationError → `draft_build_error` +
  `format_model_input_errors`）→ `assemble_model_payload` → 后续 business
  提取/insight parent 处理改读 final_payload → `_persist(payload=final_payload,
  direct_model_payload=True)` 不变。
- 新增 `app/agent_artifacts/payload_errors.py`：`_error_entries`（RFC6901 path +
  type + reason + retryable；msg 只用 `errors(include_context=False)`，绝不写
  ctx/input）、`format_payload_errors` / `format_model_input_errors`（error_code +
  errors + total_errors + truncated，序列化 ≤2048 字节：逐条减少、首条超长截断
  reason）。`builders._persist` 与 `artifacts._payload_error_summary` 改输出该 JSON。

### 验证结果

- `tests/agent_artifacts/model_inputs/test_model_inputs.py`（16）+ 重写的
  `tests/agent_runtime/tools/test_direct_artifact_builder.py`（8，模型输入形态）+
  `tests/agent_artifacts/test_direct_publish.py`（13）共 37 通过；ruff 全绿。
- `tests/agent_artifacts/` 目录另有 7 个既有失败（HEAD 上同样存在，测试库
  agent_artifacts 表有持久脏数据导致全局计数断言失败），与本次改动无关，
  未纳入本提交范围。

### 遗留事项

- `model_input_contract` 注入 Skill 上下文（提交 2+）与 insight parent module
  权威覆盖（当前 InsightBoardV1Input.module 默认 "brand"）。

## 提交 2：把精确模型输入契约交给模型

### 背景与目标

提交 1 建立了模型输入 DTO 与服务器组装边界，但模型在运行时仍看不到精确
Schema（Skill 正文只有描述性说明，模型靠猜构造 payload）。本提交把
`model_input_contract`（DTO JSON Schema 单一事实源 + 合法示例 + 发布预期）
经 `load_marketing_skill` 随 Skill 正文一起交给模型，并修正提交 1 的洞察缺陷
（insight DTO 曾暴露服务器拥有字段 module）。

### 主要改动

- `LoadMarketingSkillTool.execute`：skill 的 `artifact_contract` 已在
  `MODEL_INPUT_BY_ARTIFACT_TYPE` 注册时，返回体追加 `model_input_contract`
  （= `model_input_contract(artifact_contract)`，含 model_input_schema/
  concise_example/required_tools/publication_expectations，不截断）；整体
  JSON 序列化 >512 KiB 时 fail-closed 拒绝（`marketing_skill_contract_too_large`）；
  snapshot 审计与 safe_summary 形态不变。
- `InsightBoardV1Input`：移除 `module` 输入字段（服务器拥有字段）；
  `assemble_insight_payload` 固定 `payload["module"]="brand"`。
- pi-gateway `internal-tools.ts`：`load_marketing_skill` 与 `build_artifact_draft`
  的 description 更新，明确模型按 `model_input_contract.model_input_schema`
  构造 payload、服务器补齐服务器字段。
- 测试：新增 `tests/agent_runtime/test_pi_internal_tools.py`（7 用例）；
  `test_model_inputs.py` 追加 module 服务器字段断言；POC
  `test_internal_tools.py` 的 payload key 断言适配（+model_input_contract）。

### 验证结果

- backend：ruff 全绿；定向 32 passed（model_inputs 17 + test_direct_artifact_builder 8
  + test_pi_internal_tools 7）；POC test_internal_tools 22 passed；
  agent_runtime+agent_artifacts+pi_runtime_poc 866 passed。
- pi-gateway：typecheck 通过（npm install 后）；vitest 179 passed。
- test_mcp.py 的 2 个 wall-clock-timeout 用例偶发失败为测试库残留数据
  （agent_runs 64 行/agent_sessions 15 行，FK 自引用删除冲突）引起的环境问题，
  HEAD 上同样存在（`counts_toward_fine_grained_breaker` 稳定失败），与本次无关。

### 遗留事项

- capability pack 契约文件仍指向旧 release Schema（下一提交升级 pack）；
  insight parent module 权威覆盖（父 Artifact module 决定 payload module）待定。

## 提交 3：capability pack 1.1.0 升级 + 离线 UAT 改造 + result_unknown 元数据可观测性

### 背景与目标

1.0.0 pack 的 skills required_tools 仍含已废止 Builder/检索工具，SKILL.md/
root-policy 仍要求 Evidence 必经——与 Pi production 工具面矛盾。本提交新增
不可变 marketing-v2（1.1.0），把模型输入契约写进 Skill 正文；并补进程级自纠错
UAT（缺字段失败 → 按结构化错误修正 → 发布）与 result_unknown 的 metadata-only
可观测性（真实 round 14 个 unknown 均为 adapter call_failed，不改分类语义）。

### 主要改动

- 新增 `packs/marketing-v2/`（复制 v1 后：pack_version=1.1.0、skills version=1.1.0、
  required_tools 全部改为 Pi production 工具面、builder/exporter versions=1.1.0）；
  重写 6 个 SKILL.md 与 root-policy.md（model_input_contract 驱动、无 Evidence
  必经、禁绝 search_evidence/read_tool_result/mcp_result_v1/旧 Builder 字样）；
  digest 用临时脚本逐文件 SHA-256 重算写回 manifest；`marketing-v1/` 一字未动。
- `runtime.py`：`load_manifest("marketing-v2")`。
- 测试：test_loader 加 v2 仓库级对齐断言；test_marketing_capability_runtime
  版本断言 1.1.0。
- UAT：`payload_fixtures.brand_model_input()`（JSON 安全）；`_build_direct_brand_step`
  改用模型输入；`_drilldown_step` 的 insight 改为模型输入形态（含父 Version
  绑定）；新增 `test_direct_artifact_self_correction_bounded`（Run completed、
  Attempt 1、MCP 1 次 settled=10 分、build_artifact_draft 恰 2 次、evidence 增量 0、
  BI/Excel 同版、无 event_buffer_overflow）。
- exporter 修复：`export_artifact` 在 payload 重校验时启用
  `model_direct_lineage_context`——否则已发布 direct payload（canonical 无
  Evidence）导出必 409（提交 3 现场复现 43 个 ValidationError）。
- result_unknown 可观测性：`PiGatewayMcpFailureMetadata` 加 error_class/
  received_jsonrpc_response/dispatch_phase/is_standard_mcp_error/
  upstream_request_id（后端 + pi-gateway TS 白名单校验一致）；TS 上报点用
  `buildMcpFailureMetadata` 附加可确定字段（classifyMcpFailure 一行未改）；
  `_safe_metadata_message` 输出紧凑 JSON（含非空子集），分类语义不变
  （unknown 仍保持预留）。

### 验证结果

- backend：ruff 全绿；验证命令 165 passed（marketing_capability_pack + model_inputs
  + test_pi_internal_tools + test_direct_artifact_builder + pi_gateway）。
- pi-gateway：vitest 179 passed；typecheck、build 通过。
- 离线 UAT：新自纠错场景本地跑通（1 passed）；完整 UAT 文件 27 passed，
  仅 `test_current_to_pi_to_current_and_kill_switch_only_affects_new_runs` 失败
  （current runtime 路径 model_error，HEAD 上同样失败，与本次无关）。
- pi_runtime_poc 的 test_task8b_bridge 2 个失败为 pi-runtime node_modules 缺失
  （HEAD 上同样失败）；tests/agent_artifacts 既有脏数据失败（提交 1 已记录）不变。

### 遗留事项

- UAT 完整文件含 1 个既有失败（current runtime model_error）待单独排查；
  pi-runtime 依赖安装后 test_task8b_bridge 应恢复。

## 提交 4：文档与状态更新

### 背景与目标

提交 1–3 完成 Direct Artifact Skill 契约修复的离线实现与验证后，本提交把准确事实与
状态同步进设计/计划/授权/QA/runbook 文档，并把本日三个提交与验证结果收口到当日 changelog；
只改文档，不改代码/测试。

### 主要改动

- `docs/superpowers/specs/2026-08-13-pi-direct-mcp-result-artifact-skill-design.md`：状态头
  更新为 `READY_FOR_WEB_FUNCTIONAL_SCENARIO_2_RERUN_REVIEW`；§2.3 改写为 typed model input
  提交形态（模型禁止服务器字段、服务器选 DTO → 严格校验 → 确定性组装 → canonical 从 data
  生成）；新增 §2.4「模型输入契约暴露」、§2.5「capability pack 版本化」；可验证不变量清单
  同步更新。
- `docs/superpowers/plans/2026-08-13-pi-direct-mcp-result-artifact-skill-plan.md`：交付状态
  追加本分支 3 提交完成记录，下一状态 `READY_FOR_WEB_FUNCTIONAL_SCENARIO_2_RERUN_REVIEW`。
- `docs/superpowers/plans/2026-08-12-real-b7-uat-authorization-plan.md`：状态头追加契约修复
  完成事实；§0 表新增 Direct Artifact Skill 契约修复行、execution commit 区间允许 3 修复提交、
  当前交付状态更新；§8.5 场景验收口径改为 A/B/C 三档（禁止把单纯文字 completed 判为 PASS）。
- `docs/qa/2026-08-12-pi-b7-uat-authorization-pack.md`：状态头同步；§5.4 单场景模板新增
  A/B/C 三档验收口径与 `ACCOUNTING_UNKNOWN_DIAGNOSTIC_REQUIRED` 遗留项说明。
- `docs/runbooks/pi-agent-gateway.md`：状态头更新；新增「Direct Artifact Skill 契约」节
  （capability pack 版本 / 模型输入契约 / result_unknown 可观测性）。
- `changelog/2026-08-13.md`：追加本「提交 4」段（提交 2/3 段已在对应提交时写入）。

### 验证结果

- 三个提交的验证（累计）：backend 定向 165 passed；agent_artifacts 目录 591 passed；
  pi-gateway vitest 179 passed + typecheck + build 通过；离线 UAT 27 场景通过（含新增
  自纠错场景 `test_direct_artifact_self_correction_bounded`）。
- 环境根因闭环：此前 test_mcp wall-clock 用例的波动失败源于测试库残留 uvicorn 进程
  （agent_runs 残留行导致 FK 自引用删除冲突）；清理进程与测试库后
  `tests/agent_runtime/tools/test_mcp.py` 53 passed。
- `git diff --check` 通过；改动的 md 均无 secret 命中。

### 遗留事项

- `ACCOUNTING_UNKNOWN_DIAGNOSTIC_REQUIRED`：真实 round 的 14 个 unknown 均为 adapter
  `call_failed`（DataTap 抛异常问题），待独立诊断；不阻塞下一轮核心功能 Scenario 2 重跑，
  但阻塞完整 B7 PASS 与生产切流。
- 离线 UAT 完整文件仍含 1 个既有失败（`test_current_to_pi_to_current_and_kill_switch_only_affects_new_runs`，
  current runtime `model_error`，HEAD 上同样存在）待单独排查；pi-runtime 依赖安装后
  `test_task8b_bridge` 应恢复。
