# 2026-08-09

## B0 Gate 加固 R2

### 背景与目标

复审发现 Task 6 的 hard checks 仍可被 Artifact 自报布尔值绕过，Task 7 回放只验证预制
结果而没有执行 B0 数据链路，Task 5 的 KOL v3 scope 还存在发布门禁旁路。本轮只修复
这些 Gate/Replay 边界，不进入 B1–B7、方案 C 或真实 Task 9。

### 主要改动

- `backend/app/pi_runtime_poc/gate.py`：仅根据 published payload、canonical fields、
  field lineage、structured/narrative claims、fixture Evidence manifest、Version/Run/Session、
  availability 与 limitations 计算十项 hard checks；Evidence manifest 是唯一信任根，
  结果携带的 Evidence 副本还必须逐项匹配 fixture 的 value/unit/path。KOL v3 额外校验
  published/validation/version、候选 Evidence 身份、评分公式与叙事/限制披露；重复/未知/路径、
  scope、unit、candidate、Version 和零 DataTap 计数均 fail-closed。drilldown 必须提供前序
  published source artifact，并与 fixture 的 payload digest 精确一致。
- `backend/app/pi_runtime_poc/replay.py`：由 fake events 驱动 synthetic Evidence、当前
  Capability Pack snapshot、共享 Brand/Campaign/KOL Builder、`ArtifactPayloadValidator`、
  共享 publication validation、冻结的内存 Version 和正式 `export_artifact`；不读取预制结果，
  `hard_check_gate=PASS` 只由执行结果生成。导出摘要来自实际 XLSX bytes 的稳定规范化表示，
  只去除 renderer 写入的当前修改时间。
- `backend/app/agent_artifacts/offline_pipeline.py`、`publication_core.py`、`publishing.py`：
  抽取 direct/replay 共用的纯发布校验，并保留 direct publication 的候选 Version 与
  structured-claims 观测入口。
- `backend/fixtures/pi_runtime_poc/marketing_b0/`：仅保留脱敏 Evidence shape、scope、
  期望行为和事件输入；KOL fixture 使用完整 v3 scope，不保存待验证 hard-check 结论。
- `backend/tests/pi_runtime_poc/`：补充单位冲突、重复 Version、KOL scope/availability/评分维度、
  非法零调用、drilldown grounding、事件语义、Evidence/Version/路径/claims 篡改和共享入口行为测试。
- `docs/qa/pi-runtime-poc-rounds.md`：将 R2 前历史回放数字标为已覆盖，记录本轮真实共享链路
  与本地 synthetic 边界。

### 验证结果

- TDD 红灯：初始 R2 负向集在旧实现上为 **25 failed**；后续新增 availability、score/path
  负向也均先红后绿；最终定向门禁/发布回归为 **112 passed**。
- B0 后端全量 `tests/marketing_capability_pack tests/pi_runtime_poc tests/agent_artifacts`：
  **806 passed、9 skipped**。
- Pi Runtime `npm test`：**47 passed（9 files）**；`npm run typecheck` 通过。
- 全部修改 Python 文件 Ruff 通过；`git diff --check` 通过。
- 本轮测试生成的 xlsx 已移至 `/private/tmp/b0-gate-r2-final13-105500`（以及同轮此前的
  `/private/tmp/b0-gate-r2-final9-104500`、`/private/tmp/b0-gate-r2-final11-104900`、
  `/private/tmp/b0-gate-r2-final12-105200`），未留下工作树产物。
- 未调用模型、DataTap、钱包或积分；未运行、修改或重跑 Task 9。

### 遗留事项

B0 Gate 加固完成后停止，等待审核；不进入 B1–B7、方案 C、真实 UAT 或 Task 9。

## Pi Agent Gateway 方案 B 文件级实施计划

### 背景与目标

B0 本地 Gate 已完成加固，下一阶段需要在不改写历史真实 Gate A `EVALUATED_FAIL` 的前提下，形成
生产 Pi Agent Gateway 的可执行计划。本次只做架构与文件级规划，不实现 B1–B7，不调用模型、
DataTap、钱包或积分。

### 主要改动（关键文件）

- 新增 `docs/superpowers/plans/2026-08-09-pi-agent-gateway-plan-b.md`，把方案 B 拆为 13 个独立提交：
  Tenant/License、加密 Runtime 配置、Node Pi SDK Gateway、签名控制协议、公平队列与恢复、统一租户
  账务、用量对账、SSE、管理端、灰度回滚和最终本地验收。
- 固定生产实现继续使用现有兼容组合：Pi coding-agent 0.79.10、pi-ai/pi-tui 0.74.2、
  pi-mcp-adapter 2.20.1；历史 0.84.1 探针不作为升级依据。
- 明确 FastAPI 是唯一可信控制平面，Gateway 不直连 MySQL；MCP 必须在外发前完成 License/额度/
  10 积分 durable reservation；基础设施只允许一次 Attempt，unknown 不自动重放。
- 明确 B7 只完成本地多租户 UAT并停在 `READY_FOR_REAL_B7_UAT`。真实模型/DataTap/钱包 UAT、
  生产切流和方案 C 继续需要新的明确授权。

### 验证结果

- 已对照现有 AgentRun/Attempt/Step/ToolCall/Event、SSE、Recovery、WalletService、Admin API、Pi POC
  Extension/内部工具以及 Alembic 0036 head 校准文件和接口。
- 计划自审覆盖 B1–B7、租户隔离、密钥、调度、账务、事件、管理端、回滚和停止门；
  `git diff --check` 通过后提交。
- 本次未运行代码测试；未修改业务代码、数据库、前端或历史 round。

### 遗留事项

- 等 B0 开发会话完成当前修复并通过最终审查后，将本计划提交 cherry-pick 到其干净分支，再从
  Task 1 开始执行。
- 任何需要升级 Pi/adapter、放宽工具权限、真实外部调用或生产切流的情况必须停止并重新取得授权。

## 方案 B Task 9 编号消歧

### 背景与目标

此前“不得重跑 Task 9”的表述同时指向历史 Pi RPC/POC 真实六场景 Task 9，以及本方案 B
的 Task 9（B5-SSE），造成了执行边界歧义。本次只澄清编号，不改变任何历史真实 Gate 结果、
历史 round 或外部调用禁令。

### 主要改动

- 方案 B 计划明确禁止的是历史 Pi RPC/POC 真实六场景 Task 9；本计划 Task 9（B5-SSE）是
  获准的本地实现任务，保留原有文件范围、事件契约和验收标准。
- 停止规则同步改为：只有需要真实模型、DataTap、真实钱包、历史 Pi RPC/POC 真实六场景
  Task 9、生产切流或方案 C 才必须停止；B5-SSE 继续按计划执行。

### 验证结果

- 仅修改计划中的两处边界措辞并追加本记录；未修改历史 Gate A `EVALUATED_FAIL`、历史
  Task 9 结果或方案 B Task 9 的设计内容。
- 未调用真实模型、DataTap、钱包或积分，未启动历史真实 Task 9 runner，未创建真实 round。

### 遗留事项

- 重新完成方案 B 计划审查后，从 Task 1 开始按 13 个独立任务执行；B5-SSE 使用 fake provider、
  本地 fixture、测试库和自动化测试，真实外部 UAT、生产切流与方案 C 仍禁止。

## Task 0：稳定后端 Ruff 门禁

### 背景与目标

方案 B 启动门禁使用全局 Ruff 0.3.3 时，隐式默认规则报告了 335 项既有历史债务，无法区分
工具漂移与方案 B 回归。本 Task 只稳定项目工具和规则，不修改历史代码、不清理四个 POC 基线文件。

### 主要改动

- 创建项目 `backend/.venv`（Python 3.11）并安装项目开发依赖；Ruff 使用 `0.16.2`，满足
  `>=0.9,<1`，不改变 `pyproject.toml` 中已有版本范围。
- 在 `backend/pyproject.toml` 增加显式 `E4/E7/E9/F` 规则及四个已确认 POC 文件的最小逐文件豁免：
  `test_comparison.py`、`test_evidence_ingest.py`、`test_gate.py`、`test_internal_tools.py`。
- 在方案 B §0.3 记录版本检查、稳定规则和 335 项历史债务的来源与边界。

### 验证结果

- `.venv/bin/ruff --version`：`ruff 0.16.2`。
- Task 0 采用的验证命令继续使用 `.venv/bin/ruff`，不调用真实模型、DataTap、钱包或积分。

### 遗留事项

- Task 0 全部门禁已通过，可按同一 Ruff 配置进入方案 B Task 1–13。
- 335 项历史债务不在本 Task 修复范围内；历史 Pi RPC/POC 真实六场景 Task 9、真实 B7 UAT、
  生产切流和方案 C 仍禁止。

## Task 1（B1A）：租户身份、成员关系与 License 基础

### 背景与目标

为 Gateway 后续任务建立唯一的 FastAPI 租户控制平面：新用户与管理员创建用户必须获得个人租户、
成员关系和保守 License；Run、Session、内部 Run、重试、达人详情与离线 POC 创建路径固定租户，
并在新 Run 前执行 feature、有效期和并发授权。历史用户通过迁移获得确定性的 legacy tenant，
不把互不相关的用户合入共享钱包。

### 主要改动

- 新增 `app/tenancy`（Tenant、TenantMembership、TenantContext、用户租户解析）和
  `app/licensing`（TenantLicense、LicenseDecision、有效期/feature/租户及用户并发门禁）。
- 新增迁移 `0037_tenant_control_plane`：建立租户、成员、License 表，回填每个旧用户的 legacy
  tenant/membership/license，并将 `agent_sessions`、`agent_runs` 写入租户索引与归属列；downgrade
  先解除租户外键再删索引，并对非 legacy 成员 fail-closed。
- 身份服务与管理员创建用户在同一事务内 provision personal tenant；`GET /users/me` 及前端
  `ApiUser` 镜像 tenant id/name/role；消息、retry、utility、kol-detail、reviewer 与 Pi POC Run
  创建器均写入当前或父 Run 的 tenant_id。
- 仅允许固定 License feature slug，未知 slug/非布尔 flag 在 ORM 入口拒绝；生产模型与迁移将
  Session/Run 的 tenant_id 固定为非空，旧的直接构造测试 fixture 仅通过专用测试租户适配，所有
  生产创建入口不再猜测租户。

### 验证结果

- TDD 红灯：新增租户、License、身份 provision 与迁移测试在实现前因模块缺失失败。
- 绿灯：租户/License/身份/管理员聚焦 `18 passed`，迁移集成 `2 passed`，历史迁移回归 `25 passed`；
  `tests/agent_runtime` 全量 `783 passed`；B0 后端 `806 passed、9 skipped`；前端 Vitest `231 passed`。
- `.venv/bin/ruff`（Task 1 全部修改 Python）通过；前端 ApiUser 契约测试通过；未调用真实模型、
  DataTap、钱包或积分，未运行历史真实 Task 9。

### 遗留事项

- Task 2 将在此租户边界上增加不可变 Runtime 配置、加密密钥与 Run snapshot；本 Task 不实现
  Gateway、真实外部调用、B7 UAT、生产切流或方案 C。当前 Session Analyst 消息入口仍是
  KOL 圈选单一执行契约，License 使用 `kol_selection`；品牌/活动 Goal 接管后再按实际意图授权。

## Task 2（B1B）：版本化 Runtime 配置、加密密钥与 Run snapshot

### 背景与目标

在 Task 1 的租户与 License 控制平面上，为每个新 Run 固定不可变的 Runtime backend/config
snapshot；Pi 租户必须显式激活 tenant 级加密配置，current 继续使用脱敏的 legacy 配置。密钥
只允许在 `resolve_secret_bundle()` 这一条边界解密，历史 Run 不因后续配置切换而改变。整个 Task
只使用本地测试库、SQLite POC fixture 和 fake gateway，不进入方案 B 后续任务或任何真实外部调用。

### 主要改动

- 新增 `backend/app/runtime_config/{models.py,crypto.py,schemas.py,service.py,__init__.py}`：
  `RuntimeConfigSnapshot`/`RuntimeSecretBundle` 契约、AES-256-GCM key ring、AAD 绑定
  `tenant_id:secret_id:kind:key_version`、append-only config activation、legacy/current 与
  POC 隔离 snapshot、Pi secret bundle 运行时解密。错误均收敛为稳定 runtime config code，
  Pydantic/异常/快照只保留 masked origin、fingerprint 和版本信息。
- 新增迁移 `0038_runtime_config_secrets.py`：创建版本化配置与密文表，增加 Tenant active
  override，写入 `legacy-env-v1`/`poc-isolated-v1` 系统配置；AgentRun 先 nullable 扩列、按
  legacy snapshot 回填，再改为 non-null，并建立 `(tenant_id,runtime_backend,status,queued_at,id)`
  索引；旧迁移 0037 未修改。
- 主消息、retry、kol-detail、Utility、Reviewer、Pi POC 六类 Run 创建/继承路径均写入或校验
  `runtime_backend`、`runtime_config_version_id`、`runtime_config_snapshot_json`、`queued_at`；
  幂等重放沿用原 Run snapshot，Reviewer/Utility 不绕过父 Run 校验，POC 只接受 pi-poc marker。
- `.env.example`/Settings 增加 `RUNTIME_SECRET_MASTER_KEYS` 与 active key version；测试夹具为
  历史直接构造 Run 提供明确的 legacy test snapshot，不放宽生产入口。

### 验证结果

- TDD 红灯：实现前 runtime_config 测试收集阶段因 `app.runtime_config` 缺失失败；随后完成最小实现，
  runtime_config + Run snapshot 定向 **11 passed**，新增密文解析、嵌套凭证模式、secret bundle
  精确 Run 绑定与 snapshot 篡改测试均通过。
- B0 后端全量 `tests/marketing_capability_pack tests/pi_runtime_poc tests/agent_artifacts`：
  **806 passed、9 skipped**；`tests/agent_runtime`：**784 passed**；迁移/集成回归：**28 passed**。
- `.venv/bin/ruff --version` 为 **ruff 0.16.2**（满足 `>=0.9,<1`），`ruff check app tests` 通过；
  Pi Runtime `npm test -- --run` 为 **47 passed（9 files）**，`npm run typecheck` 与根目录
  `npm run lint` 均通过；`git diff --check` 通过。
- 独立代码审查两轮结论：**Critical 0 / Important 0**。审查提出的 active config 跨租户、Pi
  secret scope/backend、Reviewer legacy snapshot、版本并发与坏密文稳定错误均已修复；POC static
  fallback 另校验 `pi_runtime_poc` marker。测试生成的 xlsx 已移动至
  `/private/tmp/b0-task2-artifacts-dfGlUC` 和 `/private/tmp/b0-task2-final-artifacts-MYOVzO`，未留在工作树。
- 未调用真实模型、DataTap、钱包或积分；未运行或修改历史 Pi RPC/POC 真实六场景 Task 9，未进入
  B5-SSE、B7 UAT、生产切流或方案 C。

### 遗留事项

- Task 3 将在此不可变 snapshot 边界上建立独立 Node Pi Gateway；本 Task 不实现 Gateway、队列、
  账务、SSE 或管理端，仍保持 current/历史 POC 可回滚。

## Task 3（B2A）：独立 Pi SDK Gateway Worker

### 背景与目标

在 Task 2 的租户级不可变 Runtime snapshot 之后，建立无数据库权限的独立 `pi-gateway` Node
包和单 Run Pi SDK Session 工厂。目标是固定生产依赖组合、禁用 Pi 内建工具与自动资源发现，并把
Root Policy 作为 system prompt 注入；本轮只使用本地 fake provider，不调用真实模型、DataTap、钱包
或积分。

### 主要改动

- 新增 `pi-gateway/package.json`、精确 `package-lock.json`、TypeScript/Vitest 配置及
  `src/protocol.ts`：固定 `@earendil-works/pi-coding-agent@0.79.10`、`pi-ai@0.74.2`、
  `pi-tui@0.74.2`、`pi-mcp-adapter@2.20.1`、`typebox@1.3.11`，并提供 ClaimedRun、
  SecretBundle、PiRunSession 和 SDK 能力断言。
- 新增 `src/pi-session.ts`：直接调用 `createAgentSession()`，使用
  `SessionManager.inMemory()`、`AuthStorage.inMemory()`、`ModelRegistry.inMemory()` 和
  `SettingsManager.inMemory()`；每 Run 临时 cwd/agentDir 为 0700，`.mcp.json` 仅允许四个审核
  服务和环境变量引用，内建 read/bash/edit/write/grep/find/ls 不启用。
- 新增 `src/resource-loader.ts`、`src/secret-env.ts`、`src/worker-entry.ts`：关闭 Skills、context、
  prompt、theme 和自动 Extension discovery；Skill 只以目录追加到 system prompt，正文仍由后续
  `load_marketing_skill` 动态工具读取；子 Worker 环境不修改主进程 `process.env`，退出按
  abort→unsubscribe→dispose→清理顺序收口。
- 新增 5 组测试，覆盖 SDK 契约、Root Policy/system 与普通 prompt 分离、租户临时目录隔离、资源
  禁发现、endpoint/凭证无写入、密钥环境清理和异常 Worker 清理；更新 `.gitignore` 与
  `docs/qa/pi-runtime-version-probe.md`，保留历史 Pi 0.84.1 探针事实并追加生产锁定组合。

### 验证结果

- TDD 红灯：4 个初始测试套件在实现前因目标 `protocol/pi-session/resource-loader/secret-env`
  模块缺失而失败；随后对父环境秘密继承、调用方临时路径删除、IPC 与信号清理以及
  `unsubscribe` 异常路径增加的负向测试均先红后绿，最终为 5 个测试文件。
- `pi-gateway`：`npm test` **15 passed（5 files）**，`npm run typecheck` 通过，`npm run build`
  通过；`npm ls --depth=0` 显示四个 Pi/adapter 依赖与 typebox 均为精确锁定版本。
- 生产源码静态审计未发现 `child_process.exec`、shell 拼接、MySQL driver 或任意 `fetch` host；临时目录、
  system prompt、普通 user prompt、工具 allowlist、IPC secret 边界、child disconnect 和 secret-env
  隔离均有测试证据。
- 未调用真实模型、DataTap、钱包或积分；未运行或修改历史 Pi RPC/POC 真实六场景 Task 9，未进入
  B5-SSE、Task 4、真实 B7 UAT、生产切流或方案 C。

### 遗留事项

Task 4 将在此 Worker 边界上接入 FastAPI 签名控制协议、生产 adapter/internal-tool Extension 和
secret envelope；本 Task 不实现控制平面 HTTP、队列、账务、SSE 或管理 API。提交后继续按方案 B
任务边界执行，任何真实外部调用或升级锁定 Pi/adapter 依赖都必须停止并重新授权。

## Task 4（B2B）：FastAPI↔Gateway 签名协议与生产内部工具桥

### 背景与目标

在独立 Pi SDK Worker 边界上接入仅由 FastAPI 控制的签名控制协议、短期 Run lease、密封密钥包与
生产内部工具桥。本轮只使用本地测试库、fake Node provider 和离线回归，禁止真实模型、DataTap、
钱包/积分、历史 Pi RPC/POC 真实六场景 Task 9、真实 B7 UAT、生产切流和方案 C。

### 主要改动

- 新增 `backend/app/pi_gateway/{auth.py,contracts.py,models.py,service.py,router.py,internal_tools.py}`
  与迁移 `0039_pi_gateway_control_plane.py`：实现 HMAC method/path/body 完整性校验、DB nonce
  重放门禁、gateway/run/attempt lease 归属、AES-GCM/HKDF secret envelope、严格终态与 source-event
  DTO，以及 Gateway 内部工具端点。
- 新增 `backend/app/agent_runtime/tools/pi_internal_tools.py` 作为生产与 POC 共用的 B0 工具实现；
  POC 仅组装自身 registry，生产桥直接组装生产 registry，不再依赖 POC 设置守卫、审计旁路或数据库门禁。
- 扩展 AgentRun/Attempt/Session/Event 的 gateway、active-run、runtime-backend、source-event 字段；
  current Executor 只领取 `runtime_backend=current`，终态在同一事务回调内清理 Attempt、Session slot
  与 gateway/general lease；source event 拒绝绕过终态收口的 `run.*` 事件。
- 新增/修改 `pi-gateway/src/{protocol.ts,control-plane-client.ts,internal-tools.ts,secret-env.ts}`：
  Pydantic/TypeScript 镜像 parser、请求签名、origin/HTTP 环境门禁、保守父环境 allowlist、敏感/身份
  字段递归拒绝和工具参数边界。

### 验证结果

- TDD 红灯覆盖模块缺失、错误签名/nonce/lease、协议额外字段与敏感 payload、跨 Run 终态、current
  Executor 误领 Pi Run、source-event 伪终态和生产桥 POC 依赖；最小实现后定向后端 **64 passed**。
- `tests/marketing_capability_pack tests/pi_runtime_poc tests/agent_artifacts`：**806 passed、9 skipped**。
- Pi Gateway `npm test -- --run`：**22 passed（9 files）**；`npm run typecheck`、`npm run build` 通过；
  Pi Runtime `npm test -- --run`：**47 passed（9 files）**；Pi Runtime `npm run typecheck` 与根目录
  `npm run lint` 通过；全部本 Task 修改 Python 文件使用项目 Ruff 规则通过；`git diff --check` 通过。
- 独立复审结论：**Critical 0 / Important 0**。Node IPv6 loopback 兼容性仅列为 Minor，不影响本轮门禁。
- 未调用真实模型、DataTap、钱包或积分；未运行、修改或重跑历史 Pi RPC/POC 真实六场景 Task 9，未进入
  B5-SSE、真实 B7 UAT、生产切流或方案 C。

### 遗留事项

Task 5 负责持久队列、公平调度、draining 与 Worker pool；Task 4 不实现后续队列、统一账务、SSE、
管理端或真实外部 UAT。任何升级锁定 Pi/adapter 依赖、放宽 Gateway 工具权限或真实外部调用均须停止并重新授权。

## Task 5（B3A）：持久队列、租户公平调度、会话互斥与 draining

### 背景与目标

在已提交的签名 Gateway 控制协议上增加持久化 Pi 队列、公平租户轮转、Session 互斥和可控 draining。
本轮仍只使用事务测试库、fake control plane 和本地 Node Worker，不调用真实模型、DataTap、钱包或积分，
也不运行历史 Pi RPC/POC 真实六场景 Task 9。

### 主要改动

- 新增 `backend/app/pi_gateway/scheduler.py`，将租户/License/Gateway/Session/Run/队列状态锁定、
  Pi Attempt、lease、active count 和 fair cursor 放入同一 claim 事务；候选租户只是投影，锁定后重新
  校验 License 与租户/用户并发上限，Gateway `status/mode/desired_capacity` 均 fail-closed。
- 新增 `0039a_pi_session_mutex_backfill.py`：为既有活动 user Run 按 `created_at,id` 回填
  `AgentSession.active_run_id`，同 Session 多活动或现有槽不一致直接失败，不擅自终止 Run；同步补齐
  internal child Run 不覆盖主 Session 槽、stale slot 读写拒绝与 current/Pi 共用互斥。
- 统一消息、scheduler、terminal 的锁序为 `Tenant → Session → Run → child rows`；terminal 先锁
  Session 再验 lease，终态事务内清理 Attempt、Session 槽和 Gateway lease。`release_run` 使用 lease
  hash 条件更新作为 per-Run release marker，重复恢复/终态不会重复扣减 active count。
- 新增 `pi-gateway/src/{gateway.ts,worker-pool.ts,server.ts}`：容量池支持 draining 时拒绝排队任务，
  SIGTERM/SIGINT 进入 draining，shutdown 只 abort 当前 Worker 且有超时；Worker 在初始化期间即续租，
  heartbeat cancel 发送取消确认，heartbeat 丢失则保留 Run 供恢复，不伪造业务失败；早期 heartbeat
  reject 绑定消费路径，避免 unhandled rejection。

### 验证结果

- TDD 红灯覆盖 scheduler/互斥/锁后配额重验、legacy active slot、draining、heartbeat 丢失/取消、
  shutdown timeout、SIGTERM/SIGINT 和初始化期 heartbeat failure；最小实现后相关后端定向回归 **148 passed**。
- B0/POC/Artifact：**806 passed、9 skipped**；Pi Gateway：**32 passed（13 files）**，
  `npm run typecheck`、`npm run build` 通过；Pi Runtime：**47 passed（9 files）**、`npm run typecheck`
  通过；根目录 `npm run lint` 通过。
- `.venv/bin/ruff --version` 为 **ruff 0.16.2**，动态收集的全部 Task 5 Python 改动文件 Ruff 通过；
  `git diff --check` 通过；迁移 head/tenant 回填检查通过。测试生成的 3 个 xlsx 已移动到
  `/private/tmp/b0-task5-final-artifacts-Jdny8U`，未留在工作树。
- CodeGraph 使用项目自带 `codegraph sync` 增量校准，结果为 **497 files / 9,772 nodes / 27,083 edges**，
  状态 `Index is up to date`，无运行时 lock；未触碰 `codegraph.db`、WAL 或 SHM。
- 独立复审结论：**Critical 0 / Important 0**；审查提出的 active slot 回填、锁顺序、锁后配额、
  release 幂等、heartbeat 初始化竞态及 Node child 生命周期均已修复。

### 遗留事项

- Task 6 继续实现基础设施恢复、一次 Attempt、unknown 调用和取消状态机；Task 5 不实现统一钱包、
  SSE、管理端或真实 B7 UAT。历史真实 Task 9、真实模型/DataTap/钱包/积分、生产切流和方案 C 仍禁止。

## Task 6（B3B）：基础设施恢复、一次 Attempt、unknown 调用与取消

### 背景与目标

在 Gateway lease/Worker 生命周期基础上增加 Pi 专属基础设施恢复边界：current Executor 不得抢占
Pi Run；首次 Gateway 丢失只结束当前 Attempt 并回队列，第二次才失败；结果不明的工具调用继续保留
预留并只读核对；取消请求在 Worker/恢复两侧均只收口 cancelled，不重放模型或工具。

### 主要改动

- `RecoveryLoop` 增加 `runtime_backend`、Pi recovery/cancel 回调，并在 stuck/unknown MCP 扫描中按
  Run backend 过滤；现有 current 恢复保持兼容，Pi 恢复不再调用进程内 current Executor。
- 新增 `PiGatewayRecoveryService`：锁定 Session→Run→Attempt，使用 `populate_existing` 当前读校验
  过期 lease；第一次基础设施失败递增 `infrastructure_retry_count`、结束 Attempt、释放 Gateway
  lease/active count 并把同一 Run 重新置 queued，第二次经 `AgentEventStream.settle_terminal` 发唯一
  `run.failed`（`pi_infrastructure_retry_exhausted`）。取消中的过期 Pi Run 直接发唯一 cancelled，
  清理 slot/lease，不创建新 Attempt；回队列前将当前 Attempt 的 `reserved/running` ToolCall
  原子迁移为 `unknown`，保留预留，阻止新 Attempt 与旧外发调用并行。
- `pi-gateway/src/{control-plane-client.ts,gateway.ts,worker-entry.ts}` 增加 child exit/signal、SDK
  protocol 与控制平面不可达的稳定基础设施分类；父进程不为基础设施错误伪造 failed，控制平面短暂不可达时
  仅保留有界内存事件，超过上限 abort 并交给恢复；HTTP 5xx/网络错误归一为 `control_plane_unreachable`。
  新增 gateway cancel/crash、transport、terminal、event-buffer 负向测试。

### 验证结果

- TDD 红灯：Pi recovery import/runtime_backend 隔离、一次 retry/二次失败、取消孤儿、child failure
  分类、cancel ack 和 worker crash 均先以失败测试暴露，随后最小实现转绿。
- 后端恢复/Gateway/Executor 定向 **93 passed**；Pi Gateway **35 passed（15 files）**，
  `npm run typecheck`、`npm run build` 通过；Pi Runtime **47 passed（9 files）**、typecheck 通过；
  根目录 `npm run lint` 通过；本轮修改 Python 文件 Ruff 全部通过。
- 未调用真实模型、DataTap、钱包或积分；未重跑历史 Pi RPC/POC 真实六场景 Task 9，也未进入 B5-SSE、
  真实 B7 UAT、生产切流或方案 C。

### 复审修复与最终验证

- 初次复审发现两个 Important：首次 Pi recovery 未立即隔离当前 Attempt 的未完成 ToolCall；Node
  Worker/控制平面基础设施错误可能伪造业务 failed。第二轮复审补充取消孤儿 ToolCall 隔离与事件发送
  恢复 flush 的红灯后，最终 `tests/pi_gateway tests/agent_runtime` 为 **810 passed**，Pi Gateway
  为 **40 passed（15 files）**，`npm run typecheck` 与 `npm run build`
  通过；根目录 `npm run lint` 与全部修改 Python 文件 Ruff 通过，`git diff --check` 通过。
- 绿色断言覆盖：`reserved/running → unknown` 且 points reservation 保留；首次基础设施失败只回队列、
  第二次才 failed；claim/heartbeat/terminal 网络故障稳定为 `control_plane_unreachable` 并留给恢复；
  child `sdk_protocol_error` 不产生 terminal failed；本地事件 buffer 在控制平面恢复后 flush，超限 abort
  且不产生伪终态；取消孤儿不创建新 Attempt，reserved 调用以 `definitely_not_sent` 释放预留，
  running 调用保留预留并置 unknown。

### 遗留事项

- Task 7 实现统一租户钱包与 MCP 前置结算；真实 MySQL 多窗口恢复演练与外部 UAT 仍需后续获准，
  本 Task 不自动重放任何不确定调用。
