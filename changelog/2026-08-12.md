# 2026-08-12

## 独立审核收口：in-process server 启动失败的 task 泄漏

### 背景与目标

方案 B 最终独立审核在 `e362e47` 上提出最后一项 Important：
`backend/tests/integration/pi_uat/harness.py` 的 `_InProcessServer.start()` 在创建
serve task 后，若 5 秒轮询窗口内 `started` 仍为 False，直接抛
`in_process_server_start_timeout`，但不 cancel/gather serve task；同时
`PiUatTopology._start_topology()` 只有 `await server.start()` 成功后才把 server 追加到
`self._servers`，启动失败时外层 cleanup 看不到该 server，serve task/监听/端口可能跨拓扑残留。

最小复现：`in_process_server_start_timeout task_done=False active=False`。

### 红灯复现（先写测试，确认失败）

`tests/integration/pi_uat/test_harness_lifecycle.py` 新增两个测试，轮询 sleep 经
monkeypatch 缩到 0，不真实等待 5 秒：

- `test_in_process_start_timeout_cancels_and_reaps_serve_task`：fake uvicorn
  （started 恒 False、serve 长期等待）启动超时后，断言 serve task 已 done/cancelled、
  实例不在 `_active_servers`、无遗留 watcher/task。
- `test_topology_reaps_in_process_server_when_start_fails`：拓扑级反例——fake server
  启动失败后外层 `__aenter__` cleanup 仍完成、已创建 task 完整收口、端口可重新 bind
  （无监听残留）、DB teardown 仍执行、抛出的仍是原始
  `in_process_server_start_timeout`（不被 cleanup 吞掉）。

红灯实证：两个测试均失败于 `task_done=False`（serve task pending 泄漏），0.07s 内完成。

### 修复方法（仅测试基础设施，不动生产业务逻辑）

- `_InProcessServer.start()`：轮询窗口整体包 try/except BaseException——超时、
  serve 提前退出（新增检测，报 `in_process_server_start_aborted` 并链上 serve 异常）、
  取消，统一经新增的 `_abort_failed_start()` 有界收口后原样 re-raise。
  `_abort_failed_start()` 设置 `should_exit`（AppStatus 与 server）、cancel serve task、
  复用现有 `_reap_tasks_bounded`（timeout=2，报 `in_process_server_abort_timeout`）、
  经 `_drain_sse_shutdown_watchers` 只收本 server 创建的 watcher，finally 中维护
  `_active_servers`/AppStatus 一致性（仅空集时重置全局标志）。收口自身失败会继续抛错，
  绝不把失败启动静默伪装成成功；不使用无限 gather，不新增固定 sleep，不扩大 timeout。
- `_start_topology()` 改为先 `self._servers.append(server)` 再 `await server.start()`：
  start 已具备失败自清理语义，登记保证外层 cleanup 的 `server.stop()` 作为第二道防线。

### 验证结果（全部串行）

- 红灯：上述两测试修复前 `task_done=False` 失败（已复现）。
- 绿灯：`pytest -q tests/integration/pi_uat/test_harness_lifecycle.py
  tests/pi_gateway/test_lifespan_wiring.py` → 14 passed。
- 成功路径回归：`test_pi_gateway_offline_uat.py::
  test_brand_report_full_chain_same_version_excel_bi_and_gate` → 1 passed（13.52s）。
- `ruff check`（harness.py + test_harness_lifecycle.py）：All checks passed。
- `git diff --check` 干净；无 pytest/uvicorn/gateway/worker 残留进程。
- 仅修改两个测试基础设施文件，未重跑三轮 UAT、品牌十轮、后端全量、前端或 Pi Runtime。

### 遗留事项

- 状态仍为 READY_FOR_REAL_B7_UAT_REVIEW，等待架构复核。
- 仍禁止真实模型、DataTap、钱包/积分、历史真实 Task 9、真实 B7 UAT、生产切流和方案 C。

## REAL B7 UAT 授权包准备（纯文档，零外部调用）

### 背景与目标

方案 B 本地生产拓扑修复与架构复核已收口（Critical 0 / Important 0 / Minor 1），当前状态
仅为 `READY_FOR_REAL_B7_UAT_REVIEW`，真实 B7 UAT 尚未授权。本轮只做仓库事实核对与授权
方案设计：产出真实 B7 的授权计划与授权包两份 Markdown，并对三处历史文档漂移做纯文档纠偏；
不执行真实 B7 UAT，不连接真实模型、DataTap、钱包、数据库、预生产或生产环境，不创建真实
round 目录。

### 主要改动

- 新建 `docs/superpowers/plans/2026-08-12-real-b7-uat-authorization-plan.md`：固定状态头
  （AWAITING_USER_AUTHORIZATION，四项 NO）；round 身份格式
  `REAL_B7_<YYYYMMDDTHHMMSSZ>_<SHORT_COMMIT>`（本轮不创建 round）与 18 个执行前必须固定
  的字段；12 项隔离环境要求；9 项凭证引用清单（只记引用不记值）；工具/网络 allowlist
  （29 个已审核 DataTap 工具 + 13 个 Pi 内部工具，均核对自当前代码）；15 字段预算授权表
  （数值全部 NEEDS_USER_APPROVAL）；Level 0 预检 12 项 + Level 1 冒烟 1 项 + Level 2
  场景 20 个（destructive test-state change 逐项 `+D(...)` 授权位）。
- 新建 `docs/qa/2026-08-12-pi-b7-uat-authorization-pack.md`：append-only 证据目录设计
  （`docs/qa/evidence/pi-b7/<round_id>/`，15 个文件，manifest 19 组字段与脱敏规则）、
  19 条硬停止条件与 6 条停止后行为、12 项自审结果、授权文本模板（末尾明确：文档、commit、
  测试通过或 READY 状态均不构成真实外部调用授权）。
- 方案 B 计划纠偏（`docs/superpowers/plans/2026-08-09-pi-agent-gateway-plan-b.md`）：勾选
  Task 5/6/7 的 Commit 项（`60bf99f`/`b4a1497`/`6193c78` 早已在历史中，属文档漂移而非代码
  待办）；不改写其他历史内容。
- `docs/runbooks/pi-agent-gateway.md`：记录本地代码与架构复核完成（C0/I0/M1），状态维持
  `READY_FOR_REAL_B7_UAT_REVIEW`，仍等待用户真实 B7 授权；不写 B7 PASS / production ready。
- `docs/qa/pi-agent-gateway-local-uat.md`：保留历史否决与修复事实，追加 2026-08-12 更新
  （架构复核通过、等待真实 B7 授权、本地 fake topology ≠ 真实 UAT）。

### 验证结果

- 现场只读核对：分支 `codex/marketing-capability-pack-b0`，HEAD
  `61576f7a45c3a93063bdaae5328aefd67933df68`，工作树干净，`git diff --check` 通过。
- 仓库事实核对：迁移 head `0043_billing_downgrade_guard`；Pi 锁定版本 0.79.10/0.74.2/0.74.2 +
  adapter 2.20.1；`mcp_call_points=10` 启动硬校验；29 个动态工具与 13 个内部工具逐一对照
  `registry.py` 与 `pi_gateway/service.py`；adapter 服务别名对照 `protocol.ts`。
- 本轮未运行 pytest/npm/构建/迁移/UAT（纯文档任务，按任务书要求不执行）。
- 自审 12 项全部通过（见授权包 §4）：无明文凭证、无 READY→PASS 误写、未授权历史 Task 9/
  生产切流/方案 C。

### 遗留事项

- 全部待授权字段见授权计划 §1.2/§3/§5 与授权包 §5 模板；只有用户在新消息中完整确认授权
  文本后才允许执行真实 B7 UAT。
- 仍禁止真实模型、DataTap、钱包/积分、历史真实 Task 9、生产切流和方案 C。

## REAL B7 UAT 授权包第二轮修复：授权模式消歧与专用环境绑定（纯文档，零外部调用）

### 背景与目标

- 首次真实 B7 执行尝试在启动门禁 fail-closed：工作树存在两份未提交授权文档修改
  （B7_BLOCKED），未开启 round、未连接任何环境、未读取任何凭证。
- 用户决定：授权流程支持两种合法模式——模式 A（两阶段，推荐）与模式 B（一次性完整授权
  L0→L1→L2），本次执行采用模式 B；把已创建并核验的专用隔离环境（`kol_insight_b7_uat`）
  写入授权计划、授权包、runbook 与本 changelog；execution commit 消歧。
- 本轮仅文档修复与提交：不启动 FastAPI/Gateway，不读取 Keychain 明文，不连接数据库/模型/
  DataTap，不创建 round，不执行真实 B7，不修改生产代码、测试代码或迁移。

### 主要改动

- `docs/superpowers/plans/2026-08-12-real-b7-uat-authorization-plan.md`：
  - §7 重写为两种授权模式（§7.1/§7.2 模式 A、§7.3 模式 B、§7.4 执行与收口）；模式互斥；
    模式 B 不降低预算、隔离、destructive 开关与 19 条硬停止条件；L0 失败仍停止、L1 失败
    不得进入 L2；模式 B 无第二条授权消息要求。
  - 新增 §2.0 专用隔离环境绑定（VERIFIED）：数据库 `kol_insight_b7_uat`（`127.0.0.1:3306`、
    `kol_b7_uat@localhost`、`utf8mb4`/`utf8mb4_unicode_ci`、73 表、migration head
    `0043_billing_downgrade_guard`）、专用账号访问 `kol_insight.users` 被 MySQL 1142 拒绝、
    初始状态（tenants=0、encrypted_runtime_secrets=0、2 条系统迁移种子保留）、
    `retained_by_policy` 保留策略；严禁 `kol_insight`/`kol_insight_test`，禁止 DROP/CREATE/
    重建、普通 pytest、离线 UAT harness、迁移 downgrade。
  - §3 凭证表 reference 列核验为 VERIFIED（Keychain 三引用 + `backend/.env` 引用，只记
    引用不记值；master key 为 v1 + 32 bytes）；§1.1/§1.2 两模式化并新增「授权模式=模式 B」
    VERIFIED 行；§5 注明本次模式 B 授权数值以用户 2026-08-12 授权消息为唯一权威来源；
    §6.1 新增 L0-00 环境初始化核验项（仅经生产 domain/admin service 幂等写入，禁止直接
    写 `encrypted_runtime_secrets`）。
  - execution commit 消歧：`61576f7…` 仅 last production-code baseline，`f7ab159…` 仅
    第一版文档基线历史事实；实际候选 execution commit 为授权包最终修复提交后的 clean
    HEAD，round_id 用其前 8 位；文档不内嵌自指 SHA。
- `docs/qa/2026-08-12-pi-b7-uat-authorization-pack.md`：状态头更新为
  `AUTHORIZED_MODE_B_ROUND_NOT_OPENED`；§2.1/§2.2 增加模式 B 专用 `authorization.md`
  （授权文本全文 + 实际 execution HEAD + 数据库身份 + Keychain reference + 消息 ID +
  SHA-256）；§4 自审第 1/6 项更新；§5 重构为两种模式（§5.1/§5.2 模式 A，新增 §5.3 模式 B
  一次性完整授权模板）；§6.5 记录第二轮修复与验证。
- `docs/runbooks/pi-agent-gateway.md`：状态更新（模式 B 已授权、round 未开启）；新增
  「真实 B7 UAT 专用隔离环境」小节（数据库身份、Keychain 引用、运维边界、
  retained_by_policy）。
- `docs/qa/pi-agent-gateway-local-uat.md`：追加 2026-08-12 更新（二），不改写历史事实。

### 验证结果

- grep 核验：四份文档均含 `kol_insight_b7_uat` 与 `kol_b7_uat`；禁止 `kol_insight`/
  `kol_insight_test` 条款存在；两种授权模式定义清楚且无冲突；模式 B 无"必须第二条授权
  消息"阻断。
- 六类 secret 扫描（全部改动文件）：API key/JWT 形态、Bearer 头、私钥块、DSN 连接串、
  secret/password 赋值形态、未脱敏手机号——0 命中。
- `git diff --check` 通过；提交前 `git diff --cached --check` 与提交后 `git show --check`
  通过；提交后工作树干净。
- 本轮未运行 pytest/npm/构建/迁移/UAT（纯文档任务）。

### 遗留事项

- round 未开启：真实 B7 执行须以本提交后的 clean HEAD 为 execution commit 候选，通过全部
  启动门禁（工作树干净、HEAD 线性且区间仅文档变更、数据库身份逐项核验、Keychain 引用可读）
  后按模式 B 授权消息执行；任一不符即 B7_BLOCKED。
- 仍禁止生产切流、方案 C、历史真实 Task 9（round `20260808T060814Z`）重跑。
