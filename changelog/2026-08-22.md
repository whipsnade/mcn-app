# 2026-08-22

## 背景与目标

继续 `PI_AUTONOMOUS_MARKETING_SKILLS_PRODUCTION_RELEASE_GOAL` 的本地隔离发布验证。目标是验证
`ff66286d07f732d9a22a3d9032cdb260d396ca91` 的 Pi adapter catalog 容量和事件投递修复，并在符合目录、
账务与租户隔离前置条件后进入真实 Web UAT。

## 主要改动

- `backend/app/pi_gateway/accounting.py`：修复管理员租户钱包调整仅为 membership 校验构造内部
  `McpPreflightContext` 时，把过长审计 reference 作为受限 MCP run/tool-call identity 传入的问题。内部
  占位 identity 现在有界；真实账务 transaction 的 `reference_id`、幂等键和审计记录不变。
- `backend/tests/pi_gateway/test_mcp_chain.py`：新增长管理 reference 的 RED→GREEN service-level 回归，
  锁定 active member 的租户钱包调整账务路径。
- `docs/qa/2026-08-21-real-uat-mcp-cancel-repair.md`、两份运行手册与实施计划：记录本地真实目录输入阻断。

## 验证结果

- 启动门禁：候选分支/HEAD 与 origin 一致；根 `main` 仍停在 `6282659`，用户的 `.codex/.omo/.playwright-mcp`
  改动未触碰。GitHub Actions `32538601536`（attempt 1）确属 `ff66286` 且 Backend、Frontend、Migration、
  Browser E2E、Pi Runtime、Pi Gateway 全绿。
- 隔离身份：`DATABASE()=kol_insight_test`、`CURRENT_USER()=kol_test@localhost`、字符集/排序规则为
  `utf8mb4/utf8mb4_unicode_ci`；访问 `kol_insight` 和 `kol_insight_b7_uat` 被 MySQL 拒绝。
- 候选定向验证：backend catalog/event 19 项通过；Pi Gateway catalog/event 53 项通过、1 项历史 skip；
  Gateway typecheck/build 与前端 build 通过。
- 钱包调整修复：RED 复现 `McpPreflightContext` 两个 64 字符长度错误；GREEN 定向回归 `1 passed`，Ruff 与
  `git diff --check` 通过。
- 本地 FastAPI/Gateway/Frontend 均由本会话启动并健康后停止；本次没有留下 8000/5173/9471 监听。
- 真实 DataTap lifecycle discovery 结果为 29 项 approved+enabled（12/8/5/4）及 quarantined
  `query_user_info`，与现行 `DYNAMIC_TOOL_ALLOWLIST` 完全一致。没有直接写 catalog、没有创建真实业务
  Run/Attempt、模型请求、DataTap dispatch 或积分扣费。

## 遗留事项

- （2026-08-22 稍后修订）下述“58-tool Snapshot 前置条件”表述已被 reviewed set 门禁取代，见文末
  “目录输入门禁改为 reviewed set 口径”一节；58 是历史预发布快照，不是最低门槛。
- 在此之前，UAT A（Direct MCP+取消）和 UAT B（正式报告+Excel）均未执行，不得报告本地功能通过；远端
  111.10.192.19 仍关闭，待本地门禁解除后再进入预发布部署验证。

## 本地发布验证续作：CI 分类、测试隔离修复与目录门禁纠偏（2026-08-22 上午，主代理会话）

### 背景与目标

接管此前由另一会话开始的本地发布验证（该会话已由用户关闭；其当日线性提交 447e7f5/788310e/d226ccc/
8a174a6/29b2ad4/6106869 全部保留，未 reset/rebase）。目标：分类 CI run `32543004551`（29b2ad4）的
Backend/Browser E2E 失败、修复 `test_adapter_catalog_capacity.py` 全文件运行的测试隔离问题（不删除
`kol_insight_test` 的 29 条 lifecycle 现场行）、纠正 ambiguity 检查的技术口径，并取消错误的固定
58-tool UAT 门禁。

### 主要改动（含关键文件）

- CI 失败分类：Backend = 确定性回归（`_reviewed_adapter_catalog` 改两次查询后旧测试 fake 只实现
  `.all()`，`AttributeError: 'Result' object has no attribute 'scalars'`），已由 6106869 修复（fake
  精确模拟 catalog `scalars().all()` + discovery `all()`），新 CI `32543470791` 六 job 全绿验证；
  容量门禁、128 KiB 输入与 ambiguity 断言均未放宽。Browser E2E = 既有 flake（d226ccc 绿 → 29b2ad4
  红 → 6106869 仅改 Python 测试又绿，无前端代码差异；本地全量 E2E 50 passed / 1 failed，唯一失败为
  `artifact-workspace` 未读圆点用例仅在 mobile-390 满载时序下偶发，隔离复跑 1 passed）。
- `backend/tests/runtime_config/test_adapter_catalog_capacity.py`：新增 `isolated_reviewed_catalog`
  fixture——在测试事务内把预存 approved+enabled catalog 行临时置 disabled，随 `db_session` 外层事务
  回滚恢复；新增 `test_precexisting_catalog_rows_restored_after_rollback` 用独立连接 + 显式回滚证明
  现场恢复。固定数量（32/33/58/128/129）、排序过滤、重复身份、Snapshot 不可变与 128 KiB 用例恢复可
  独立运行。
- `backend/app/runtime_config/service.py` 与同测试文件：为 `runtime_adapter_catalog_ambiguous_remote`
  补充正确因果注释——`discovery_digest()` 覆盖 remote name + input/output schema，两个 remote 不可能
  仅因 Schema 相同而共享 digest；该 fail-closed 检查防御异常数据库状态、重复审批绑定、数据损坏或
  极端 digest 冲突，不拒绝正常同 Schema 工具。行为与测试名不变。
- 目录输入门禁改为 reviewed set 口径：修订
  `docs/superpowers/plans/2026-08-21-pi-autonomous-marketing-skills-implementation.md`、
  `docs/runbooks/phase-2-runtime.md`、`docs/runbooks/pi-agent-gateway.md`、
  `docs/qa/2026-08-21-real-uat-mcp-cancel-repair.md` 与本文件。现行门禁：模型可见目录 = 当前审核集合；
  lifecycle discovery、`mcp_tool_catalog` 与受控 `DYNAMIC_TOOL_ALLOWLIST` 三方逐项一致（internal
  name、remote name、service、输入/输出 Schema、digest）；quarantined/unknown/disabled 不进 Snapshot
  且 `query_user_info` 保持 quarantined；Snapshot 完整不截断不分页，≤128 entries 且 canonical JSON
  ≤128 KiB；29 为当前事实、58 为历史预发布快照与容量回归案例（保留为历史，不改写）；禁止 seed catalog、
  直接写库或扩张 allowlist。

### 验证结果

- 隔离修复 RED→GREEN：修复前全文件 `6 failed, 3 passed`（29 条现场行叠加进固定数量断言，如
  87=29+58）；修复后 `10 passed in 0.18s`；抽查 129-True、ambiguity、恢复证明三个用例独立运行通过。
- 现场保护证明：修复后只读复核 `kol_insight_test` 仍为 29 条 approved+enabled（12/8/5/4），未删除、
  未 truncate、未让生产代码忽略现场行。
- 本地全量 Browser E2E（当前 HEAD + 隔离修复）：50 passed / 1 failed（上述 mobile-390 flake），
  隔离复跑通过；8000/5173 端口运行后已释放。
- `6106869` 的 CI `32543470791`：Backend、Frontend、Browser E2E、Migration、Pi Runtime、Pi Gateway
  全绿。

### 遗留事项

- 待本节改动以线性提交推送并取得新 SHA 首次 CI attempt 全绿后，按授权进入本地真实 UAT A1/A2/B
  （隔离库、预算内、真实模型 + 只读 DataTap）。
- mobile-390 未读圆点用例的偶发时序 flake 记录在案；无确定性复现，不在本轮修改 E2E 代码。
