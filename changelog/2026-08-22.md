# 2026-08-22

## 背景与目标

继续 `PI_AUTONOMOUS_MARKETING_SKILLS_PRODUCTION_RELEASE_GOAL` 的本地隔离发布验证。目标是验证
`ff66286d07f732d9a22a3d9032cdb260d396ca91` 的 Pi adapter catalog 容量和事件投递修复，并在符合目录、
账务与租户隔离前置条件后进入真实 Web UAT。

## 主要改动

- `backend/app/pi_gateway/accounting.py`：修复管理员租户钱包调整仅为 membership 校验构造内部
  `McpPreflightContext` 时，把过长审计 reference 作为受限 MCP run/tool-call identity 传入的问题。内部
  占位 identity 现在有界；真实账务 transaction 的 `reference_id`、幂等键和审计记录不变。
- `backend/tests/pi_gateway/test_mcp_chain.py`：新增长管理 reference 的 RED→GREEN 回归，锁定 active
  member 可经正式 Admin API 完成钱包调整。
- `docs/qa/2026-08-21-real-uat-mcp-cancel-repair.md`、两份运行手册与实施计划：记录本地真实目录输入阻断。

## 验证结果

- 启动门禁：候选分支/HEAD 与 origin 一致；根 `main` 仍停在 `6282659`，用户的 `.codex/.omo/.playwright-mcp`
  改动未触碰。GitHub Actions `32538601536`（attempt 1）确属 `ff66286` 且 Backend、Frontend、Migration、
  Browser E2E、Pi Runtime、Pi Gateway 全绿。
- 隔离身份：`DATABASE()=kol_insight_test`、`CURRENT_USER()=kol_test@localhost`、字符集/排序规则为
  `utf8mb4/utf8mb4_unicode_ci`；访问 `kol_insight` 和 `kol_insight_b7_uat` 被 MySQL 拒绝。
- 候选定向验证：backend catalog/event 19 项通过；Pi Gateway catalog/event 53 项通过、1 项历史 skip；
  Gateway typecheck/build 与前端 build 通过。
- 钱包调整修复：RED 复现 `McpPreflightContext` 两个 64 字符长度错误；GREEN 定向回归 `1 passed`，Ruff 与
  `git diff --check` 通过。
- 本地 FastAPI/Gateway/Frontend 均由本会话启动并健康后停止；本次没有留下 8000/5173/9471 监听。
- 真实 DataTap lifecycle discovery 结果为 29 项 approved+enabled（12/8/5/4）及 quarantined
  `query_user_info`，与现行 `DYNAMIC_TOOL_ALLOWLIST` 完全一致。没有直接写 catalog、没有创建真实业务
  Run/Attempt、模型请求、DataTap dispatch 或积分扣费。

## 遗留事项

- 58-tool Snapshot 是当前真实 Web UAT 的外部目录输入前置条件。需目录/安全审批提供可追溯的 58-tool
  manifest（内部/远端名、服务、审核描述、输入/输出 Schema、批准/启用状态及版本或签名），经正常受控
  变更和 lifecycle refresh 后在 `kol_insight_test` 验证。
- 在此前，UAT A（Direct MCP+取消）和 UAT B（正式报告+Excel）均未执行，不得报告本地功能通过；远端
  111.10.192.19 仍关闭，待本地门禁解除后再进入预发布部署验证。
