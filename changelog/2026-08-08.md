# 2026-08-08

## Marketing Capability Pack B0：Task 3R canonical 契约语义修复

### 背景与目标

Task 3 测试虽通过但 Gate 未过，5 项 P1 语义缺陷：① 派生指标（如 Campaign
`overview.total_volume=1`）没有对应的最终 canonical field；② `field_lineage`
按 `(evidence_id, source_path)` 过宽映射，total_volume 会带上 engagement/
platform/post_id 等无关字段；③ canonical path 使用供应商原始别名（声量、
brand_mentions），同一业务语义因别名不同产生不同路径；④ CanonicalField 是普通
dataclass、availability 任意字符串、payload 用 `dict[str, Any]` 绕过验证；
⑤ 同一 Evidence 跨多个 group 被重复 canonicalize 产生冲突字段；⑥ Top posts
expected fields 不完整且缺失时伪造空串。本轮严格 TDD 修复，不进入 Task 4–7、
B1–B7 或方案 C。

### 主要改动（关键文件）

- 新增 `app/agent_artifacts/canonical.py`：强类型 `CanonicalField`（Pydantic，
  path 非空且以 `/data/` 开头、availability 仅 complete/partial/unavailable、
  unavailable ⟺ value=None、数值 complete/partial 必须有 evidence_ids、
  evidence_ids 去重）、`CanonicalPayloadMixin`（canonical path 不重复、
  field_lineage 键与 canonical path 集合完全一致且恒等映射到自身）、
  `publish_canonical`（从最终业务 data 走查全部叶子：value 与最终 payload 同
  一计算结果；evidence_ids 来自 LineageCollector refs 去重保序；availability
  按 value/partial_paths 推导；单位按路径末段推断）。
- `raw_rows.py`：改为两层结构的**内部 normalized source rows**（保留
  RowRef/extract_rows/别名/单位表），行级按 `(evidence_id, source_path)` 去重；
  删除 `_EXPECTED_GROUP_FIELDS` 与按 group+证据 id+原始 key 生成的旧 canonical
  fields/source_index/field_lineage；新增共享 `platform_coverage_incomplete`
  （scope 声明平台未被 Evidence 覆盖且无合计行时判定部分覆盖）。
- `builders/brand.py`/`campaign.py`：用 `publish_canonical(data, refs,
  partial_paths)` 发布 canonical_data + field_lineage；平台覆盖不全时
  overview 聚合字段 canonical 标 partial、章节标 partial、limitation
  `platform_coverage_incomplete`、data_status=restricted（有行才判定，无行
  仍 unavailable）。
- `builders/sections.py`：top_posts 的 title/author 缺失不再伪造 `""`（payload
  为 None）；platform/post_id/title/url/author/published_at 文本叶子也登记
  贡献行；likes/comments/shares/engagement 保持既有登记。
- `payloads/common.py`：`TopPost.title/author` 改为 `str | None`（Excel
  `present(None)` 显示「未采集」，前端 `post.title || '无标题'` 已兼容）。
- `payloads/brand.py`/`campaign.py`：继承 `CanonicalPayloadMixin`，删除
  `canonical_data: tuple[dict[str, Any], ...]` 宽松类型。
- `agent_runtime/evidence.py`：删除旧 dataclass `CanonicalField`，改为从
  `app.agent_artifacts.canonical` 再导出（normalization.py 的 re-export 不变）。
- `tests/agent_artifacts/test_marketing_canonical_sections.py`：按新契约重写为
  13 个测试，覆盖任务列出的全部 14 项语义（中英文别名同路径、跨平台求和、
  双 Evidence、精确 lineage、单帖 total_volume=1、显式 0、缺失 unavailable、
  部分平台 partial、跨 group 去重、五章节覆盖、top post 缺失 title/url、
  wrapped JSON string 与 rows/items/data 容器、输出顺序稳定、非法输入
  fail-closed）。

### 验证结果

- 红灯：新测试在旧实现上因 `app.agent_artifacts.canonical` 缺失 collection
  fail（证明旧实现无强类型契约）；修后 3 例先红（单平台 scope 误触 partial、
  wrapped 形态构造错误）为测试自身问题，修正后全绿。
- 定向 `tests/agent_artifacts/test_marketing_canonical_sections.py` **13 passed**；
  `tests/agent_artifacts` 全目录 **476 passed**；`tests/marketing_capability_pack
  tests/pi_runtime_poc` **148 passed、9 skipped**（与基线一致）；合并跑
  **624 passed、9 skipped**。
- 范围 Ruff（任务规定 8 文件 + 新增 canonical.py/sections.py/common.py）通过；
  `git diff --check` 干净。
- 测试库说明：本工作树无 `.venv`，复用仓库既有 venv（pi-runtime-poc 工作树
  venv 含 aiosqlite，`app` 已确认解析到本工作树）；未安装或升级任何依赖。
- 未调用模型、DataTap、钱包或积分；未运行 Task 9；未进入 Task 4–7、B1–B7 或
  方案 C；未生成/检查 Excel、未修改前端、数据库或迁移。

### 遗留事项

- `tests/agent_artifacts/test_api.py` 导出用例会向 `.data/agent-exports/` 写入
  xlsx 测试产物（既有行为，pytest 导出用例用默认存储目录），提交前已清理，
  与 Task 3 一致不纳入版本控制。
- Task 4 数值 lineage 门禁（`validate_structured_claims`）待 Task 3R 审核通过
  后以 canonical_data 为唯一事实源实施。

## Marketing Capability Pack B0：Task 3R2 canonical publication 不变量收口

### 背景与目标

Gate 审核发现 Task 3R 的 canonical 虽已由 Builder 发布，但尚不能证明它完整、逐值等同
于最终 `data`；wrapped `result` 行也会在去重时丢失，且部分派生指标把无关 Evidence
纳入来源。本轮只收紧 Task 3 的发布与审计边界，不进入 Task 4–7、B1–B7 或方案 C。

### 主要改动（关键文件）

- `canonical.py`：以 JSON-mode、RFC 6901 转义后的叶子路径强制 `canonical_data` 与最终
  `data` 精确一一对应，并逐值比较；缺失、多余、错值或不匹配 lineage 一律 fail-closed。
  `publish_canonical` 以模块语义区分单位：Brand 的 volume 保持 `mentions`，Campaign
  的 volume/posts 为 `posts`，比例为 `ratio`。
- `builders/raw_rows.py`：区分对外稳定 source path 与内部 logical row identity；wrapped
  `/result` 的每个数组行独立参与聚合，兼容 group 对同一 logical row 仍只计一次。Brand
  current/mom/yoy 与 Campaign current/baseline/post 的同 Evidence 跨期复用返回稳定
  `evidence_period_reuse`。
- `builders/brand.py` 与 `builders/campaign.py`：字段来源只登记实际参与指标计算的行；补齐
  Brand daily trend、Campaign timeline 的 date/platform lineage，按真实平台/指标归属。
  平台覆盖不足会将所有依赖该集合的章节及相应 canonical 字段标为 partial；social 冲突亦
  下沉到受影响字段。TopPost 缺平台跳过且披露，缺 title/author 保持 unavailable 并标 partial。
- `payloads/common.py`：修复 import 排序及 `Iterator`/`Sequence` 的 `collections.abc` 来源。
- `tests/agent_artifacts/test_marketing_canonical_task3r2.py`：新增 22 个 TDD 语义用例；测试
  fixture 在故意修改最终 data 后使用同一发布函数重建完整 JSON canonical，避免旧 fixture
  绕过真实持久化路径。

### 验证结果

- 红灯：强契约的错值、缺字段、多余字段均未被旧 validator 拒绝；wrapped `/result` 两行只
  聚合到 100（期望 300）；日趋势的 volume lineage 误带 engagement Evidence。最小实现后，
  新增 Task 3R2 定向测试 **22 passed**，与原 Task 3R、payload/null 治理合计 **151 passed**。
- 全量 `tests/agent_artifacts`：**498 passed**；`tests/marketing_capability_pack
  tests/pi_runtime_poc`（使用既有 POC venv，未安装依赖）：**148 passed、9 skipped**；范围 Ruff
  通过，`git diff --check` 通过。
- 全程使用 test placeholder 配置；未调用模型、DataTap、钱包或积分，未运行 Task 9，未修改
  Root Policy、Skill 动态选择或 Pi Runtime 边界。

### 遗留事项

- Task 4–7、B1–B7 与方案 C 均未开始，等待 Task 3R2 审核。

## Marketing Capability Pack B0：Task 3R3 全 Builder metric-level lineage

### 背景与目标

Task 3R2 已收紧 canonical 发布不变量，但审查发现仍有按整行、整 bucket 或整周期绑定
数值来源的同类风险。Task 3R3 对 Brand/Campaign 全部 Builder 聚合 helper 做 inventory，
将每个数值的 contributors 收敛为实际计算输入；不改变公开 payload/API，也不进入 Task 4。

### 主要改动（关键文件）

- Brand：审计 overview、comparison、daily trend、topics、dimensions、organic、regions、sentiment
  与 top posts。topics/dimensions 改为每个指标独立 rows；overview 情感兜底中 count 使用本极性
  rows、share 使用三极性分子/分母并集。
- Campaign：审计 overview、platform/timeline/KOL/content contributions、comparisons、attribution、
  organic/audience、internal metrics、ROI、sentiment 与 top posts。`_group_totals` 统一同时返回
  `values` 与 metric contributors，comparison current/baseline/delta/rate 不再按整个 period 取行。
  creators 只取有效作者、互动只取有效互动、行数只取实际计数行；CPC/CPM/ROI/ROAS 使用公式输入
  contributors 并集。
- `canonical.py`：comparison JSON Pointer 正确去除 `/data/` 前缀，按同级 metric 推导 Brand 的
  mentions/interactions/posts 与 Campaign 的 posts/interactions/count；rate 固定为 ratio。
- `sections.py`：共享情感 section 的 count 仅用本极性行，share 使用本平台或全局的完整分母行集。
- 新增 `test_marketing_canonical_task3r3.py`：以拆分 Evidence 覆盖 comparison、topics/dimensions、
  creators/KOL/content/organic/attribution/internal 公式、Brand fallback sentiment 与 comparison units。

### 验证结果

- 红灯：6 项初始反例失败（comparison 跨指标/跨期串线、creator/attribution 整桶、topic/dimension
  整槽、unit=None）；补充 Brand fallback 与共享 sentiment share 的分母测试再得到 2 项业务失败。
  修复后 Task 3R3 定向 **8 passed**。
- 全量 `tests/agent_artifacts`：**506 passed**；`tests/marketing_capability_pack
  tests/pi_runtime_poc`：**148 passed、9 skipped**；基线动态列出的 5 个 Python 文件 Ruff 通过，
  `git diff --check` 通过。
- CodeGraph MCP transport unavailable（索引未改动），结构审计改用 `rg` 枚举所有 `collector.add`
  与精确源码读取；未调用模型、DataTap、钱包、积分或 Task 9。
- 独立复审两轮完成：首轮 Critical 0 / Important 2（共享 sentiment share 分母、Brand fallback
  bucket count/share 混用），已在同轮修复并以拆分 Evidence 覆盖；复审为 Critical 0 /
  Important 0 / Minor 0。

### 遗留事项

- Task 4–7、B1–B7 与方案 C 未开始；Task 3R3 提交后等待审核。

## Marketing Capability Pack B0：Task 1、Task 2 与 Task 2R

### 背景与目标

将营销根策略从普通用户 prompt 移至 Pi 真实 system 参数，关闭旧 `--skill` 第二入口，并把 Pack Contract 与实际 Skill 加载审计收敛到 Run snapshot。

### 主要改动

- Task 1 新增版本化 manifest、根策略、六个专项 Skill 与受限 loader。
- Task 2 将完整 Root Policy 与安全 Skill 目录经 `--append-system-prompt` 注入；普通 prompt 只含用户与动态上下文，Pi 只能以 `load_marketing_skill` 主动获取正文。
- Task 2R 禁止 `PiRpcConfig.skills`，强制 `--no-skills` 且拒绝任何 `--skill`；真实 Pi
  factory 从当前 Run 已持久化快照渲染完整 Root Policy 与仅含 name/description/version/
  artifact_contract 的安全目录，普通 prompt 不重复 Root Policy。
- manifest 递归拒绝敏感字段，并对 Root Policy、Skill、Contract 正文使用保守凭证模式；三个
  Contract 必须精确、唯一、校验 path/digest/content type/version。`load_marketing_skill` 在当前
  Run 锁内以 `{name, version, digest}` 幂等写入 `loaded_skills`，参数与异常均不泄露路径、正文或快照。
- 新增 Service → Registry → Tool → snapshot 全链路验收，覆盖重复加载、禁用/未知/错版本、篡改、
  跨 user/session/run token 隔离、越权参数和失败脱敏；Node 覆盖 Schema、注册和 HTTP 透传。
- Task 2R 收口补扫完整 manifest 的所有字符串值（含版本映射与普通字段），并限定唯一
  `marketing_runtime_v1`；loader、持久化 Run snapshot 与 Pi factory 三层均 fail-closed。Skill
  加载锁查询启用 `populate_existing=True`，避免用会话旧快照覆盖并发审计结果。

### 验证结果

- 后端 marketing capability pack 与 Pi POC：148 passed、9 skipped；范围 Ruff 通过。
- Pi Runtime：47 passed、typecheck 通过。
- 全部验证使用占位配置；未调用模型、DataTap、钱包或积分，未重跑 Task 9。

### 遗留事项

- B0 Task 3–7 未开始；Task 2R 仍待代码审核后再进入后续任务。

## Marketing Capability Pack B0：Task 3 canonical Evidence 到品牌/活动章节

### 背景与目标

将营销 Builder 的原始 Evidence 读取收敛到唯一 canonical 映射入口，令品牌与活动章节的数值、
可用性和来源都能以字段级方式审计；缺失字段必须保持 unavailable/None，不能补 0。

### 主要改动

- 新增 `CanonicalField(path, value, availability, evidence_ids, unit)`，并将原始 shape 的字段别名、
  单位和 Evidence path 归一为 `canonical_data`；品牌/活动 Builder 只消费 canonical 行。
- 品牌 overview、sentiment、daily_trend、top_posts，以及活动 posts/top_posts 现在写入
  `field_lineage`，将 Artifact JSON Pointer 关联到 canonical field path；缺失字段显式写为
  unavailable，活动缺失互动量仍为 `None`。
- 保留中文“日”时间列和布尔/文本归属的既有业务兼容口径；不改动 Root Policy、Skill 动态选择或
  Pi Runtime 边界。

### 验证结果

- 红灯：新增 `test_marketing_canonical_sections.py` 因 `CanonicalField` 与 canonical payload 缺失而
  collection ImportError；最小实现后定向绿灯 2 passed。
- 品牌/活动 Builder 非数据库回归：86 passed、2 deselected（两项需要测试库的 lineage 冻结测试）。
  全量 `tests/agent_artifacts` 中的数据库类用例因 `kol_test` 无 `kol_insight_pi_poc` 访问权限未运行，
  未改权限或重试外部资源。
- marketing capability pack + Pi POC 回归：148 passed、9 skipped；Task 3 范围 Ruff（忽略原有的
  raw_rows 时区/URL 检查与 payload ClassVar 基线项）及 `git diff --check` 通过。
- 全部验证使用占位配置；未调用模型、DataTap、钱包或积分，未重跑 Task 9。

### 遗留事项

- Task 4–7 与 B1–B7/C 未开始，等待 Task 3 审核。

## Gate A 改为 Pi-only 绝对验收

### 背景与目标

连续真实 Task 9 已证明 Current/Pi 双路径对比增加了钱包、夹具和依赖编排复杂度，却仍在 Pi 审计
基础设施稳定前消耗完整六场景轮次。用户确认方案 A 不再评价 Pi 是否优于 Current，只验证 Pi 能否
独立完成营销业务闭环。

### 主要改动（含关键文件）

- 新增 `docs/superpowers/specs/2026-08-08-pi-only-gate-a-design.md`，覆盖旧方案 A 的双 Runtime
  对比口径；生产 Current Runtime 和方案 B 灰度回滚不在删除范围。
- 新增 `docs/superpowers/plans/2026-08-08-pi-only-gate-a.md`，将实施拆为 Task 8E 序号当前读、
  Pi-only factory、六案例隔离编排、绝对 Gate finalizer 和唯一真实轮次五项任务。
- Task 9 改为仅执行 Pi 六场景，以 Artifact/Excel/BI、Evidence lineage、澄清、钻取、拒答和人工
  质量评分的绝对门槛作 Gate，不再计算相对 Current 指标。
- 新增 `BLOCKED / INFRA_FAILED / EVALUATED_FAIL / PASS` 四态，避免把审计或 Harness 失败误写成
  Pi 分析效果失败。
- Task 8E 先统一修复 Attempt/Step/Event 当前读和独立案例收口；真实 MySQL 拓扑回归通过后直接
  运行一次 Pi-only 完整 Task 9，不再增加重复品牌 canary。

### 验证结果

- 本次仅完成设计修订，未改 Runtime、未调用模型/DataTap、未创建 Run 或 round。
- 规格明确六场景结果必须相互隔离，执行器始终生成完整且不可变的 `execution.json`；技术执行完整后
  由零模型、零 MCP finalizer 合并人工评分生成 `summary.json`。钻取依赖失败不得阻断澄清和非营销案例。
- 设计与计划通过 `git diff --check`；未运行代码测试，未调用模型、DataTap 或 Pi。

### 遗留事项

- 待独立开发会话按新计划完成 Task 8E 与 Pi-only Task 9；当前工作树已有 Task 8D 未提交代码，
  实施时必须保留并按任务范围验证，不能 reset 或混入不相关提交。
- 方案 B/C 尚未启动；Pi-only Gate A PASS 后才进入方案 B。

## 背景与目标

修复 Task 9 中 Pi 审计 Step 为 0、而 DataTap 后台已有成功调用记录的问题，并在全部本地门禁通过后
执行一次新的真实 Current/Pi 对比。保持方案 A、精确 POC 数据库、Pi 零积分和不重复真实轮次边界。

## 主要改动（含关键文件）

- `backend/app/pi_runtime_poc/audit.py`：`PiRunAuditWriter` 查询最新 Attempt 和最大 Step sequence 时
  使用持锁当前读，避免长会话的 `REPEATABLE READ` 快照漏掉 Extension HTTP 请求已提交的审计行。
- `backend/tests/pi_runtime_poc/test_task8d_audit_concurrency.py`：新增真实 MySQL 旧快照回归，覆盖
  Runner 先建立快照、Extension 独立提交、Runner 再写 RPC 审计的精确顺序。
- `docs/qa/pi-runtime-poc-rounds.md`：记录 round `20260807T162812Z` 的真实调用、收口、根因与 Gate。

## 验证结果

- 红灯：旧实现触发 `agent_run_attempts.uq_agent_run_attempts_run_attempt` 唯一键 `IntegrityError`；
  这是同一快照缺陷比历史 Step 冲突更早的表现。
- 绿灯：定向旧快照测试 1 passed；Task 8D 真实 MySQL 并发测试 4 passed；POC 后端 89 passed；
  Pi Runtime 50 passed，typecheck 通过；本次范围 Ruff 与 `git diff --check` 通过。
- 预检：数据库为 `kol_insight_pi_poc`，迁移为 head，可执行态 Run 为 0，模型为
  `deepseek-v4-flash`，四个 DataTap 服务映射完整，8000 空闲。
- 唯一真实轮次实际创建 6 个 Run：三个 Current 路径完成或带警告完成；三个 Pi 路径均在至少一次
  真实 DataTap settled 调用并形成 Evidence 后失败。失败约束为
  `agent_events.uq_agent_events_run_sequence`。
- 轮次因 Pi 品牌 Artifact 不可用，在产物钻取依赖预检处以 `poc_dependency_run_unavailable` 停止；
  没有执行剩余澄清和非营销案例，没有 `summary.json`。
- 收口后所有 Run 均为终态；5 条 Pi running ToolCall 仅迁移为 unknown，未重放；可执行态 Run=0、
  悬挂 ToolCall=0、8000 已释放、密钥模式扫描 PASS。Pi 积分为 0。

## 遗留事项

## Pi-only Gate A 预备（Task 1–4）

### 背景与目标

将方案 A 验收 Harness 收敛为 Pi-only 绝对 Gate，保留生产 Current Runtime 的回滚能力。

### 主要改动

- Run 内 Event、Attempt、Step 序号统一锁定当前读；Pi Factory 不创建 Wallet。
- 六案例仅以 `--case all --runtime pi` 隔离运行并写入不可变 execution.json。
- 新增绝对 Gate、严格人工评分及零外部调用 finalizer。

### 验证结果

- Task 1–4 的定向 MySQL、Pytest、Ruff 与 diff 检查均已通过；尚未执行新的真实 Pi-only round。

### 遗留事项

- Task 5 需先完成无外部调用总验证和 fail-closed 预检，再按授权执行一次真实 Pi-only 六场景。

- **Gate A FAIL（POC 审计/验收夹具失败）**，不能评价 Pi 与 Current 的真实分析效果。
- 下一步若获授权，应先增加 Task 8E：修复 `AgentEventStream.append_locked()` 的 Event sequence 旧快照
  读取，并让单侧依赖失败不再中止整轮；本地门禁通过后再单独申请新的真实 round。
- 不进入方案 B/C。若未来 Gate A 通过，应基于实测 Pi API 新建方案 B 计划；Pi 链路稳定一个发布周期后
  再评估 Marketing MCP Gateway（方案 C）。

## Pi-only Gate A 唯一真实轮次（Task 5）

### 背景与目标

在 Pi-only Harness 的本地验证、精确 POC MySQL 预检与单工具真实冒烟均通过后，按用户一次性授权运行
唯一的 Pi-only 六场景 Task 9，不运行 Current Runtime。

### 主要事实

- round `20260808T030356Z` 只使用 `kol_insight_pi_poc`、`deepseek-v4-flash` 和 `high` thinking；不创建
  Current Run、Wallet 或积分流水。
- 品牌研究 Run `9d6d91f1-c923-496e-9159-cbc0b45f5f4e` 在真实 Pi 执行后收到
  `PiRpcProtocolError`，已落库 25 个 DataTap ToolCall、19 条 Evidence 和 1 个 `brand_report_v3` Artifact。
- Run 终态后，本地编排进程无 MySQL 锁、无 Pi 子进程却不再推进。为释放 8000，仅终止该精确挂起进程；
  没有重跑模型、DataTap 或整个 round。
- 同一 append-only 目录补写了六案例 `execution.json`：品牌 failed、钻取 skipped_dependency，其余四例
  not_run；零外部调用 finalizer 生成 `summary.json`。

### 验证结果

- POC 本地测试 107 passed、Task 8D MySQL 并发回归 7 passed、Pi Runtime 46 passed、typecheck 通过；
  本 Task 范围 Ruff、`git diff --check` 与新增 diff 密钥扫描通过。全仓 Ruff 仍有 149 项历史基线债务，
  未在本任务修改。
- 产物 Excel 仅用 openpyxl 检查：品牌 Artifact 可打开、有 8 个 Sheet、同 Version 存在 BI payload。
- Wallet=0、WalletTransaction=0、points reserved/settled=0、悬挂 ToolCall=0；8000 已释放，round 与诊断
  文件密钥模式扫描无命中。
- `human-review.json` 不适用；最终 Gate 为 **INFRA_FAILED**，不可评价 Pi 的六场景业务效果。

### 遗留事项

- 最小修复范围是让 Pi RPC 协议错误后显式关闭 client、返回案例结果并继续无依赖案例，同时以测试保证
  execution.json 始终包含六案例。因本轮已有真实外部调用，任何新完整 Gate round 都须重新授权。
- 停止于方案 A，不进入方案 B/C。

## Pi 0.79 RPC 终态兼容与有界事件修复

### 背景与目标

上一轮 Pi-only Task 9 的首例在真实 `agent_end` 大记录上触发 `rpc_record_too_large`，随后 Runner 又因
等待不存在的 `agent_settled` 和关闭路径无界等待而未能推进。仅修复本地协议处理，不启动真实服务。

### 主要改动

- `backend/app/pi_runtime_poc/rpc.py`：单条 RPC 限制改为 16 MiB；对 agent_end、message_update、工具、
  response 等事件进行最小投影；协议错误改用稳定安全 code；关闭过程对进程、reader 和 stdin 分别限时。
- `backend/app/pi_runtime_poc/runner.py`：以 `agent_end/willRetry=false` 作为唯一成功终态；重试 end
  不收口；协议失败终态记录安全子 code；在审计前再次做事件投影。
- `backend/app/agent_runtime/events.py`：只有非重试 agent_end 才投影为 thinking 完成。
- `backend/tests/pi_runtime_poc/test_rpc.py`、`test_runner.py`：覆盖真实 Pi 0.79 事件顺序、大终态投影、
  message snapshot 隔离、安全错误码与关闭超时。

### 验证结果

- 红灯覆盖 1 MiB 大终态拒绝、缺少 error code、虚构终态和无界 close；绿灯为 POC **113 passed**、
  Pi Runtime **46 passed**、typecheck、范围 Ruff、`agent_settled` 扫描与 diff 检查通过。
- 未调用模型、DataTap、钱包、积分、单工具冒烟或 Task 9；未创建或修改真实 round。

### 遗留事项

- 真实调用需要重新授权，且只能使用新的 append-only round；停止于方案 A，不进入方案 B/C。

## Pi-only Gate A 唯一真实轮次 `20260808T060814Z`

### 背景与目标

在 Pi 0.79 `agent_end`、大终态投影和有界关闭路径完成本地验证后，按用户一次性授权运行六场景
Pi-only Task 9；不运行 Current Runtime，不访问钱包/积分，不覆盖历史 round。

### 主要事实

- 精确依赖、`deepseek-v4-flash`、`high` thinking、四个 DataTap 服务、POC 数据库、迁移 head、
  8000 与零悬挂状态预检全部通过；随后只执行一次正式 shell 入口。
- 六案例全部得到终态：三个报告 Run 为 completed_with_warnings，钻取 completed，范围澄清
  clarification_requested，非营销案例 completed。共 6 次模型调用、63 次 DataTap ToolCall（53 settled、
  10 failed）、53 条 Evidence 和 4 个 Artifact Version。
- 钱包与积分保持隔离：本轮用户 Wallet/WalletTransaction 均为 0，全库钱包与流水计数和预检基线完全
  一致，points reserved/settled 为 0；Run/Attempt/ToolCall 无悬挂，8000 已释放。
- 真实 Pi 0.79 终态投影生效：正常完成 Run 的 `agent_end` 不含 messages；本轮最小 RPC 投影约
  4.92 MB，不再出现旧轮 339 MB 完整 message snapshot。

### 验证结果与 Gate

- openpyxl 验证所有 Excel 可打开：品牌 8 Sheet、活动 9 Sheet/3 图表、两份达人名单各 4 Sheet。
- 内容 Gate 失败：品牌结构化章节几乎全不可用且 Evidence 引用为 0，活动多数核心章节不可用，两者叙事
  仍使用无 supporting_paths 的精确数值；两份达人名单均只含空昵称/unknown platform/0 分/无报价
  item，且丢失品牌、平台和预算 scope。非营销案例保持零 DataTap，但没有拒答而是直接回答。
- 严格复核 12 项评分存在多项低于 3；一次性 summary 最终为 **EVALUATED_FAIL**。finalizer 首个启动
  尝试暴露了 import 链意外要求 Settings 的本地问题；当时未创建 summary，之后使用非真实占位配置完成
  同一 round 的纯本地汇总。
- round、诊断、human-review 与 summary 的真实密钥、endpoint、DSN 扫描均为 0 命中；
  `git diff --check` 通过。

### 遗留事项

- 最小修复仅限方案 A：强制叙事数字绑定同版本 supporting_paths/Evidence lineage；修复品牌/活动
  Evidence 到结构化章节的映射；达人 Builder 保留 scope 并拒绝空候选；把非营销拒答和钻取可追溯性
  加入绝对 hard_check；移除 finalizer 的 Settings/DB import 副作用。
- 不重跑本 round，不进入方案 B/C。Gate A 修复完成并重新取得授权后，才可创建新的 append-only round。

## Marketing Capability Pack B0：Task 3R4 跨版本与活动口径收口

### 背景与目标

Task 3R3 已完成 Brand/Campaign metric-level Evidence contributor 与 comparison unit
收口；本轮只处理审核发现的三个窄范围阻断，不重写 contributor 架构，也不进入 Task 4：
历史 Artifact 读取/导出兼容、活动缺平台行误归 `all`、以及 social volume 冲突误标帖子数和
内部 spend。

### 主要改动（关键文件）

- `app/agent_artifacts/canonical.py`：允许 legacy payload 的 `canonical_data` 与
  `field_lineage` 同时缺失或为空；两者只出现其一仍 fail-closed。新增
  `require_canonical()` 作为写入/发布严格门禁，并补齐 count、score、CPC/CPM、ROI/ROAS
  的 canonical unit。
- `app/agent_artifacts/validation.py`：新建 Draft、Revision 更新和发布复用的强类型
  validator 调用严格 canonical 门禁；历史读取/导出仍走兼容 read 模式。
- `app/agent_artifacts/builders/campaign.py`：仅显式平台行进入活动平台相关聚合；明确的
  合计/全部仍可归一为 `all`，缺平台帖子被排除并以 `post_platform_missing` 标记相关章节
  partial。social volume 冲突只披露 `social_volume`，不再关联 `overview.total_volume`
  或 `internal_metrics.spend`；互动冲突仍只关联互动字段。
- `app/agent_artifacts/builders/raw_rows.py`：平台覆盖判断不再把缺失平台当作 aggregate；
  仅原始字段明确写出合计/全部时视为完整覆盖。
- 测试：新增 `test_marketing_canonical_task3r4.py` 覆盖 legacy read/export、严格写入、
  缺平台活动聚合、social volume 冲突与数值 unit；同步更新显式 aggregate 与同指标冲突断言。

### 验证结果

- R4 定向与 Builder/Exporter/Validator 回归：95 passed；完整 `tests/agent_artifacts`：
  **515 passed**。
- `tests/marketing_capability_pack tests/pi_runtime_poc`：**148 passed、9 skipped**。
- 本轮修改 Python 文件范围 Ruff（忽略既有 DTZ/PIE810/RUF012/UP042 基线项）：通过；
  `git diff --check`：通过。
- 独立代码审查：Critical 0、Important 0、Minor 0；确认四项 R4 边界均闭环。
- 未调用模型、DataTap、钱包或积分；未重跑 Task 9；CodeGraph MCP transport unavailable，
  结构审计使用 `rg` 与精确源码读取完成。

### 遗留事项

- Task 4–7、B1–B7 与方案 C 未开始；等待 Task 3 Gate 审核。

## Marketing Capability Pack B0：Task 3 两个活动 Evidence 边界修复

### 背景与目标

Task 3 Gate 复审指出两个 Important：缺平台且没有 TopPost 身份字段的纯指标行被静默
丢弃并伪造 complete；social volume 冲突未验证同指标、单位与 scope 周期，还会错误
降低 overview。只修复这两个边界，不改 contributor 架构，不进入 Task 4。

### 主要改动（关键文件）

- `app/agent_artifacts/builders/campaign.py`：独立识别非空 normalized 缺平台行；平台
  聚合继续 fail-closed 排除这些行，并只对实际可能受影响的章节/数值叶子生成
  `post_platform_missing` 与 partial。显式合计/全部仍保留为 aggregate；无比较期时不
  额外降级空 comparisons 章节。
- `app/agent_artifacts/builders/campaign.py`：social volume/engagement 冲突只比较能
  证明位于当前 scope period 的同指标行；缺日期或跨期不比较，显式非 mentions/非
  interactions 单位不比较。
  volume 冲突使用空 `affected_paths` 的来源指标披露，不改变 overview 或 spend；互动
  冲突仍只影响 `overview.total_engagement`。
- `app/agent_artifacts/builders/raw_rows.py`：保留 volume 行的显式 unit 元数据供内部
  同口径门禁使用，不改变公开 payload/API。
- 测试：新增纯指标缺平台、显式 aggregate、跨周期 volume、同周期 volume、单位不一致
  与同周期互动冲突断言；为既有冲突 fixture 补充明确日期，避免依赖无周期元数据。

### 验证结果

- TDD 红灯：基线新增边界测试 **3 failed、77 passed**；失败分别证明纯指标无 limitation、
  跨期仍产生 social conflict、volume conflict 使用错误 affected path/降级 overview。
- 定向修复测试：**19 passed**；完整 `tests/agent_artifacts`：**522 passed**。
- `tests/marketing_capability_pack tests/pi_runtime_poc`：**148 passed、9 skipped**。
- 本轮全部修改 Python 文件范围 Ruff（忽略既有 DTZ/PIE810/RUF012/UP042）：通过；
  `git diff --check`：通过。
- 未调用模型、DataTap、钱包或积分；未重跑 Task 9；Task 4–7、B1–B7 与方案 C 未开始。

### 遗留事项

- 等待 Task 3 Gate 审核；不在本提交后进入 Task 4。

## Marketing Capability Pack B0：Task 4 结构化 claims publication 门禁

### 背景与目标

Task 3 已提供 Brand/Campaign 的 canonical data 与 field lineage；Task 4 将发布前的
结构化数值事实绑定到当前候选 Artifact Version，避免叙事引用 unavailable、canonical
值漂移、partial 无限制披露或跨 Run/Session Evidence 进入不可变 Version。门禁只读取
结构化字段，不以 Markdown 正则推断数字；历史非 canonical Artifact 读取/导出保持兼容。

### 主要改动（关键文件）

- `app/agent_artifacts/lineage.py`：新增 `ValidationIssue`、`EvidenceScope` 与
  `validate_structured_claims`，精确把 dotted supporting path 转为 RFC 6901，校验
  canonical/data 恒等、field_lineage、availability、partial limitation、候选
  Version 与 Evidence scope；MCP Evidence 增加当前 Run 归属校验，Run 前上传 Evidence
  继续允许同一用户/Session 的合法例外；同时校验数据库中上传记录自身的 user/session/run
  元数据，异常关联 fail-closed。
- `app/agent_artifacts/publishing.py`：直接发布顺序固定为强类型 payload → lineage
  freeze → structured claims → Version；validation snapshot 记录 structured stage，
  预生成候选 Version UUID 并将同一 UUID 传入最终 Version，失败不更新 latest/state。
- `app/agent_artifacts/service.py`：旧 `publish_batch` 同样在 Version 前执行 claims 门禁，
  全部通过后才落 immutable Version，并保存结构化校验快照。
- `app/agent_artifacts/validation.py`：复用并导出统一结构化校验接口。
- `tests/agent_artifacts/test_publication_lineage.py`：拆分覆盖 supporting path、
  canonical/value/field_lineage、unavailable/partial limitation、候选 Version、
  跨 Session/Run Evidence、上传 Evidence、direct 与 batch 发布、Brand/Campaign 合法
  发布和同 Revision 幂等，并覆盖上传元数据跨 user/session/run 的拒绝。

### 验证结果

- TDD 红灯：旧实现下合法结构但叙事引用 unavailable canonical 的 Brand Draft 实际
  `published`，新增测试因此按业务断言失败；不是导入、夹具或环境错误。
- 绿灯：Task 4 定向测试 **19 passed**；定向 direct/lineage 回归 **70 passed**；完整
  `tests/agent_artifacts` **541 passed**；`tests/marketing_capability_pack tests/pi_runtime_poc`
  **148 passed、9 skipped**。
- 本次修改 Python 文件范围 Ruff（忽略既有 DTZ/PIE810/RUF012/UP042）：通过；
  `git diff --check`：通过。独立代码审查 Critical/Important 均为 0；新增上传元数据边界
  已在审查后补充并复测。
- 未调用模型、DataTap、钱包或积分；未运行或修改 Task 9；Task 5–7、B1–B7 与方案 C
  未开始。

### 遗留事项

- Task 4 完成后等待独立审核；本提交后停止，不进入 Task 5。

## Marketing Capability Pack B0：Task 4R 历史 lineage 与 validation snapshot 收口

### 背景与目标

Task 4 审核补充要求历史 Artifact 可作为当前 Run 的受控输入，同时不能放松当前 Run
直接 MCP Evidence 的归属门禁；并要求 validation snapshot 明确区分 structured claims
未执行与已评估结果，避免 lineage 失败被误记为 claims 通过。

### 主要改动（关键文件）

- `app/agent_artifacts/lineage.py`：ArtifactSource 递归进入独立的历史 lineage 上下文，
  保留当前 user/session、Artifact Version、JSON Pointer、payload hash 与工具状态校验；
  外层直接 Evidence 仍必须属于当前 Run。历史上传 Evidence 的文件审计字段保持冻结不变。
- `app/agent_artifacts/publishing.py`：structured claims stage 在 payload/lineage 失败时写入
  `status=not_evaluated` 且不执行校验；成功或 claims 失败分别写入
  `status=evaluated` 与真实 valid/error 结果。
- `app/agent_artifacts/service.py`：batch 成功快照写入 `structured_claims.status=evaluated`，
  并继续使用传给 validator 的同一候选 Version UUID 持久化 Version。
- 测试：新增历史 MCP/上传 lineage、当前 Run 跨 Run 拒绝、冻结闭包不变、claims
  not-evaluated/evaluated 与 direct/batch 候选 UUID 捕获断言。

### 验证结果

- 红灯：历史 Artifact 递归旧 Run Evidence 被错误拒绝；lineage 失败 snapshot 缺少
  `not_evaluated`；batch snapshot 缺少 evaluated 状态。
- 定向 Task 4R：**75 passed**；完整 `tests/agent_artifacts`：**546 passed**；
  `tests/marketing_capability_pack tests/pi_runtime_poc`：**148 passed、9 skipped**。
- Task 4R 修改 Python 文件 Ruff（忽略既有 DTZ/PIE810/RUF012/UP042）：通过；
  `git diff --check`：通过。
- 未调用模型、DataTap、钱包或积分；未运行 Task 9、Task 5–7、B1–B7 或方案 C。

### 遗留事项

- Task 4R 审核通过后进入 Task 5；本提交保持独立，后续任务不混入本提交。

## Marketing Capability Pack B0：Task 5 KOL scope 与候选发布有效性

### 背景与目标

Task 4R 完成后，本轮只收口 KOL 圈选产物的范围冻结与候选发布门禁，不改变
Root Policy、Skill/Pi Runtime 边界，也不提前进入 Task 6。历史
`kol_selection_v3` 必须继续可读取和导出，因此新增范围字段采用兼容默认值，
只在 direct/batch 正式发布前执行候选有效性校验。

### 主要改动（关键文件）

- `app/agent_artifacts/payloads/kol_selection.py`：新增
  `KolSelectionScopeV3`，完整保存 brand/category、platforms、audience、region、
  age_range、period、budget、filters、ranking_mode、top_limit、scoring_version；
  保留 `KolSelectionScope` 兼容别名与历史默认值。
- `app/agent_artifacts/builders/kol_selection.py`：新建名单补齐范围默认值，缺失
  `kol_uid` 不再用 nickname 冒充稳定身份，并保持显式报价 0；评分版本、排序模式、
  Top limit 随 payload 冻结。
- `app/agent_artifacts/validation.py`：新增纯值对象
  `validate_kol_candidates(payload) -> list[ValidationIssue]`，拒绝空昵称、unknown/all
  或越界平台、无稳定身份、全评分缺失、叙事点名名单外达人、预算/报价不一致与空名单
  正式发布；新增 gate 仅由 direct/batch 发布调用，空名单仍可作为 Draft gaps 保存。
- `app/agent_artifacts/publishing.py`、`service.py`：direct 与 batch 在 lineage 前的
  强类型发布校验中启用 KOL 候选门禁，旁路篡改不能生成 Version。
- `app/agent_artifacts/exporters/{__init__,kol_selection}.py`：导出只接受 published
  或历史兼容 Version；显式失败 validation snapshot、无效候选统一 fail-closed。
- `tests/agent_artifacts/test_kol_publication_validity.py`：覆盖合法/历史兼容、范围字段、
  身份与平台、全空评分、narrative outsider、预算、空名单、direct/batch 旁路及导出边界。

### 验证结果

- TDD 红灯：新增门禁测试首轮在收集阶段因 `validate_kol_candidates` 尚不存在而失败；
  该失败证明测试先于实现建立。
- 定向 KOL 有效性、Builder、Exporter：**49 passed**；完整
  `tests/agent_artifacts`：**563 passed**。
- `tests/marketing_capability_pack tests/pi_runtime_poc`：**148 passed、9 skipped**。
- 本轮全部修改 Python 文件范围 Ruff（忽略既有 DTZ/PIE810/RUF012/UP042）：通过；
  `git diff --check`：通过。
- 独立代码审查：Critical 0、Important 0、Minor 0；审查后修复了昵称回填与报价
  无快照痕迹问题并重新通过定向/全量门禁。未调用模型、DataTap、钱包或积分。

### 遗留事项

- Task 6 纯本地 Gate/finalizer、Task 7 离线六案例回放尚未开始；本提交完成后再进入
  Task 6，禁止混入本提交。

## Marketing Capability Pack B0：Task 6 纯本地 Gate/finalizer

### 背景与目标

Task 5 后继续收口 B0 POC 的验收边界。本任务将业务 hard checks 从带运行时依赖的
comparison 路径拆为纯值对象 Gate，并让 finalizer 在没有数据库、模型或 DataTap 环境的
干净子进程中只读取 execution、fixture 和人工复核，严格一次性追加 summary。Task 7
离线六案例回放尚未进入本提交。

### 主要改动（关键文件）

- `app/pi_runtime_poc/gate.py`：新增仅依赖标准库的 `HARD_CHECKS`、`evaluate_case`、
  `finalize_execution`、`Summary` 和 `write_summary_append_once`。十项检查覆盖数值
  lineage、scope、KOL 候选、叙事、钻取 Version/证据、拒答、澄清、重复报告及 partial
  limitations；报告需有期望 Artifact/Version/Evidence，KOL 候选需有稳定身份、昵称、
  合法平台和观测评分输入，非报告案例显式检查零 Artifact/零 DataTap。
- `app/pi_runtime_poc/comparison.py`：保留既有执行器与旧 Gate 兼容 API，并导出纯 Gate
  接口，避免改变已有 POC runner。
- `scripts/finalize_pi_runtime_poc.py`：移除运行时/基础设施导入，只读取 JSON，拒绝密钥
  形态并通过独占创建保证 summary append-once；已有 summary、坏 manifest、secret 或
  缺人工复核均 fail-closed。
- `tests/pi_runtime_poc/test_gate.py`、`test_finalizer_isolation.py`：覆盖十项检查逐项
  失败、KOL 候选、精确钻取 Version、澄清/拒答零副作用、人工复核、干净子进程导入和
  summary 不覆盖。

### 验证结果

- TDD 红灯：新增测试首轮因 `app.pi_runtime_poc.gate` 不存在在收集阶段失败；实现后
  Gate/finalizer 定向 **19 passed**。
- POC 回归：`tests/pi_runtime_poc` **140 passed、9 skipped**；comparison/Task9 兼容
  定向 **24 passed**。
- Task 6 Python 范围 Ruff（忽略既有 DTZ/PIE810/RUF012/UP042）：通过；
  `git diff --check`：通过；纯 Gate 六案例形状演练十项检查 **60/60 true**。
- 未调用模型、DataTap、钱包或积分；未重跑或修改真实 Task 9。

### 遗留事项

- Task 7 离线六案例业务回放尚未开始；本提交完成后才进入 Task 7，不进入 B1–B7 或方案 C。

## Marketing Capability Pack B0：Task 7 六案例离线业务回放

### 背景与目标

Task 6 的纯本地 Gate 已具备十项业务门禁；本任务用脱敏 synthetic Evidence shape 和
fake Pi events 回放品牌、活动、KOL、Artifact 钻取、信息澄清及非营销拒答六类行为，
证明 B0 可重复验证而不触碰真实模型、DataTap、钱包、积分或 Task 9。回放不是对真实
供应商效果的结论，也不伪造人工评分。

### 主要改动（关键文件）

- `app/pi_runtime_poc/replay.py`：新增纯标准库 `run_offline_marketing_replay` 与
  `OfflineExecution`，校验固定案例顺序、Pi/completed 状态、期望 Artifact 类型、报告
  Version 去重、钻取确切 Version、澄清/拒答零副作用、fake event 顺序和 fixture 密钥
  模式；输出稳定 fixture digest 与不含人工分数的 hard-check summary。
- `fixtures/pi_runtime_poc/marketing_b0/{cases,results,events}.json`：新增脱敏六案例
  输入，前三案例带 Version/Evidence/scope 与 hard-check 元数据，后三案例明确零 Artifact
  和零 DataTap。
- `tests/pi_runtime_poc/test_marketing_b0_replay.py`：覆盖确定性、三类报告十项门禁、
  钻取绑定篡改、澄清/拒答零副作用和事件/案例篡改 fail-closed。
- `docs/qa/pi-runtime-poc-rounds.md`：记录本地 synthetic-only 回放结果，明确不等同
  真实 Gate A、未运行 Task 9。

### 验证结果

- TDD 红灯：新增测试首轮因 `app.pi_runtime_poc.replay` 不存在在收集阶段失败；实现后
  Task 7 定向 **6 passed**。
- 后端 `tests/pi_runtime_poc tests/agent_artifacts`：**709 passed、9 skipped**；Task
  范围 Ruff 与 `git diff --check`：通过。
- Pi Runtime `npm test`：**47 passed（9 files）**；`npm run typecheck`：通过。
- 独立代码审查结论在提交前复核；未调用模型、DataTap、钱包、积分，未运行或修改 Task 9。

### 遗留事项

- B0 Task 1–7 已完成；按范围停止，不进入 B1–B7 或方案 C，不进行真实 UAT。
