# 2026-08-08

## Gate A 改为 Pi-only 绝对验收

### 背景与目标

连续真实 Task 9 已证明 Current/Pi 双路径对比增加了钱包、夹具和依赖编排复杂度，却仍在 Pi 审计
基础设施稳定前消耗完整六场景轮次。用户确认方案 A 不再评价 Pi 是否优于 Current，只验证 Pi 能否
独立完成营销业务闭环。

### 主要改动（含关键文件）

- 新增 `docs/superpowers/specs/2026-08-08-pi-only-gate-a-design.md`，覆盖旧方案 A 的双 Runtime
  对比口径；生产 Current Runtime 和方案 B 灰度回滚不在删除范围。
- 新增 `docs/superpowers/plans/2026-08-08-pi-only-gate-a.md`，将实施拆为 Task 8E 序号当前读、
  Pi-only factory、六案例隔离编排、绝对 Gate finalizer 和唯一真实轮次五项任务。
- Task 9 改为仅执行 Pi 六场景，以 Artifact/Excel/BI、Evidence lineage、澄清、钻取、拒答和人工
  质量评分的绝对门槛作 Gate，不再计算相对 Current 指标。
- 新增 `BLOCKED / INFRA_FAILED / EVALUATED_FAIL / PASS` 四态，避免把审计或 Harness 失败误写成
  Pi 分析效果失败。
- Task 8E 先统一修复 Attempt/Step/Event 当前读和独立案例收口；真实 MySQL 拓扑回归通过后直接
  运行一次 Pi-only 完整 Task 9，不再增加重复品牌 canary。

### 验证结果

- 本次仅完成设计修订，未改 Runtime、未调用模型/DataTap、未创建 Run 或 round。
- 规格明确六场景结果必须相互隔离，执行器始终生成完整且不可变的 `execution.json`；技术执行完整后
  由零模型、零 MCP finalizer 合并人工评分生成 `summary.json`。钻取依赖失败不得阻断澄清和非营销案例。
- 设计与计划通过 `git diff --check`；未运行代码测试，未调用模型、DataTap 或 Pi。

### 遗留事项

- 待独立开发会话按新计划完成 Task 8E 与 Pi-only Task 9；当前工作树已有 Task 8D 未提交代码，
  实施时必须保留并按任务范围验证，不能 reset 或混入不相关提交。
- 方案 B/C 尚未启动；Pi-only Gate A PASS 后才进入方案 B。

## 背景与目标

修复 Task 9 中 Pi 审计 Step 为 0、而 DataTap 后台已有成功调用记录的问题，并在全部本地门禁通过后
执行一次新的真实 Current/Pi 对比。保持方案 A、精确 POC 数据库、Pi 零积分和不重复真实轮次边界。

## 主要改动（含关键文件）

- `backend/app/pi_runtime_poc/audit.py`：`PiRunAuditWriter` 查询最新 Attempt 和最大 Step sequence 时
  使用持锁当前读，避免长会话的 `REPEATABLE READ` 快照漏掉 Extension HTTP 请求已提交的审计行。
- `backend/tests/pi_runtime_poc/test_task8d_audit_concurrency.py`：新增真实 MySQL 旧快照回归，覆盖
  Runner 先建立快照、Extension 独立提交、Runner 再写 RPC 审计的精确顺序。
- `docs/qa/pi-runtime-poc-rounds.md`：记录 round `20260807T162812Z` 的真实调用、收口、根因与 Gate。

## 验证结果

- 红灯：旧实现触发 `agent_run_attempts.uq_agent_run_attempts_run_attempt` 唯一键 `IntegrityError`；
  这是同一快照缺陷比历史 Step 冲突更早的表现。
- 绿灯：定向旧快照测试 1 passed；Task 8D 真实 MySQL 并发测试 4 passed；POC 后端 89 passed；
  Pi Runtime 50 passed，typecheck 通过；本次范围 Ruff 与 `git diff --check` 通过。
- 预检：数据库为 `kol_insight_pi_poc`，迁移为 head，可执行态 Run 为 0，模型为
  `deepseek-v4-flash`，四个 DataTap 服务映射完整，8000 空闲。
- 唯一真实轮次实际创建 6 个 Run：三个 Current 路径完成或带警告完成；三个 Pi 路径均在至少一次
  真实 DataTap settled 调用并形成 Evidence 后失败。失败约束为
  `agent_events.uq_agent_events_run_sequence`。
- 轮次因 Pi 品牌 Artifact 不可用，在产物钻取依赖预检处以 `poc_dependency_run_unavailable` 停止；
  没有执行剩余澄清和非营销案例，没有 `summary.json`。
- 收口后所有 Run 均为终态；5 条 Pi running ToolCall 仅迁移为 unknown，未重放；可执行态 Run=0、
  悬挂 ToolCall=0、8000 已释放、密钥模式扫描 PASS。Pi 积分为 0。

## 遗留事项

- **Gate A FAIL（POC 审计/验收夹具失败）**，不能评价 Pi 与 Current 的真实分析效果。
- 下一步若获授权，应先增加 Task 8E：修复 `AgentEventStream.append_locked()` 的 Event sequence 旧快照
  读取，并让单侧依赖失败不再中止整轮；本地门禁通过后再单独申请新的真实 round。
- 不进入方案 B/C。若未来 Gate A 通过，应基于实测 Pi API 新建方案 B 计划；Pi 链路稳定一个发布周期后
  再评估 Marketing MCP Gateway（方案 C）。
