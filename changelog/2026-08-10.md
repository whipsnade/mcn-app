# 2026-08-10

## 背景与目标

架构独立审核否决了 2026-08-09 写入的 `READY_FOR_REAL_B7_UAT`：方案 B 的生产组合链没有真正接通
（当时的「本地 UAT」是组件级 fixture，未启动真实 FastAPI/Gateway 进程）。本日按
REQUIRES_PLAN_B_REPAIR 执行修复：7 个 Repair 独立提交，恢复完整离线进程级 fake topology，
最终状态 `READY_FOR_REAL_B7_UAT_REVIEW`（是否恢复 READY 由架构审核独立复审决定）。

授权边界：只做本地代码修复 + 离线进程级集成测试 + 管理端补全 + 文档纠偏；未触碰真实模型、
真实 DataTap、真实钱包/积分、历史真实 Task 9（round `20260808T060814Z` 永为 EVALUATED_FAIL）、
真实 B7 UAT、生产切流或方案 C。

## 主要改动

### Repair 1（bee93d5）fix: make pi gateway control path executable

- HMAC 跨语言修复：Node 改对完整挂载路径 `/api/v1/internal/pi-gateway/v1/...` 签名（此前签
  `/claims`，真实请求必然 401）；跨语言固定夹具两侧测试互锁签名值。
- 新增生产 Gateway 组合根：`pi-gateway/src/{config,health,main}.ts`、`tsconfig.build.json`、
  `npm start`（fail-closed 配置校验、claim/tick 有界 backoff、health/readiness/metrics、
  SIGTERM/SIGINT draining 有限时退出；secret envelope 在 run/attempt/config/gateway AAD 下解密）。
- Minor：test_event_idempotency.py EOF 空行；conftest 注入 TENCENT_PLAN_API_KEY/
  DATATAP_MCP_TOKEN 测试占位（裸 pytest 不再依赖开发者 .env）。

### Repair 2（e6871c2）fix: bridge isolated pi worker control plane

- 父子 IPC RPC 桥（`ipc-rpc.ts`/`worker-bridge.ts`）：父持 HMAC secret 与 lease token 不下发；
  Child 内部工具调用经 IPC 由父转发 FastAPI；MCP 顺序严格 preflight durable commit → adapter
  外发 → finalize/fail；adapter 按路径 jiti 加载；`.mcp.json` 只含环境变量引用。

### Repair 3（c73a07c）fix: close pi runtime recovery and accounting gaps

- Pi recovery 接入 lifespan（pi 专属 RecoveryLoop，只处理 runtime_backend=pi；第一次基础设施
  丢失一次新 Attempt，第二次 failed；cancel/unknown 不恢复不重放；current executor 永不领 Pi Run）。
- projector 多事件输出 + terminal 完成门禁（completed 前必须有 assistant completion，usage 不吞
  message.completed；409 时 gateway 安全收口 failed）。
- TenantWallet 唯一事实源 fail-closed（删除旧钱包回退分支）；License 在 reserve_mcp_call 内复核；
  nonce 窗口基于签名时间戳+skew。

### Repair 4（998831d）fix: enforce idempotent admin and safe billing rollback

- 迁移 `0042_admin_idempotency_records` + `0043_billing_downgrade_guard`；管理写操作真实幂等
  （同 key 同请求回放/异请求稳定 409，操作+幂等记录+审计同事务，secret 不入记录）；
  downgrade guard 经真实测试 MySQL 验证（迁移后发生新流水则 fail-closed）。

### Repair 5（28c8612）feat: complete pi gateway administration console

- B6 管理台六模块（License/RuntimeConfig/PiRuntime/Usage/RunDiagnostics/TenantAdmin）：
  版本追加/激活/暂停、write-only secret 不回显、容量与 draining、用量分页筛选、诊断时间线
  unknown/reserved 标记；dialog label/键盘关闭/focus trap，不再依赖 window.prompt/confirm。
  前端 278 passed/37 files、lint/build 干净。

### Repair 6（523e00e）test: restore full offline pi gateway topology gate

- 恢复 f3186be 原始 Task 12/13 验收口径：新增
  `backend/tests/integration/test_pi_gateway_offline_uat.py` 共 16 个进程级场景（清单见
  `docs/qa/pi-agent-gateway-local-uat.md` 2026-08-10 版）；删除被弱化的
  `test_pi_gateway_local_uat.py`（纯函数断言）。
- 进程级拓扑暴露并修复的生产缺陷：`turn.start` 事件别名三份白名单（projector/protocol.ts/
  contracts.py + events.py）漂移导致事件流断流与租约过期双重执行；heartbeat 单次失败即丢租约
  （改连续 3 次）；abort 仅 SIGTERM 杀不掉卡住的子进程（加 5s SIGKILL 升级）；worker 显式
  process.exit；`get_session_context` 多列查询误用 `scalars()`；MCP finalize 写 Evidence
  （输出 Schema 校验、幂等回放、succeeded_empty）；`datatap_mcp_origin`/
  `agent_recovery_interval_seconds` 走 Settings（离线拓扑加速用，非测试旁路）。
- harness 加固：启动中途失败也回收子进程（泄漏 FastAPI 会在共享测试库窃取后续拓扑的 Run，
  是此前全文件回归级联失败的根因）。

### Repair 7（本次）docs: correct plan b readiness after repair

- `docs/qa/pi-agent-gateway-local-uat.md` 重写为 2026-08-10 版：进程级拓扑证据、场景清单、
  flake 记录，状态 `READY_FOR_REAL_B7_UAT_REVIEW`。
- 方案 B 计划追加「修复期偏差记录（2026-08-10）」：如实记录 08-09 READY 被否决、组件级→进程级
  的口径恢复、各 Repair 提交，不重写历史设计。
- runbook 补 heartbeat 连续失败语义与 abort SIGKILL 升级、离线拓扑入口。

## 验证结果

- 离线进程级 UAT：16/16 全绿（275.94s；此前两次全文件回归失败均为被中断运行遗留进程污染
  共享测试库的级联，清场后恢复，harness 已加固）。
- tests/pi_gateway 70 全绿；pi-gateway 93 vitest 全绿 + typecheck/build；ruff 通过；
  git diff --check 通过。最终全量（backend pytest 全量、pi-runtime、前端）见本日最终汇报。

## 遗留事项

- 共享测试库 + 固定 gateway_id（gw-uat-1）：并行会话同时跑 UAT 文件会互相污染，需清场串行。
- Attempt 2 在第二次基础设施失败后 outcome 保持 running 敞开（随 Run 终态失效，语义自洽，
  未加断言）。
- nonce 屏障 `_authenticate` 的过期删除跨行加锁，多 gateway 实例并发下理论存在竞争，留作观察点。
- 最终状态仅为 READY_FOR_REAL_B7_UAT_REVIEW；真实 B7 UAT 需要新的明确授权、独立测试租户、
  测试钱包、append-only 证据目录和停止条件。
