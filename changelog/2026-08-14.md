# 2026-08-14

## Web Scenario 2 门禁纠偏：audited baseline 固化到 Direct Artifact Skill 修复线（纯文档）

### 背景与目标

- Direct Artifact Skill 契约修复在 `codex/direct-artifact-skill-contract-repair` 分支完成
  （六个线性提交 `284e4c7`/`45ec465`/`260f5cc`/`d4ab189`/`f15ff5d`/`37be5b6`，独立审查
  Critical 0 / Important 0 / Minor 0），修复真实 Web Scenario 2 的失败根因（模型被迫手写
  服务器字段、看不到 Artifact Schema、错误反馈无字段路径、capability pack 残留 Evidence
  必经指令）。
- 本轮把现行 audited baseline 从 `c01ec1ba`（Direct MCP Smoke baseline）切换到
  `37be5b67c52764abb0ab38c458197d3827729144`（Direct Artifact Skill baseline），统一
  Web Functional Scenario 2 的 execution gate：execution HEAD 必须是 `37be5b67` 的线性
  后代、包含上述六个已审核提交、`37be5b67..execution HEAD` 区间只允许本次门禁纠偏的
  Markdown/changelog 提交、执行时工作树必须干净、禁止 reset/rebase/amend、下一条真实
  UAT 授权消息必须逐字绑定纠偏提交后的完整 execution HEAD、branch 必须是
  `codex/direct-artifact-skill-contract-repair`。
- `c01ec1ba` 只保留为历史 Direct MCP Smoke baseline，不再是新 Scenario 2 的 execution
  gate；旧表述（「c01ec1ba..HEAD 只能包含文档变更」「只包含 284e4c7/45ec465/260f5cc
  三个修复提交」「branch codex/real-mcp-evidence-bridge-repair 作为现行执行分支」）删除
  或明确标为历史规则。
- 纯文档任务：未启动 FastAPI/Gateway/Frontend，未连接数据库、模型、DataTap 或钱包，未
  创建 UAT round。

### 主要改动（关键文件）

- `docs/superpowers/plans/2026-08-12-real-b7-uat-authorization-plan.md`：状态头、
  §0 表（新增现行 audited Direct Artifact Skill baseline `37be5b67…` 行，`c01ec1ba…`
  改标历史、Direct Artifact Skill 契约修复行更新为六提交、execution commit 规则、当前
  交付状态）、事实判读段、§1.1、§1.2（commit_sha/branch）、§7.3 启动门禁、§8 状态入口
  与 §8.1 执行身份门禁全部切换到新门禁。
- `docs/qa/2026-08-12-pi-b7-uat-authorization-pack.md`：状态头、§5.4 引言状态入口与
  单场景授权模板的 execution commit 身份规则/branch 切换到新门禁。
- `docs/runbooks/pi-agent-gateway.md`：状态头更新为 Direct Artifact Skill 门禁纠偏状态
  （现行 audited baseline `37be5b67…` + 六提交 + 审查结论 + 完整执行门禁规则）。
- `changelog/2026-08-14.md`：本文件。

### 保持不变（明确未改）

- Direct MCP 架构（标准 MCP Tool Result 直接交给模型，不恢复 Evidence Bridge）。
- capability pack `marketing-v2` / 1.1.0（manifest digest
  `65d28bb1afdc20c51729f4222fbed929165d0412ad188f59d7d55d5c9e9931ce` 不变）。
- A/B/C 三档功能验收口径（PASS / PASS_WITH_ACCOUNTING_WARNINGS / FAIL）。
- `ACCOUNTING_UNKNOWN_DIAGNOSTIC_REQUIRED` 遗留项。
- Evidence 增量 0 为预期事实；预算、自主决策边界与专用 UAT 数据库隔离规则。

### 验证结果

- 门禁核验：branch `codex/direct-artifact-skill-contract-repair`、HEAD 严格等于
  `37be5b67…`、工作树干净、`7c40d864` 与 `c01ec1ba` 均为祖先、六个提交线性存在。
- rg 检查：三份文档中 `c01ec1ba` 仅出现在历史语境（Smoke round 名、历史基线行、历史
  状态流转），§8.1 与授权包 §5.4 使用相同的新门禁表述。
- secret/token/DSN 扫描：0 命中。
- `git diff --check` / `git diff --cached --check` / `git show --check`：通过。
- 未运行 pytest/npm（纯文档纠偏）。

### 遗留事项

- 状态保持 `READY_FOR_WEB_FUNCTIONAL_SCENARIO_2_RERUN_REVIEW`；真实 Scenario 2 重跑
  必须由用户按授权包 §5.4 单场景模板逐字重新授权。
- 两个离线 UAT 场景（`test_generic_proxy_bare_remote_name_billing_chain`、
  `test_model_budget_missing_fails_closed_before_worker_start`）在串行全量 pytest 中出现
  波动性失败，单独重跑与 UAT 全文件（28 passed）均通过——记录为既有 flakiness，待后续
  UAT 基础设施轮次独立诊断，不阻塞核心功能 Scenario 2 重跑。
- `ACCOUNTING_UNKNOWN_DIAGNOSTIC_REQUIRED` 仍为独立遗留项（不阻塞核心功能 Scenario 2
  重跑，但阻塞完整 B7 PASS 与生产切流）。

## Web Functional Scenario 2 验收复核与 main 集成审核（纯文档）

### 背景与目标

- 真实 Web Functional Scenario 2（round `DIRECT_MCP_WEB_S2_1786672664_bab957e2`，
  execution HEAD `bab957e2`）已执行并独立复核；本段记录验收结论、accounting
  warning、reviewer 修正事项与 main 集成审核结果。本段为纯文档，未调用真实模型/
  DataTap/钱包，未创建新 UAT Run，未修改代码/测试/迁移。

### 主要改动（关键文件）

- 新建 `docs/qa/2026-08-14-web-functional-scenario-2-review.md`：功能验收复核记录
  （结论、accounting warning、reviewer 修正事项、合并前代码审核 APPROVE_WITH_MINORS、
  main 集成阻塞状态与恢复路径、边界声明）。
- 证据目录 `/private/tmp/DIRECT_MCP_WEB_S2_1786672664_bab957e2/` 由 independent
  reviewer 封口：verdict.md、reviewer 帧 + round_sealed 帧、目录级 hashes.sha256。
- `changelog/2026-08-14.md`：本段。

### 验证结果

- 独立复核：Run/Attempt/terminal、事件顺序、Artifact 归属、BI/Excel 同版、钱包恒等式、
  Evidence 增量 0、隔离证明全部与 operator 报告一致（数据库只读重核）。
- 合并前代码审核（7c40d864..bab957e2）：Critical 0 / Important 0 / Minor 4
  （截断算法边缘、insight module 硬编码、_VersionLike 死值、跨模块私有导入），
  主链不变量全部成立。
- secret/DSN 扫描 0 命中；`git diff --check`/`git show --check` 通过。
- main 工作树不干净（.codex/config.toml 修改、已删除 xlsx、三个未跟踪目录），
  按授权规则不得覆盖/stash/清理，merge 未执行。

### 遗留事项

- Scenario 2 判定 `FUNCTIONAL_SCENARIO_2_PASS_WITH_ACCOUNTING_WARNINGS`；本轮新增
  2 笔 unknown（reserved=20）未处理，与 `ACCOUNTING_UNKNOWN_DIAGNOSTIC_REQUIRED`
  一致，阻塞完整 B7 PASS 与生产切流。
- 尚未完成完整 B7 / 生产切流 / 方案 C / Scenario 3–7。
- 下一步为 main 集成：待 main 工作树由文件所有者清理后，以非快进 merge 保留历史
  集成 repair 分支，再执行合并后验证（pytest 串行、Gateway Vitest/typecheck/build、
  前端全量）。
