# 2026-08-03

## 统一 Agent 运行时切档收尾：运行手册、Changelog 与发布回滚清单（Task 27）

- 背景与目标：模型主导统一 Agent 运行时（Agent Runtime v3）的代码与测试已完成（2026-08-02，
  `codex/agent-runtime` 分支，设计
  `docs/superpowers/specs/2026-08-02-model-led-agent-runtime-design.md`，实施计划
  `docs/superpowers/plans/2026-08-02-model-led-agent-runtime.md`）。本任务是该迁移的
  **最终任务**：把仓库文档切换到新架构、写一次性切换清单（含 UAT 发现的两个 must-resolve
  阻断项）、跑最终验证矩阵并提交。
- 架构要点（一次性切换）：每条用户消息创建一个可恢复、可审计的 Agent Run；四个 Profile
  （`session_analyst_v1` / `artifact_reviewer_v1` / `kol_detail_v1` / `utility_v1`）只限能力集合，
  不含固定业务调用顺序；统一动作协议 `ask_user / call_tool / submit_review / complete`；证据
  `evidence_items` 不可变、正式 Artifact 强类型 + 字段级 lineage，Draft → Reviewer（最多两次
  revise）→ 不可变 Version 原子发布；Run Attempt 30 分钟 / 50 决策保护；DataTap MCP 每次 10
  积分，故障分类 definitely_not_sent / failed_confirmed / result_unknown / settled，unknown 禁止
  自动重放。旧 Brainstorm、GoalPlanner、TaskGoal、业务 Agent Loop 与四个 Quick 功能已删除
  （`brainstorm/`、`orchestration/` 整包移除，quick/goals/tasks/artifacts/reporting/workspace/
  selection 的 `models.py` 仅保留 legacy ORM 只读）；迁移 `0027_agent_runtime_v3` 只新增新表，
  **不 drop 旧表**。

### 主要改动

- `AGENTS.md`：项目概述、目录、API 契约、测试策略全部切换为 Agent Runtime v3；删除对
  GoalPlanner / TaskGoal / Quick / 旧 orchestration/loop / reporting/builders / selection/exporter
  的当前路径描述；标注 legacy ORM 只读、changelog 需 `git add -f`。
- `README.md`：当前能力 / 技术架构 / 验证命令改为新运行时（品牌/活动/圈选/KOL 详情经 agent
  runtime；Quick 四入口已移除，能力并入普通会话）。
- `docs/runbooks/phase-2-runtime.md`：保留 UAT/生产部署约定（ssh/systemd/nginx/rsync/凭据日志），
  API 与模型引用改为 `/api/v1/agent`、Run/Attempt/Evidence/Reviewer 与恢复核对。
- `docs/runbooks/agent-runtime-v3-cutover.md`（新建）：一次性切换清单——测试库迁移、生产备份、
  前后端同批部署、路由冒烟（旧执行入口 404）、功能冒烟、积分抽查、发布闸门复核、回滚应用版本；
  明确首次切换不 drop 旧表。含设计 §19 发布阻断条件重述与两个 must-resolve 阻断项
  （见遗留事项 1/2）。
- `.gitignore`：新增 `outputs/`（UAT 结构化结果不提交）。

### 验证

- 后端：`.venv/bin/ruff check app tests` 通过；`.venv/bin/pytest -q` → **808 passed / 1 failed /
  9 skipped**。唯一失败为既有环境性用例 `test_real_tencent_adapter_uses_confirmed_model`
  （断言 `deepseek-v4-pro`，本机 `backend/.env` 为 `glm-5.2`），与本任务无关。
- 前端：`npm run test` → **339 passed（37 文件）**；`npm run lint`（tsc --noEmit）零错误；
  `npm run build` 成功（仅 chunk >500kB 提示）。
- E2E：`npm run test:e2e` → **42 passed**（desktop-1440 / tablet-1024 / mobile-390 三视口全过，
  含「四个旧快捷入口消失」断言）。
- 迁移：`alembic heads` → 仅 `0027_agent_runtime_v3 (head)`。
- 真实 UAT 未复跑（Task 26 已执行），复跑入口 `cd backend && ./scripts/run_real_agent_uat.sh`。

### 遗留事项

1. **阻断项（cutover 前必须解决）**：真实 DataTap 传输层无可靠超时/取消（Incident #8，长查询可
   挂死 Run）；真实模型在 Attempt 预算内无法可靠产出 lineage 有效正式 Artifact（probe 45 决策 /
   17 次 revision）。详细判定标准与修复方向见 `agent-runtime-v3-cutover.md` §1。
2. 其余 UAT 缺口：`kol_detail_v1` Profile 未允许 `MCP_TOOLS`（达人详情真实 fetch 链路不可用）；
   `app/main.py` 生产引擎未注册 calculation/history/artifact 静态工具（不补齐无法产出正式
   Artifact）。
3. **首次全量 pytest 出现 11 个 agent_runtime/agent_artifacts 用例的瞬态失败**，隔离运行全过、
   随后两次全量稳定（808 passed/1 env fail）——疑似测试库状态/隔离的偶发 flake，后续全量跑
   复现再排查。
4. `outputs/` 已加入 .gitignore；`outputs/agent-runtime-uat-results.json` 为 Task 26 已提交文件，
   保持跟踪（UAT 记录引用其路径），如需彻底移出版本库另立清理。
5. `changelog/` 在 .gitignore 中但既有 changelog 文件均跟踪；新增 changelog 需 `git add -f`。
6. 旧前端残留 API Client（`src/api/sessions.ts` / `tasks.ts` / `brainstorm.ts` / `taskStream.ts`
   与 `taskEvents.ts`）与后端 `thinking/` SSE 流仍存在但新运行时不再消费/写入，属待清理只读残留。

## 统一 Agent 运行时二次审核与修复设计

### 背景与目标

- 对照当前 `codex/agent-runtime` 代码、原始设计、真实 UAT、Codex 初审和 Kimi 审核再次逐项
  核对，统一问题严重级别并形成可实施的加固设计。
- 结论保持为“禁止切档”：生产静态工具未注册、MCP 外发前持久化不成立、Artifact 发布缺少
  代码级强类型和 frozen lineage、DataTap 长调用与真实 Artifact 交付仍是核心阻断项。

### 主要改动

- 新增设计：`docs/superpowers/specs/2026-08-03-agent-runtime-v3-hardening-design.md`。
- 校正审核结论：`tool.started` 的提交发生在积分预留之前，预留/调用行仍处于外层未提交事务，
  因此 B3 完全成立；Task 26 应表述为“部分真实 UAT 已执行，核心闭环未通过”。
- 已读状态修复采用新增 `agent_artifact_read_states`，不改旧表外键，保留应用版本回滚能力。
- 将 Artifact 发布前强类型校验和 frozen lineage 固化提升为发布阻断项；补充
  `data_status=restricted` 缺少反向章节聚合约束的问题。
- 修复分为两道闸门：Gate A 加固可信运行时，Gate B 完成确定性 Builder、Prompt、前端契约和
  真实 UAT。两道闸门都通过前不得合并。
- 明确模型/代码边界：模型决定澄清、查询、证据、产物和钻取流程；Builder 只把模型选定的
  Evidence 确定性转换成强类型 Artifact，不主动查询 MCP 或决定业务流程。

### 验证结果

- 本次仅新增设计与审核记录，没有修改业务代码、数据库或运行配置。
- 设计已完成占位符、内部一致性、范围和歧义检查；没有 TBD/TODO 或未决业务口径。

### 遗留事项

- 设计需由用户完成书面复核。
- 用户确认 Spec 后，分别生成 Plan A“可信运行时加固”和 Plan B“Artifact 交付与切换”的详细
  测试先行实施计划；尚未进入业务代码实施。

## A1：新建 agent_artifact_read_states 并切换已读水位（加固设计 §5.2/§5.6）

### 背景与目标

- 二次审核发现：旧表 `artifact_read_states.session_id` 外键指向旧 `sessions`（迁移 0022 建），
  新系统不写旧 `sessions`，`PUT /api/v1/agent/sessions/{id}/artifact-read-state` 首次写入在
  生产必触发 FK 1452 → 500；既有测试靠测试脚手架往旧 `sessions` 插同 id 行兜底才绿。
- 按加固设计 §5.2：不改旧表（保留旧应用版本回滚能力），新建 `agent_artifact_read_states`，
  新 Agent API 只读写新表，不迁移旧水位。按 §5.6 给 `agent_artifact_versions` 增加
  `lineage_snapshot_json`（本次只建列，写入逻辑属后续任务 A5）。
- 顺带修复环境敏感用例 `test_real_tencent_adapter_uses_confirmed_model`：原硬编码断言
  `deepseek-v4-pro`，`backend/.env` 的 `TENCENT_PLAN_MODEL` 覆盖默认值后全量 pytest 开箱即红。

### 主要改动

- 迁移 `0028_agent_artifact_read_states`（新建）：`agent_artifact_read_states`
  （id/user_id/session_id FK→`agent_sessions.id` ON DELETE CASCADE/module/
  last_seen_sequence/updated_at，唯一 `uq_agent_artifact_read_states_user_session_module`）
  + `agent_artifact_versions.lineage_snapshot_json`（JSON, NULL）。旧表不动，downgrade 只删
  新表与新列。
- ORM：`AgentArtifactReadState` 改映射到新表（不再 extend_existing 旧表，移除对
  `app.artifacts.models` 的导入依赖）；`AgentArtifactVersion` 增加 `lineage_snapshot_json`
  映射（nullable）。
- `agent_artifacts/service.py::advance_read_state`：读写新表；删除旧表 `module_key` 兼容
  回填/双形状查询逻辑（随旧表废弃）；保留单调推进 max(old,new) 与并发首插 IntegrityError
  重读更新语义。路由无改动（会话归属校验本就走 agent_sessions）。
- 测试脚手架清理：删除 `tests/agent_artifacts/conftest.py` 的 `legacy_session_factory`、
  `test_api.py` 的 `_insert_legacy_session` 及全部调用点；已读测试改为只建 agent session；
  `test_read_state.py` 删除遗留 module_key 合并用例，新增「只写新表、旧表零写入」断言。
- `test_phase2_migrations.py` 新增 `test_0028_agent_artifact_read_states_schema`（唯一约束
  列序、FK 目标、新列 nullable、旧表保留不动）；`test_schema.py`、两个 test_models.py 注册表
  断言同步到新表名；`test_real_providers.py` 改为断言与当前 settings 一致。

### 验证

- TDD：先改测试确认 12 个失败（含 FK 1452 复现），实现后全绿。
- `pytest tests/agent_artifacts tests/test_phase2_migrations.py tests/test_schema.py -q` →
  220 passed；`pytest tests/agent_runtime -q` → 355 passed；`ruff check app tests` 通过。
- 迁移 up/down：0028→0027→0028 单步验证通过；0027→head（0028+0029）全链验证通过；
  `alembic heads` 单一 head。

### 遗留事项

1. **并行任务同 worktree 协作**：另一 A 任务（agent_runs.created_at 稳定排序）同期在同
   worktree 未提交作业，曾短暂产生双 0028 迁移双 head；对方已改为 `0029_agent_run_created_at`
   链接到本迁移之后（down_revision=0028）。`test_migration_chain_has_single_head` 的 head
   预期随对方 0029 落地需指向 0029（对方已在工作树改好；本提交内保持 0028 预期，保证本提交
   独立自洽）。
2. **测试库曾被并发迁移实验打坏并修复**：双 head 窗口内 reversibility 测试把
   `kol_insight_test` 降到 0022 且 0022 downgrade 因存量数据（analysis_reports 跨
   report_type 同 session+version）失败中断，0023 回填重跑撞唯一键。已手工修复：
   补回 `mcp_calls.goal_id` 列与 `uq_analysis_reports_session_type_version` 约束、
   stamp 0023（跳过重跑回填，与事故前数据状态一致）、按真实迁移跑 0024/0025、stamp 0026
   （列已存在）、upgrade head。唯一不可恢复损失：`mcp_calls.goal_id` 原列数据（测试库
   UAT 残留，不影响开发库/生产）。
3. `lineage_snapshot_json` 写入逻辑（发布时冻结 Evidence 传递闭包）属后续任务 A5，本次未实现。

## A7：删除旧 thinking 链路、runs 稳定排序与前端契约加固

- 背景与目标：v3 加固设计（`docs/superpowers/specs/2026-08-03-agent-runtime-v3-hardening-design.md`
  §5.8/§6.4）审核发现的四项问题——旧 thinking 路由残留致前端 404 重连风暴、会话恢复锚定
  随机 uuid 顺序、前端无 URL 协议白名单、未读水位跨会话不重置。
- 旧 thinking 链路整体删除：后端取消 `api/router.py` 的 `thinking_router` 注册（`rg` 确认
  `app.thinking` 无其他生产引用），删除 `app/thinking/` 整包与 `tests/thinking/`；前端删除
  `useSessionThinkingStream`、`api/sessionThinking.ts`、`state/sessionThinking.ts` 及各自测试，
  `ChatArea` 移除 ThinkingPanel 渲染与 merge/dedupe 逻辑（新运行时 thinking 只经 Run 卡
  `AgentThinking` 展示）。`tests/agent_runtime/test_legacy_routes_removed.py` 新增
  `/sessions/{id}/events` 404 断言与 OpenAPI 未注册断言。
- runs 稳定排序：`agent_runs` 表本无 `created_at` 列，新增迁移 `0029_agent_run_created_at`
  （回填 `COALESCE(started_at, NOW())`，链在 0028 之后）；会话详情 runs 改按
  `created_at` 升序 + id tie-break（该 hunk 已被并行的 2b61a0e 提交先带入分支）。
  前端 `useAgentWorkspace` 锚定改为：优先活动（queued/running/reviewing/paused）Run，
  否则取服务端顺序最后一个用户可见 Run。
- URL 白名单：新增 `artifacts/urlUtils.ts` 的 `safeHttpUrl`（仅 http/https，其余返回 null），
  KolDetailArtifactDialog / BrandArtifactView / CampaignArtifactView / InsightBoardView 四处
  统一改用，非法 URL 渲染为不可点受限态。
- 未读水位按会话隔离：`ArtifactWorkspace` 在 `sessionId` 变化时重置 `lastSeen` 与初始化
  ref（重置 effect 声明在初始化 effect 之前，同批次先重置再按新会话产物重建水位）。

### 验证

- TDD：排序/锚定/URL/水位用例先红后绿；thinking 路由未注册断言随删除转绿。
- 后端：`pytest tests/agent_runtime/test_legacy_routes_removed.py tests/agent_runtime/test_api.py -q`
  → 44 passed；`ruff check app tests` 通过；全量（除 integration）上一窗口 750 passed，
  唯一失败为并行任务窗口期的 head 断言，双方迁移落链后已恢复。
- 前端：`npm run test` → 322 passed（37 文件）；`npm run lint`（tsc）通过。

### 遗留事项

1. 本任务与 A 系列另一任务（渠道权限装配、agent_artifact_read_states）同 worktree 并行；
   双方迁移以 0028→0029 线性链接，`test_migration_chain_has_single_head` 预期值随 0029
   落地更新。测试库曾被对方迁移实验打坏并由对方修复；开发库迁移在并行窗口内未由本任务
   执行，切换前需按运行手册统一 `alembic upgrade head`。
2. `ThinkingPanel`/`TaskFlowNodes`（旧任务流）仍有相互引用，属另一条弃用链（ChatArea
   deprecated props 标注的后续任务），本次未动；`Message.thinking` 类型随之保留。

## Gate A 修复：MCP durable-before-send、熔断接线、禁自动重试与 reconciler 接线（A3）

### 背景与目标

- 依据 `docs/superpowers/specs/2026-08-03-agent-runtime-v3-hardening-design.md` §2.1/§5.3，
  修复审核发现的四个发布阻断缺陷（均属阻断条件 #1「重复执行/重复扣费/错误释放」）：
  1. `AgentMcpTool.execute` 把调用行、积分预留、`mark_running` 放在外层事务的 savepoint
     里，进程在「外发后、返回前」崩溃时整体回滚，恢复后模型可能重发同一调用；
  2. 旧服务级熔断（`circuit_scope="service"` 默认）与细粒度熔断两层叠加，且每个
     `AgentMcpTool` 实例自建空熔断器，细粒度熔断在生产永不触发；
  3. DataTap 传输层对 5xx/504（属 result_unknown）自动重发一次，违反「禁止自动重放」；
  4. `app.state.agent_tool_reconciler` 从未接线（admin confirm_success 永远取不回
     payload），且 reconcile 取回的 payload 未经输出 Schema 校验直接落 Evidence。

### 主要改动

- `backend/app/agent_runtime/tools/mcp.py`：新增 `DurableToolCallCoordinator`——
  `prepare()` 用独立 DB Session 单一事务完成 Run/Step 归属校验、logical_call_id
  锁定、调用行插入或复用、10 积分预留、`running`+`started_at` 落库，**commit 之后才
  允许外发**（预留与调用行同事务，消除悬挂预留窗口）；`finalize_success/release/
  unknown` 各自独立事务并带终态守护（已终态幂等跳过，不重复触碰钱包）；reconcile
  系列（`load_call/keep_unknown/confirm_success/confirm_failure`）同样独立事务即时
  提交。`AgentMcpTool.execute` 全部改走协调器；进程取消（`asyncio.CancelledError`）
  且请求可能已发送时按 `result_unknown` 收口后继续抛出。旧 `_db_transaction`
  savepoint 助手删除（无其他调用方）。`db_session` 构造参数保留兼容（A2 装配路径），
  durable 读写一律经 `session_factory`（默认真实 `SessionFactory`）。
- `backend/app/mcp_gateway/datatap.py`：新增构造参数 `retry_policy`
  （`"transient_once"` legacy / `"never"` 默认）。Agent 路径固定 `"never"`：
  504、5xx、协议中断、PossiblySentTimeout 都不自动重发（生产已无 legacy 消费方，
  默认直接收紧；legacy 策略可显式 opt-in）。
- `backend/app/mcp_gateway/service.py`：新增 `get_agent_mcp_transport()`（lru_cache，
  `circuit_scope="none"` + `retry_policy="never"`）。
- `backend/app/agent_runtime/tools/factory.py`（与 A2 签名协调的最小增量）：
  `AgentToolRegistryFactory` 新增可选 `breaker` 参数并传入每个 `AgentMcpTool`；
  默认 `transport_getter` 改为 `get_agent_mcp_transport`。
- `backend/app/main.py`：`create_agent_runtime` 创建进程级共享
  `FineGrainedCircuitBreaker`，engine 工具与恢复工具共用；`create_app` 接线
  `app.state.agent_tool_reconciler = get_agent_mcp_transport().reconcile_tool_call`
  （lifespan 与测试回退路径都设置）。
- `backend/app/agent_runtime/recovery.py`：`RecoveryLoop` 新增
  `migrate_stuck_tool_calls()`——超过 `stuck_seconds`（新配置
  `AGENT_TOOL_CALL_STUCK_SECONDS`，默认 900s，`started_at` 为准，NULL 按 stuck）
  仍处于 running/reserved 的调用，经 `SELECT ... FOR UPDATE` + 状态谓词守护迁移为
  unknown（保留预留，绝不直接释放或重发），`run_once` 同轮完成迁移 + 核对，返回值
  扩展为四元组。
- `backend/app/admin/service.py`：admin confirm_success 取回的 payload 经
  `resolve_allowlist_entry` + `validate_output` 校验通过才写 Evidence，否则退化为
  settle + `result_unavailable`（不伪造 Evidence）。
- 测试：`tests/agent_runtime/tools/test_mcp.py` 全部重写为真实提交语义（独立
  SessionFactory 已提交链路 + 显式清理）：durable-before-send 用独立连接在外发瞬间
  核对行+预留、外发后崩溃（BaseException 不调 finalize）行与预留可见且重入只回放、
  四类故障分类积分断言、跨实例共享熔断、reconcile 四结果 + 未过输出校验不落
  Evidence、取消收口 unknown；`tests/agent_runtime/test_recovery.py` 新增 stuck 迁移
  六个用例（含真实 SessionFactory 全链路恢复）；`tests/agent_runtime/
  test_runtime_wiring.py`（新）装配断言共享熔断器、`circuit_scope="none"` 与
  reconciler 接线；`tests/mcp_gateway/test_transport_policy.py` 按新契约重写重试
  用例（默认不重试 call_count==1，legacy opt-in 保留一次重试）；admin 测试新增非法
  payload 不落 Evidence；engine 测试经共享会话工厂适配协调器。

### 验证结果

- `cd backend && .venv/bin/pytest tests/agent_runtime tests/mcp_gateway tests/admin
  tests/billing -q` → **454 passed**。
- `cd backend && .venv/bin/pytest -q` → **793 passed / 9 skipped**。
- `cd backend && .venv/bin/ruff check app tests` → 通过。

### 遗留事项

- 恢复 transcript 重建（RunTranscriptLoader，§5.4）与租约心跳属后续任务，本次仅
  保证 stuck 调用不重发、不重复扣费、不错误释放。
- `tests/integration/test_agent_runtime_real.py`（真实 UAT，默认跳过）仍用
  `get_mcp_transport()` legacy 传输，切档真实 UAT 前需切到 `get_agent_mcp_transport()`。

## Gate A 修复：Run 接管重建上下文、reviewing 恢复、租约心跳、取消语义与 resume 互斥（A4）

### 背景与目标

- 依据 `docs/superpowers/specs/2026-08-03-agent-runtime-v3-hardening-design.md` §5.4/§5.5，
  修复审核发现的五个恢复/并发缺陷：
  1. 接管不重建 Run 内对话上下文（executor `_build_messages` 只注入用户输入），恢复后模型
     看不到本 Run 已完成的工具调用与结果，必然重复调用已 settled 的 MCP 工具 → 重复扣费；
  2. reviewing 状态崩溃不可恢复（可领取扫描只认 queued/running、`claim_lease` 拒绝非
     running）→ Reviewer 复核期间崩溃 Run 永久卡死；
  3. 租约只在决策循环顶续租（300s），单次长迭代（decide/MCP/Reviewer）超时即被误判死亡
     → 双 worker 并发窗口或 Run 以 `run_lease_lost` 误失败；
  4. 取消路径断裂（生产无人调用 `request_cancel`，API 直接 `cancel` 置终态）→
     `run.cancelled` 事件从不发出，decide 期间到达的取消不阻止随后的工具外发与计费；
  5. paused Run resume 不检查同 Session 其他活动主 Run → 双主 Run 并发。

### 主要改动

- `backend/app/agent_runtime/transcript.py`（新建）：`RunTranscriptLoader` 从触发消息 +
  本 Run 完整 Step 重建模型上下文。已完成 tool_call Step 回放持久结果（settled 回放
  `evidence_id + 结构化预览`，与正常工具返回形态一致，不回灌 raw payload；failed/unknown
  回放原结构化错误）；崩溃残留的 running Step 按 `agent_tool_calls` 行当前状态构造结果
  回放（settled→success、failed→原错误、其余→unknown 待核对），并作为 `resume_step`
  交给引擎：模型重新发起相同调用（同工具同参数）时复用该 Step——step_id 不变 →
  `logical_call_id` 不变 → A3 协调器幂等回放，绝不重发、不重复扣费（防重不依赖模型记忆）。
- `backend/app/agent_runtime/heartbeat.py`（新建）：`RunLeaseHeartbeat` 用独立 DB Session
  每 `lease_seconds/3` 续租一次，覆盖 decide/MCP/Reviewer 长调用全程；renew 明确失败
  （租约被接管）置 `lost`，瞬时异常（DB 抖动）记日志下轮重试不判死。引擎在 decide 返回后
  的闸门与发布 Artifact 前再次确认租约持有：丢失则安静退出——不发布、不写终态、不落
  assistant 消息，交还接管方。
- `backend/app/agent_runtime/engine.py`：`run()` 接入心跳（finally 停止）与
  `resume_step`/`resumed_by` 参数；新增 reviewing 入口分支（`_resume_reviewing`：读取既有
  Review Batch/Item/Attempt 与当前 Draft Revision，已 approve 的 Item 由 `review_pending`
  自动跳过重审，pending/revise 继续）；`_handle_submit_review` 拆出 `_settle_review`
  （submit_review 与 reviewing 接管共用），全 approve 时 `results` 为空（重复接管）不发
  重复 `review.approved`、发布前 `holds_lease` 再确认；新增 decide 后统一闸门
  `_post_decision_gate`（取消→不落任何 assistant 产物直接收口，租约丢失→安静退出，覆盖
  minor b）；`_settle_cancelled` 统一取消收口（释放本 Run 持有的 Draft 为 idle + 幂等
  `cancel` + 恰好一个 `run.cancelled` 事件），循环顶/decide 后/外发前/Reviewer 返回后
  四个检查点全部接入；`run.resumed` 事件 payload 增加 `resumed_by: "user"|"system"`
  （minor a：API resume 留下的 NULL 租约领取归因 user，过期租约/ reviewing 接管归因
  system）。
- `backend/app/agent_runtime/executor.py`：`_build_messages` 删除，改经
  `RunTranscriptLoader` 构建初始 messages + resume_step；`_find_claimable_id` 与
  `_claim_and_prepare` 纳入租约过期/无租约的 reviewing Run（沿用 open Attempt，保持
  reviewing；无 open Attempt 属不变量破坏，记日志跳过）；`_claim_and_prepare` 返回
  `resumed_by` 归因。
- `backend/app/agent_runtime/recovery.py`：`reclaim_expired_runs` 扫描纳入租约过期的
  reviewing Run。
- `backend/app/agent_runtime/repository.py`：`claim_lease` 允许 running/reviewing
  （reviewing 接管保持状态不拉回 running）；`_owns_active_lease` 公开为
  `owns_active_lease` 并新增 `holds_lease`（发布/终态写入前再确认）。
- `backend/app/agent_runtime/router.py`：`POST /runs/{id}/cancel` 分流——
  queued/paused/clarification 立即 `cancel` 并写 `run.cancelled` 终态事件（终态幂等）；
  running/reviewing 只 `request_cancel`，由 Engine 在安全点收口。`POST /runs/{id}/resume`
  锁 Session 行后检查同 Session 其他活动 `session_analyst_v1` Run，存在则 409
  `active_run_in_progress`（与 messages 并发车道同一语义）。
- `backend/app/main.py`：engine_factory 传 `session_factory=SessionFactory`（生产心跳
  独立会话真实提交；测试缺省共享会话包装）。
- 测试（TDD 先红后绿）：新增 `test_transcript.py`（12 例：回放形态/raw payload 不泄漏/
  崩溃残留三类/触发消息回退）、`test_heartbeat.py`（4 例：续租/lost/瞬时异常不判死/幂等
  启停）、`test_takeover.py`（14 例：settle 后、外发后、settle 前 Step 未更新、decide 后
  四个崩溃注入点的接管不重复外发不重复扣费、resumed_by user/system、reviewing 接管跳过
  已 approve、重复接管幂等无重复 Version/事件、租约未过期不接管、长 decide 心跳覆盖、
  接管后旧 worker 不写终态、发布前租约丢失不发布、decide 后取消不落消息×2、Reviewer
  返回后取消释放 Draft 收口）；`test_api.py` 新增取消分流 4 例 + resume 互斥 1 例；
  `test_repository.py` reviewing 接管语义改断言；`test_engine.py` 两个取消用例随闸门前移
  更新断言（取消拦截点从「step 创建后外发前」前移到「decide 后分发前」，`cancelled_not_sent`
  Step 不再产生，外发前核对保留为 step 创建后窗口的兜底）。

### 验证结果

- `cd backend && .venv/bin/pytest tests/agent_runtime -q` → **400 passed**。
- `cd backend && .venv/bin/pytest -q` → **825 passed / 9 skipped**（中途一次全量出现真实
  provider 集成用例 `test_real_tencent_adapter_recovers_from_json_schema_incompatibility`
  瞬态失败，隔离与复跑均过，与本改动无关）。
- `cd backend && .venv/bin/ruff check app tests` → 通过。

### 遗留事项

1. `_settle_cancelled` 的 Draft 释放只覆盖 cancelled 出口；§5.7 要求的 ask_user/complete/
   paused/failed 等其余非发布出口释放 Draft owner 属后续任务（A5 Draft/Batch 生命周期）。
2. decide 后闸门到 `transition` 之间仍存在理论上的极窄租约丢失窗口：此时 `transition` 抛
   `run_lease_not_held` 冒泡到 executor 的 `_finalize_failed`，`force_fail` 因他人持有活跃
   租约返回 False，旧 worker 不写终态——行为正确但依赖异常路径，未做显式闸门。
3. reviewing 接管后 `run.resumed` 事件 payload 带 `reviewing: true` 标记；前端对未知
   payload 键走 default 忽略，无需改动。
4. `tests/integration/test_agent_runtime_real.py`（真实 UAT，默认跳过）尚未覆盖恢复/取消
   场景；Gate A 真实 UAT（设计 §8.4 第 9 项「取消、进程重启和恢复」）属后续切档任务。
