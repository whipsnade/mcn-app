# 2026-08-21

## 背景与目标

在隔离的 `codex/pi-autonomous-marketing-skills-implementation` worktree 中开始执行已确认的 Pi 自主营销 Skill 实施计划。第一阶段建立数据库 Revision/Activation 注册表，确保生产 Skill 内容来自不可变快照，而不是 Git 工作树或本机目录。

## 主要改动

- 新增 `backend/app/marketing_skills/`：Skill Revision/Activation ORM、规范化 digest、严格 frontmatter 与安全边界校验、稳定租户灰度分桶及激活解析。
- 将 Skill Revision 的 update/delete 在 ORM 层 fail-closed，Activation 仅保存可切换指针。
- 新增 `backend/migrations/versions/0045_marketing_skill_registry.py`，建立两张新表并写入 marketing-v2 基线 Revision 与 production 全局激活指针；不修改或删除历史迁移。
- 注册新模型到 `backend/app/db/models.py`。

## 验证结果

- Task 1 RED 阶段先确认模块不存在导致收集失败。
- GREEN 阶段：`tests/marketing_skills/test_validation.py`、`test_repository.py`、`test_migration_0045.py` 共 13 个定向测试通过。
- `ruff check app/marketing_skills app/db/models.py tests/marketing_skills` 通过。

## 遗留事项

- 0045 的实际 Alembic 升级/回滚验证将在不触碰生产库的隔离迁移检查中完成。
- 后续阶段需要把新 Revision manifest 接入 Run Snapshot、Pi Gateway 原生加载、通用报告与管理端；本阶段未改变现有模型/MCP 外发路径。

## Task 2：管理服务与 API

### 背景与目标

为管理员提供 Skill Revision 的校验、查看、数据库 Diff、激活、灰度和回滚入口，同时复用既有持久化幂等记录与审计日志。

### 主要改动

- 新增 `backend/app/marketing_skills/schemas.py`、`service.py`、`router.py`，挂载 `/api/v1/admin/skills`。
- 所有管理写操作都要求 `Idempotency-Key`；同 key 同请求回放安全 DTO，不同请求返回冲突；Revision 创建、激活和回滚均写 `admin_audit_logs`。
- 发布前只使用数据库审核工具目录和本地轻量校验，不调用模型、DataTap 或 Corpus；Diff 只读取数据库不可变 Revision 内容。

### 验证结果

- Task 2 定向 RED 先确认 schema/router 缺失；实现后营销 Skill 测试共 19 个通过。
- `ruff check app/marketing_skills app/api/router.py tests/marketing_skills` 通过。

### 遗留事项

- 管理 UI 和 Run Snapshot 尚未接入；当前 API 仍只管理 Revision/Activation 指针，不改变运行中 Run 的 Skill 来源。

## Task 3：Run Skill Snapshot 与恢复不变性

### 背景与目标

让新 Pi Run 在创建时解析数据库 Activation 并冻结 Skill manifest；existing Run、child Run、resume/recovery 只验证并复用已持久化快照，不重新读取当前激活指针。

### 主要改动

- 新增 `backend/app/marketing_skills/snapshot.py`，提供冻结的 `SkillManifestEntry/SkillManifest`、manifest digest 校验和 DB Revision → `MarketingRunCapability` 解析。
- `RuntimeConfigSnapshot` 增加可选 `skill_manifest`，新 Pi Run 注入数据库 Revision；动态内容不改变 Root Policy、Capability Pack 版本或工具审核目录。
- child snapshot 从父快照复制 capability/manifest；existing snapshot 只做结构、digest、租户与配置版本校验。
- `load_marketing_skill` 保留历史兼容路径，同时优先读取 Run manifest，审计记录增加 revision。
- 修正 0045 基线的标准契约映射（`insight_board_v1`、`strategy_advice_v1`）并把基线时间写为真实 UTC `datetime`，避免 MySQL 将 `now()` 表达式当作字符串。

### 验证结果

- Snapshot/Runtime wiring 定向测试 4 个通过；RuntimeConfig schema/backend snapshot 回归 6 个通过；RuntimeConfigService 回归 8 个通过；Pi 内部 Skill 工具回归 7 个通过。
- `ruff check` 覆盖 Task 3 受影响模块通过。
- `kol_insight_test` 已完成 0045 downgrade→upgrade 循环，基线 Revision 可重建，未触碰旧业务表。

### 遗留事项

- Task 4 已补齐 Pi Gateway 对 `skill_manifest` 的校验、原子物化与显式加载；真实模型、DataTap、钱包、生产数据库和 Web UAT 仍按计划不执行。

## Task 4：Pi Gateway 原生 Skill 目录物化与显式加载

### 背景与目标

把后端签名并校验过的 Run Skill manifest 变成当前 worker 专属的原生 `SKILL.md` 目录，关闭 Pi SDK 的用户级、项目级和 cwd 自动发现，确保坏快照在模型或 MCP 外发前稳定失败。

### 主要改动

- 新增 `pi-gateway/src/skill-snapshot.ts`：校验名称、revision、内容 digest、大小、重复项和未知文件；在 `.skill-snapshot.tmp-*` 中以 0700/0600 权限写入，fsync 后原子 rename 到当前 Run 的 `.skill-snapshot`，拒绝 symlink、路径穿越和重复物化。
- 扩展 Gateway runtime snapshot 协议和 claim 映射：逐项核对 manifest 与 capability pack 的 revision、digest、正文、required tools、artifact contract，并在 worker spawn 前拒绝内容漂移、超限或 manifest digest 错误。
- `createProductionResourceLoader` 固定 `noSkills/noContextFiles/noPromptTemplates/noThemes`，只允许当前 Run 根目录下的显式快照路径；Root Policy 仍由受控 system prompt 注入，旧 `load_marketing_skill` 兼容路径未删除。
- `createProductionPiSession` 将快照物化置于 MCP 配置、readiness、模型 registry 与 SDK session 创建之前；失败时不会启动 readiness、模型或 MCP。

### 验证结果

- RED 阶段先确认 `skill-snapshot` 模块不存在；GREEN 阶段定向 Vitest：5 个文件、39 个测试通过。
- `npm run typecheck` 通过。
- 新增覆盖：显式快照可加载、自动发现关闭、权限 0700/0600、digest/路径/symlink/未知文件失败、capability 元数据漂移失败、快照损坏时 session 启动前失败。

### 遗留事项

- 管理 UI、正式 Run 行为和生产 Skill 文案迁移尚未开始；`analysis_report_v1` 与 `workbook_v1` 已完成后端基础链路。真实模型、DataTap、钱包、生产数据库和 Web UAT 仍按计划不执行。

## Task 5：analysis_report_v1 强类型模型输入、发布与能力注册

### 背景与目标

为非固定品牌/活动/达人模板的长尾营销需求提供通用 Report Version，同时保持标准 Artifact 的强类型校验、服务器字段所有权和数据受限披露语义不变。

### 主要改动

- 新增 `backend/app/agent_artifacts/payloads/analysis_report.py`：定义 metric cards、typed table、time series、link list、chart、narrative、methodology/limitations 七类 Block，严格列类型、稳定 block id、fulfillment 数量语义、URL/公式/敏感内容拒绝和可选 workbook 布局引用。
- 新增 `backend/app/agent_artifacts/model_inputs/analysis_report.py`：模型只提交业务字段；服务器确定性补齐 `schema_version=analysis_report_v1`、`module=report`、`data_status`、fulfillment availability 与自动 limitation，不设置 Top20/Top40 业务行数门禁。
- 将 `analysis_report_v1` 接入 payload/input 单一映射、`report` Artifact key/business identity、Direct Builder、Profile allowlist 和 `marketing-v2` capability pack；新增通用 Report contract 与 `analysis-report`/`workbook-export` Skill。0045 已包含这两个基线 Revision/Activation，本阶段没有修改历史迁移。
- 增加通用 Report/Workbook 的配置化技术上限字段，供 exporter 分页/拆 Sheet 使用；标准 brand/campaign/insight payload 与现有 capability loader 保持兼容。

### 验证结果

- RED 阶段确认新 input 模块缺失导致 pytest collection 失败。
- 第5阶段定向回归：报告/发布/Direct Builder/Profile/Pack 与既有 payload/input 共 218 个测试通过；受 capability allowlist 影响的 RuntimeConfig/Skill Snapshot/内部工具 21 个测试通过。
- 受影响 Python 模块与测试 Ruff 全部通过；未执行真实模型、DataTap、钱包、生产数据库或 Web UAT。

### 遗留事项

- 正式 Run 行为、管理 UI 与生产 Skill 文案迁移尚待后续任务；Workbook 导出只接受已发布 Report Version，真实模型、DataTap、钱包、生产数据库和 Web UAT 仍按计划不执行。

## Task 6：workbook_v1 同版确定性 Excel 与缓存

### 背景与目标

把 `analysis_report_v1` 的不可变 Report Version 安全投影为 `workbook_v1`，支持自定义列、跨平台统一表头、冻结/筛选/排序、分页和拆 Sheet；Skill 热更新或布局变化不能污染历史导出缓存。

### 主要改动

- 新增 `backend/app/agent_artifacts/exporters/workbook.py` 与 `analysis_report.py`：使用 openpyxl 纯值渲染，规范化 ZIP 时间与属性保证相同 Report/Exporter/Layout 输出稳定；链接仅允许 http/https，公式样式文本统一转义，不接受宏、脚本或二进制输入。
- `WorkbookLimits` 只表达 Sheet、行、列、单元格和文件大小技术上限；业务行数超限通过单 Sheet 分页/拆 Sheet 保留全部真实数据，不能静默截断；超出可用 Sheet 数返回 `workbook_technical_limit_exceeded`。
- `ExportCacheService` 为 `analysis_report_v1` 使用 `SHA256(version_id + exporter_version + layout_digest)` 完整缓存身份，标准 brand/campaign/kol 导出继续使用历史 `schema_version` 键；0046 将缓存字段扩展到可容纳完整 digest。
- 导出 API 继续只读取已发布不可变 Version；技术上限错误稳定映射为 409，模型/MCP 不进入表现层。

### 验证结果

- RED 阶段确认泛型 exporter、布局 digest 与缓存键接口不存在导致 pytest collection 失败。
- Task 6 定向测试：`test_analysis_report_export.py` 与 `test_export_cache.py` 共 28 个通过；标准 `test_brand_export.py` 12 个通过。
- 受影响 exporter/cache/payload/model/migration/test 模块 Ruff 全部通过；隔离测试库已从 0045 升级至 0046。

### 遗留事项

- 正式 Run 行为、管理 UI 与生产 Skill 文案迁移尚待后续任务；未执行真实模型、DataTap、钱包、生产数据库、部署或 Web UAT。

## Task 7：Pi 正式 Run 澄清、自主错误降级与主报告终态

### 背景与目标

把 `analysis_report_v1` 接入正式 Pi Run 的通用完成语义：普通用户可见 Run 必须拥有当前 Run 发布的顶层主 Artifact，但不绑定固定报告类型；澄清终态、utility 文本 Run 和 `result_unknown` 的既有安全语义保持独立。

### 主要改动

- `CompletionValidator` 现在在 terminal、ACK 丢失恢复和 force-complete 共用路径上要求当前 Run 的已发布顶层 Version（`parent_artifact_id IS NULL`）；历史 Run、其他 Session 或 child insight 不满足条件，缺失稳定返回 `pi_gateway_main_artifact_missing`。
- `clarification_requested` 与 `utility_*` Run 保持无报告文本出口；标准 Artifact 与 `analysis_report_v1` 都只要在冻结 allowlist 内且发布链/lineage 有效即可通过，不增加固定 contract、工具顺序或调用次数门禁。
- Pi Gateway terminal 错误白名单、Engine/Builder/内部上下文契约说明同步更新；UAT 拓扑新增当前 Run 顶层主报告契约查询辅助。
- 新增离线行为测试：首次 MCP `result_unknown` 后不同参数可继续外发，旧 logical call 不 replay；长尾明细使用通用 `analysis_report_v1 → workbook_v1` 投影。既有 assistant-only 兼容测试显式改为 utility fixture，避免掩盖新业务门禁。

### 验证结果

- RED：主报告缺失、历史产物和 child insight 三个新增断言失败；受限沙箱首次因无可写临时目录未进入 pytest 收集，提升权限后确认 RED。
- GREEN：Task 7 定向组 `20 passed in 0.23s`；受影响 terminal gate/direct builder 回归 `13 passed in 0.39s`；变更文件 `ruff check` 与 `git diff --check` 通过。
- 未执行真实模型、DataTap、钱包、生产数据库、部署、Corpus Replay、Stage 2A/2B、60-observation 或稳定性验证。

### 遗留事项

- 管理端 Skill 工作台、前端通用报告展示/Excel 入口及 Native Skill/Report 迁移文档仍待后续任务；Task 7 不改变 MCP 直通、Evidence、计费或 unknown 恢复实现。

## Task 8：管理端营销 Skill 工作台

### 背景与目标

为不可变 Skill Revision/Activation API 提供可审计的管理员工作台：查看 Revision 与激活状态、编辑并校验 Markdown、读取数据库 Diff、按租户灰度/全量激活，以及通过确认对话框回滚上一 Revision。

### 主要改动

- 新增 `src/api/skills.ts` 与 `src/api/contracts.ts` 管理端契约，覆盖列表、详情、Revision、校验、Diff、激活和回滚；所有写操作自动携带或透传 `Idempotency-Key`，Skill 名称按 URL 编码。
- 新增 `src/components/admin/SkillAdmin.tsx`，展示 Revision 创建人、激活更新人、环境/租户/灰度比例、digest、结构化校验错误与只读 Diff；编辑器只创建新 Revision，不改写历史内容。
- 使用既有 `ConfirmDialog` 完成回滚确认和焦点/键盘可访问性，不使用 `window.confirm` 或 `window.prompt`；激活和回滚状态沿用数据库事实源，运行中 Run 的 snapshot 不在 UI 中被改写。
- 将 `营销 Skills` 加入 `AdminNavigation` 与 `AdminPanel`，并补充 API、工作台和导航回归测试。

### 验证结果

- RED：新增 API/UI 测试先因模块不存在而收集失败；UI 断言修正异步详情加载、跨节点文本匹配和租户激活刷新后的 fixture 后通过。
- GREEN：`npm test -- --run src/api/skills.test.ts src/components/admin/SkillAdmin.test.tsx`：42 个文件、313 个测试通过。
- `npm run lint` 已确认本任务新增前端类型错误清零；命令仍被仓库既有 `pi-gateway/src/main.ts` 类型错误及 `pi-runtime` 缺失外部模块阻断，未修改这些无关问题。
- 未执行真实模型、DataTap、钱包、生产数据库、部署、Corpus Replay、Stage 2A/2B、60-observation 或稳定性验证。

### 遗留事项

- Task 9 继续处理正式运行时/Native Skill 文案与通用 Report 表现层衔接；本 Task 只提供管理端 Skill 控制面，不改变模型、MCP、计费或 Run snapshot 的后端语义。

## Task 9：通用报告 API 类型、BI 展示与 Excel 入口

### 背景与目标

让 `analysis_report_v1` 作为不绑定固定业务模板的通用 Report Version 在前端可安全收窄、展示和导出；保留全部真实长尾行，明确数据受限语义，并让 Excel 导出与当前查看的不可变 Version 使用同一身份。

### 主要改动

- 扩展 `src/api/agentArtifacts.ts` 判别联合，镜像通用报告的 metric cards、typed table、time series、link list、chart、narrative、methodology/limitations、fulfillment 与 workbook layout DTO；`isAgentArtifactPayload`/`getAgentArtifactPayload` 接受 `analysis_report_v1`。
- 新增 `src/components/artifacts/AnalysisReportView.tsx`：按 block 类型安全渲染，typed table 不做 Top20/Top40 截断，所有 null/受限值显示「数据受限」，未知未来 block 进入受控降级；链接统一经过 http/https 白名单。
- `ArtifactWorkspace` 新增「通用报告」一级 Tab，按 `report` 模块选择最新非生成中的已发布 `analysis_report_v1`，复用版本选择、未读水位和 `exportArtifact`；`analysis_report_v1` 加入 Excel 导出类型。
- 收紧 `safeHttpUrl` 输入处理，拒绝非字符串、控制字符和非 http/https 协议；新增前端测试覆盖跨平台列、链接、45 行长尾表、受限数据、同版导出和未知 block。

### 验证结果

- RED：API 判别联合、通用报告视图模块和 Workspace Tab 缺失时按预期失败。
- GREEN：`npm test -- --run src/api/agentArtifacts.test.ts src/components/artifacts/AnalysisReportView.test.tsx src/components/artifacts/ArtifactWorkspace.test.tsx`：43 个文件、316 个测试通过。
- `npm run lint` 已确认 Task 9 新增类型错误清零；仍被既有 `pi-gateway/src/main.ts` 类型错误及 `pi-runtime` 缺失外部模块阻断。
- 未执行真实模型、DataTap、钱包、生产数据库、部署、Corpus Replay、Stage 2A/2B、60-observation 或稳定性验证。

### 遗留事项

- Task 10 继续更新 Native Skill/Report 迁移文案、runbook 和 QA 记录；通用报告视图不改变后端发布、lineage、计费或 Run snapshot 语义。

## Task 10：Native Skill/Report 迁移边界与离线 QA 记录

### 背景与目标

把六个营销 Skill 文案和 Pi 内部工具边界迁移到模型主导的 Native 路径：正常运行只依赖当前
Run Snapshot、Tool Contract 和 Root Policy；旧 POC 工具仅作为兼容入口保留；通用报告与同版
workbook 的契约、安全限制和未执行的真实验证边界必须可审计。

### 主要改动

- 重写六个 `pi-runtime/skills/*/SKILL.md`，明确模型自主决策、直接标准 MCP Result、Evidence
  可追溯、partial/限制披露、结构化校验反馈、`analysis_report_v1`/`workbook_v1`，移除固定顺序、
  固定规模、固定权重和旧 Builder/发布工具的生产必经语义。
- 在 `pi-runtime/src/extensions/internal-tools.ts` 增加 `compatibility` 明示标识；保留
  `load_marketing_skill` 供迁移期 POC/兼容测试，补充测试防止它被误解为 Native 正常路径。
- 更新 `docs/runbooks/pi-agent-gateway.md`，补充快照物化、路径隔离、digest fail-closed、direct
  MCP、unknown、Artifact 发布链路与通用 Report/Workbook 边界；新增离线 QA 覆盖矩阵和外部动作清单。

### 验证结果

- RED：新增 Native Skill 语义断言先因旧文案缺少模型主导、Snapshot、Tool Contract 和泛型报告
  约束而失败；内部工具测试因当前 worktree 缺少 `pi-runtime/node_modules/typebox` 无法收集。
- GREEN：`npm --prefix pi-runtime test -- --run tests/skills.test.ts`：1 个文件、6 个测试通过。
- `git diff --check` 通过；Skill 文案静态扫描未发现 Evidence Bridge、`mcp_result_v1`、旧 Builder/发布
  必经名或固定规模/权重文字；secret/DSN/Bearer 模式扫描无敏感值。
- 未安装依赖，未执行真实模型、DataTap、钱包、生产数据库、部署、Web UAT、Corpus Replay、Stage
  2A/2B、60-observation 或稳定性验证。

### 遗留事项

- 依赖补齐后再运行 `tests/internal-tools.test.ts` 与 Pi Runtime typecheck；Task 11 继续做设计覆盖
  自审、独立边界审查和本地 integration candidate 验证，真实外部验证仍需单独授权。

## Task 11：设计覆盖自审、独立审查与受影响问题修复

### 背景与目标

完成设计 §2–§15 的逐节覆盖审查，核对模型主导边界、报告/Workbook 安全、Revision scope 并发唯一性、
租户 Diff 归属和最终候选验证；遵守用户要求不重复仓库全量测试或真实外部动作。

### 主要改动

- 通用 Report 前端新增顶层方法论卡片；`workbook_v1` 首个 Sheet 追加可信元数据区，保留数据状态、
  数据截至、来源、方法说明、限制和 fulfillment，不再只依赖显式 methodology block。
- Skill Revision/Activation 增加非空 `scope_key`，用 `__global__` 表达全局 scope；新增迁移
  `0047_marketing_skill_scope_uniqueness` 回填现有数据并把唯一约束切换到 scope key，避免 MySQL
  可空 `tenant_id` 允许重复全局记录。
- Diff 服务、后端路由、前端 API 与管理 UI 支持 tenant scope；补齐对应后端/前端 RED 测试。
- QA 文档补录 §2–§15 自审、独立审查结果、Important/Minor 修复和未执行外部动作边界。

### 验证结果

- RED：前端元数据断言按预期失败；导出原始行为四项元数据均缺失；内存 SQLite 复现可空全局
  Revision 重复、Diff 方法不接受 tenant scope。后端 pytest 初始环境缺少 worktree venv/`pytest_asyncio`，
  后改用仓库已有离线依赖环境完成验证，未安装新依赖。
- GREEN：受影响后端 `test_service.py`、`test_repository.py`、`test_snapshot.py`、
  `test_analysis_report_export.py` 共 18 项通过；前端 Skills API、SkillAdmin、AnalysisReportView
  共 10 项通过；受影响后端 Ruff 全部通过。
- 独立只读审查：Critical 0；2 个 Important 与 1 个 Minor 均已修复并经受影响回归确认。
- `git diff --check`、`git show --check` 与 secret/DSN/Bearer/旧桥接静态扫描通过；未执行真实模型、
  DataTap、钱包、生产库、部署、Web UAT、Corpus Replay、Stage 2A/2B、60-observation 或稳定性循环。

### 遗留事项

- 仍需创建不移动 `main` 引用的本地 integration candidate；`pi-runtime` 的 `typebox` 等依赖缺失
  仍阻断内部工具套件/typecheck，按用户要求不擅自安装或重跑全量。
