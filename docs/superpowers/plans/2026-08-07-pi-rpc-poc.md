# Pi RPC POC Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在独立数据库中用 Pi RPC 直接调用 DataTap，并与当前 Runtime 使用同一模型完成六类对比验收，证明 Pi 可自主研究且能复用既有 Evidence、Builder、Artifact、Excel 与 BI 契约。

**Architecture:** Python POC runner 为每个 Run 启动一个无状态 Pi RPC 子进程；本地 TypeScript Extension 直接连接 DataTap MCP，并通过带单 Run 临时令牌的内部 HTTP 端点透明写入 Evidence、调用既有内部工具与发布服务。Pi 路径不接钱包；Current 基线路径保持原生产计费代码并只使用 POC 独立库中的一次性测试钱包。POC 不开发新的积分能力、License、管理端、正式前端或并发，所有数据只进入 `kol_insight_pi_poc`。

**Tech Stack:** Python 3.11/3.12、FastAPI、SQLAlchemy Async、MySQL 8、Node.js 20+、TypeScript、当前官方 Pi coding-agent、MCP TypeScript SDK、pytest、Vitest。

## Global Constraints

- 设计依据：`docs/superpowers/specs/2026-08-07-pi-agent-runtime-integration-design.md`。
- 仅执行方案 A；不得实现生产 Gateway、管理端、License、积分或方案 C。
- 当前 Runtime 与 Pi 必须使用完全相同的 provider、model、thinking level、问题、历史和日期窗口。
- 数据库必须精确为 `kol_insight_pi_poc`；检测到其他数据库名时立即拒绝运行。
- Pi 必须使用 `--mode rpc --no-session --no-builtin-tools --no-context-files`，关闭自动发现并只显式加载已审核 Extension/Skills。
- Pi 直接调用 DataTap；Extension 不得修改工具名、参数、原始结果或错误，也不得自动重试、拆分或切换工具。
- DataTap token、模型 key 和内部 Run token 不得写入 Prompt、Skill、事件、Artifact、fixture 或输出文件。
- 积分不属于 POC 评价指标，禁止读写真实用户钱包。Pi 路径不创建钱包，Pi 直连 MCP 产生的
  `AgentToolCall.points_reserved/points_settled` 恒为 0；Current 路径不得绕过既有计费，
  必须在 `kol_insight_pi_poc` 中使用一次性测试钱包执行真实 reserve/settle/release。
- 每个 runtime/case 使用独立用户；两侧写入相同的默认渠道权限。仅 Current 用户创建余额
  10,000 的 POC 测试钱包，保证余额不成为研究路径变量；其流水仅作真实性审计，不参与 Gate。
- 每个 Pi Run 最多 30 分钟、50 次模型决策；POC 并发固定为 1。
- 六类强类型产物必须继续使用既有 Builder 与 `ArtifactPublicationService`，不允许 Pi 手写 payload。
- 每项任务严格 TDD；只暂存本任务文件，不能把工作树中既有未提交文件混入 commit。
- Pi 包近期从 `badlogic/pi-mono`/旧 npm scope 迁移到 `earendil-works/pi`；Task 1 必须以官方当前发行版实测并锁版本，不得直接使用浮动 `latest`。

---

## File Structure

```text
pi-runtime/
  package.json                         # 独立 Node workspace 与锁定依赖
  package-lock.json
  tsconfig.json
  src/rpc/protocol.ts                  # 严格 LF JSONL 类型与解析
  src/extensions/poc-runtime.ts        # Extension 组合入口
  src/extensions/datatap-mcp.ts        # DataTap 直连、透明 Hook
  src/extensions/internal-tools.ts     # FastAPI 内部工具适配
  src/http/client.ts                   # 带单 Run token 的最小 HTTP client
  src/redaction.ts                     # 输出前密钥脱敏
  tests/*.test.ts
  skills/<name>/SKILL.md
  skills/brand-research-report/references/chatgpt-datatap-success.md
backend/app/pi_runtime_poc/
  __init__.py
  auth.py                              # 单 Run 临时 token 验证
  schemas.py                           # POC 内部 API 严格契约
  service.py                           # Step/ToolCall/Evidence 与内部工具桥
  router.py                            # 仅 POC 启用的 internal routes
  rpc.py                               # Python Pi RPC 子进程 client
  runner.py                            # 单 Run 生命周期、事件映射与收口
  comparison.py                        # current/pi 对比指标
backend/scripts/run_pi_runtime_poc.py   # CLI 入口
backend/tests/pi_runtime_poc/*.py
backend/tests/integration/test_pi_runtime_poc_real.py
backend/scripts/run_pi_runtime_poc.sh   # 强制独立库的真实服务入口
backend/fixtures/pi_runtime_poc/cases.json
docs/qa/pi-runtime-poc-rounds.md        # 每轮追加记录
outputs/pi-runtime-poc/.gitkeep
```

POC 不新增数据库表；复用当前 `AgentSession/Message/Run/Attempt/Step/ToolCall/Event`、
`EvidenceItem`、Artifact Draft/Version 表。Pi 特有版本、Skill digest 和对比 case id 写入
`AgentRun.prompt_snapshot_json["pi_runtime_poc"]`，避免 POC schema 污染生产迁移。

---

### Task 1: 锁定 Pi 发行版并验证最小 RPC/Extension 契约

**Files:**
- Create: `pi-runtime/package.json`
- Create: `pi-runtime/package-lock.json`
- Create: `pi-runtime/tsconfig.json`
- Create: `pi-runtime/src/rpc/protocol.ts`
- Create: `pi-runtime/tests/rpc-probe.test.ts`
- Create: `docs/qa/pi-runtime-version-probe.md`

**Interfaces:**
- Consumes: 官方 Pi RPC、Extension、Skills 和 Tool flags 文档。
- Produces: 锁定的 Pi CLI 包版本；`parseRpcLine(line: string): PiRpcRecord`；实测事件样本和命令行能力记录。

- [ ] **Step 1: 写严格 JSONL 解析的失败测试**

```ts
import { describe, expect, it } from "vitest";
import { parseRpcLine } from "../src/rpc/protocol";

describe("parseRpcLine", () => {
  it("只接受单条 LF JSON 记录并保留字符串内 U+2028", () => {
    const record = parseRpcLine('{"type":"message_update","text":"a b"}\r');
    expect(record.type).toBe("message_update");
    expect(record.text).toBe("a b");
  });

  it("拒绝空行和非对象 JSON", () => {
    expect(() => parseRpcLine(" ")).toThrow("empty_rpc_record");
    expect(() => parseRpcLine("[]")).toThrow("invalid_rpc_record");
  });
});
```

- [ ] **Step 2: 运行测试确认红灯**

Run: `cd pi-runtime && npm test -- rpc-probe.test.ts`

Expected: FAIL，模块尚不存在。

- [ ] **Step 3: 初始化独立 Node workspace 并锁定官方包**

先检查官方当前 README/package 名和 `npm view <package> version`，再把解析出的精确版本写入
`package.json`，运行 `npm install --ignore-scripts` 生成 lockfile。禁止使用 `^`、`~` 或
`latest`。至少锁定 Pi coding-agent、`@modelcontextprotocol/sdk`、TypeScript、tsx、Vitest。

- [ ] **Step 4: 实现严格 LF JSONL 类型与解析**

```ts
export type PiRpcRecord = { type: string; [key: string]: unknown };

export function parseRpcLine(line: string): PiRpcRecord {
  const normalized = line.endsWith("\r") ? line.slice(0, -1) : line;
  if (!normalized.trim()) throw new Error("empty_rpc_record");
  const value: unknown = JSON.parse(normalized);
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    throw new Error("invalid_rpc_record");
  }
  if (typeof (value as { type?: unknown }).type !== "string") {
    throw new Error("invalid_rpc_record");
  }
  return value as PiRpcRecord;
}
```

- [ ] **Step 5: 对真实 CLI 运行无模型最小探针**

验证 `--mode rpc`、`--no-session`、`--no-builtin-tools`、`--no-context-files`、
`--no-extensions -e <path>`、`--no-skills --skill <path>`、`get_state`、`abort`，并确认 stdout
只有 JSONL 协议记录，诊断走 stderr。把命令、Pi 版本、包 scope、RPC 事件名和已验证限制写入
`docs/qa/pi-runtime-version-probe.md`；不得记录任何 key。

- [ ] **Step 6: 验证与当前 Runtime 相同的模型配置**

从后端 Settings 读取当前 OpenAI-compatible `base_url/model/reasoning_effort`，在单独的临时
`PI_CODING_AGENT_DIR/models.json` 中生成 Pi 自定义 provider/model 配置；key 只通过环境变量
传入。执行一次最小 prompt，记录请求成功、实际 provider/model/thinking 和 token usage，
不得记录 key、Authorization 或完整 Prompt。若 Pi 当前版本无法使用同一 endpoint/model，
Task 1 直接失败并停止 POC，不允许换模型继续。

- [ ] **Step 7: 运行测试与类型检查**

Run: `cd pi-runtime && npm test && npm run typecheck`

Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add pi-runtime/package.json pi-runtime/package-lock.json pi-runtime/tsconfig.json \
  pi-runtime/src/rpc/protocol.ts pi-runtime/tests/rpc-probe.test.ts \
  docs/qa/pi-runtime-version-probe.md
git commit -m "build: pin pi rpc poc runtime"
```

### Task 2: 增加 POC 配置、独立数据库硬门禁与临时 Run token

**Files:**
- Modify: `backend/app/core/config.py`
- Modify: `backend/.env.example`
- Create: `backend/app/pi_runtime_poc/__init__.py`
- Create: `backend/app/pi_runtime_poc/auth.py`
- Create: `backend/app/pi_runtime_poc/schemas.py`
- Test: `backend/tests/pi_runtime_poc/test_auth.py`
- Test: `backend/tests/pi_runtime_poc/test_config.py`

**Interfaces:**
- Consumes: 当前 `Settings` 与 FastAPI dependency 模式。
- Produces: `PiPocSettingsGuard.assert_safe(settings)`；`issue_run_token(run_id)`；`verify_run_token(token, run_id)`；严格 Pydantic 请求/响应 DTO。

- [ ] **Step 1: 写数据库名和环境门禁失败测试**

```python
def test_poc_guard_rejects_non_poc_database(settings_factory):
    settings = settings_factory(app_env="development", mysql_database="kol_insight")
    with pytest.raises(RuntimeError, match="pi_poc_database_required"):
        PiPocSettingsGuard.assert_safe(settings)


def test_poc_guard_accepts_exact_database(settings_factory):
    settings = settings_factory(app_env="test", mysql_database="kol_insight_pi_poc")
    PiPocSettingsGuard.assert_safe(settings)
```

- [ ] **Step 2: 写 token 归属和过期测试**

覆盖正确 Run、错误 Run、篡改 token、过期 token；错误统一为 401，不能泄漏 Run 是否存在。

- [ ] **Step 3: 运行聚焦测试确认红灯**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_auth.py tests/pi_runtime_poc/test_config.py -q`

Expected: collection FAIL。

- [ ] **Step 4: 增加仅 POC 使用的配置**

新增：`PI_RUNTIME_POC_ENABLED=false`、`PI_RUNTIME_POC_INTERNAL_SECRET`、
`PI_RUNTIME_POC_BASE_URL=http://127.0.0.1:8000/api/v1/internal/pi-poc`、
`PI_RUNTIME_POC_RUN_TIMEOUT_SECONDS=1800`、`PI_RUNTIME_POC_MCP_TIMEOUT_SECONDS=180`、
`PI_RUNTIME_POC_MAX_DECISIONS=50`。启动 POC runner 时同时校验 enabled、环境和数据库名。

- [ ] **Step 5: 实现 HMAC 单 Run token**

payload 只含 `run_id`、`exp`、随机 nonce；使用后端 secret 签名。验证使用恒定时间比较，
返回上下文中的 run_id，不接受请求 body 覆盖身份。

- [ ] **Step 6: 定义严格 POC DTO**

至少包含 `PiToolStarted`、`PiToolSettled`、`PiToolFailed`、`PiInternalToolRequest`、
`PiInternalToolResponse`、`PiRunContextRead`；全部 `ConfigDict(extra="forbid")`，原始 payload
字段允许 `Any`，密钥字段不存在于 Schema。

- [ ] **Step 7: 验证并提交**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_auth.py tests/pi_runtime_poc/test_config.py -q`

Run: `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc app/core/config.py`

```bash
git add backend/app/core/config.py backend/.env.example backend/app/pi_runtime_poc \
  backend/tests/pi_runtime_poc/test_auth.py backend/tests/pi_runtime_poc/test_config.py
git commit -m "feat: guard isolated pi poc runtime"
```

### Task 3: 实现 Python Pi RPC 子进程客户端

**Files:**
- Create: `backend/app/pi_runtime_poc/rpc.py`
- Test: `backend/tests/pi_runtime_poc/test_rpc.py`

**Interfaces:**
- Consumes: Task 1 锁定的 CLI 与 JSONL 协议。
- Produces: `PiRpcClient.start(config)`, `prompt(message)`, `events()`, `abort()`, `close()`；`PiRpcProtocolError`、`PiRpcExited`、`PiRpcTimeout`。

- [ ] **Step 1: 写 fake subprocess 协议测试**

测试必须覆盖：prompt correlation id、stdout 分片跨 read、CRLF、字符串内 U+2028、stderr
独立收集、非 JSON stdout 立即失败、30 分钟墙钟、abort 后进程退出、close 清理子进程。

- [ ] **Step 2: 运行测试确认红灯**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_rpc.py -q`

- [ ] **Step 3: 实现不依赖 `readline` 的 LF framing**

以 bytes buffer 查找 `b"\n"`，仅在 LF 处切帧；末尾 `\r` 可剥离。stdout 只解析 RPC，
stderr 使用有界 ring buffer 保存诊断。启动参数由 list 构造，不经 shell。

- [ ] **Step 4: 固定启动权限**

命令必须包含 `--mode rpc --no-session --no-builtin-tools --no-context-files --no-extensions`
与显式 `-e`；Skills 使用 `--no-skills` 后逐个 `--skill`。设置 `PI_OFFLINE=1`、
`PI_SKIP_VERSION_CHECK=1`，并使用每 Run 临时 `PI_CODING_AGENT_DIR`，结束后安全清理。

- [ ] **Step 5: 验证并提交**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_rpc.py -q`

Run: `cd backend && .venv/bin/ruff check app/pi_runtime_poc/rpc.py tests/pi_runtime_poc/test_rpc.py`

```bash
git add backend/app/pi_runtime_poc/rpc.py backend/tests/pi_runtime_poc/test_rpc.py
git commit -m "feat: add strict pi rpc subprocess client"
```

### Task 4: 实现透明 DataTap MCP Extension 与 Evidence 旁路

**Files:**
- Create: `pi-runtime/src/http/client.ts`
- Create: `pi-runtime/src/redaction.ts`
- Create: `pi-runtime/src/extensions/datatap-mcp.ts`
- Create: `pi-runtime/src/extensions/poc-runtime.ts`
- Create: `pi-runtime/tests/datatap-mcp.test.ts`
- Create: `pi-runtime/tests/redaction.test.ts`
- Create: `backend/app/pi_runtime_poc/service.py`
- Create: `backend/app/pi_runtime_poc/router.py`
- Modify: `backend/app/api/router.py`
- Test: `backend/tests/pi_runtime_poc/test_evidence_ingest.py`

**Interfaces:**
- Consumes: `DATATAP_MCP_URL/TOKEN`、MCP `tools/list`/`tools/call`、Task 2 token。
- Produces: DataTap 工具动态注册；`POST /internal/pi-poc/runs/{run_id}/tool-calls/start`；`.../{call_id}/settle`；`.../{call_id}/fail`；settle 返回 `evidence_id`。

- [ ] **Step 1: 写 Extension 透明性测试**

用 fake MCP server 和 fake audit server 断言：发往 MCP 的 tool name/arguments 与 Pi 输入深相等；
返回 Pi 的业务 payload 与 MCP 响应深相等；仅新增顶层 `_runtime_metadata`；Extension 不发起
第二次 MCP 调用；超时/错误不改写 error code/message；token 不出现在 stdout/audit body。

- [ ] **Step 2: 写后端 Evidence 持久化失败测试**

覆盖：错误 Run token 401、跨 Run call id 404、start 幂等、settle 幂等、Pi 路径成功创建零积分
`AgentStep + AgentToolCall + EvidenceItem`、failed/unknown 不产生 available Evidence、原始
payload hash 可复核、事件分别为 tool.started/succeeded/failed/unknown。

- [ ] **Step 3: 运行两端测试确认红灯**

Run: `cd pi-runtime && npm test -- datatap-mcp.test.ts redaction.test.ts`

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_evidence_ingest.py -q`

- [ ] **Step 4: 实现 MCP discovery 和动态工具注册**

Extension 启动时连接单个 DataTap MCP endpoint，执行 `tools/list`，按远端 Schema 原样注册。
禁止读取现有 Python `DYNAMIC_TOOL_ALLOWLIST` 来裁剪 POC 工具目录；POC 的目的正是让 Pi
看到 DataTap 原始目录。Extension 只允许配置的 DataTap endpoint，不提供任意 URL 工具。

- [ ] **Step 5: 实现调用旁路状态机**

Pi Extension 的 start 先落 `planned/running` 零积分 ToolCall；随后直接 MCP call；成功则 settle 并经
`EvidenceWriter` 写不可变 Evidence；确定失败记 failed；客户端超时或发送后断链记 unknown。
`logical_call_id` 使用 run + Pi call id，不能用相同参数 hash 做业务熔断。

- [ ] **Step 6: 实现递归脱敏**

key 名匹配 `authorization|api[_-]?key|token|secret|password|cookie` 时值替换为 `***`；同时
替换当前进程内已知 secret 的字符串出现。脱敏只用于审计和事件，不修改返回 Pi 的 MCP
业务 payload。

- [ ] **Step 7: 验证并提交**

Run: `cd pi-runtime && npm test && npm run typecheck`

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_evidence_ingest.py -q`

Run: `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc app/api/router.py`

```bash
git add pi-runtime/src pi-runtime/tests backend/app/pi_runtime_poc \
  backend/app/api/router.py backend/tests/pi_runtime_poc/test_evidence_ingest.py
git commit -m "feat: capture direct datatap evidence for pi poc"
```

### Task 5: 暴露受控历史、Builder 与发布内部工具

**Files:**
- Create: `pi-runtime/src/extensions/internal-tools.ts`
- Create: `pi-runtime/tests/internal-tools.test.ts`
- Modify: `backend/app/pi_runtime_poc/service.py`
- Modify: `backend/app/pi_runtime_poc/router.py`
- Test: `backend/tests/pi_runtime_poc/test_internal_tools.py`

**Interfaces:**
- Consumes: 既有 `ToolRegistry.execute`、六类 Builder、`ArtifactPublicationService`、历史读取工具。
- Produces: Pi 工具 `get_session_context`、`search_evidence`、`read_tool_result`、`read_artifact`、六个 `build_*_draft`、`publish_artifacts`；服务端强制注入 user/session/run/worker。

- [ ] **Step 1: 写最小工具目录测试**

断言 Pi 只看到设计允许的历史、Builder、publish 和 DataTap 工具；不得出现 bash/read/write/
edit/任意 HTTP。内部 HTTP 请求里的伪造 `user_id/session_id/run_id/worker_id` 必须被忽略或
Schema 拒绝。

- [ ] **Step 2: 写 Builder/发布集成失败测试**

至少以 `brand_report_v3` fixture 覆盖：Evidence 归属正确可构建 Draft；外部 Evidence 404；
Pi 手写强类型 payload 无工具入口；Builder 返回 gaps/limitations 摘要；publish 使用当前
Run 活跃租约且幂等；发布后 Excel 导出与 Artifact Version payload 指向同一 version。

- [ ] **Step 3: 运行测试确认红灯**

Run: `cd pi-runtime && npm test -- internal-tools.test.ts`

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_internal_tools.py -q`

- [ ] **Step 4: 构建只含内部工具的 Registry**

复用 `ReadArtifactTool/SearchEvidenceTool/ReadToolResultTool`、计算工具和六类 Builder；不得注册
`AgentMcpTool`，因为 DataTap 已由 Pi Extension 直连。服务端从 token 对应 Run 加载 user、
session、profile 和 channel permission，调用 `ToolRegistry.execute`。

- [ ] **Step 5: 增加统一 Builder 反馈投影**

不修改 Builder payload；内部 API 从最新 Draft Revision 提取受限 `schema_version`、
`data_status`、`availability`、`limitations`、Evidence refs 和可安全返回的 coverage 摘要，
让 Pi 判断补查。不得把完整 Excel 或超大 raw Evidence塞回模型。

- [ ] **Step 6: 接入确定性发布**

`publish_artifacts` 只接受当前 Run 持有的 draft ids，调用既有发布服务；逐项返回 published/
validation_failed/failed。成功发布后立即验证 Version 可由现有导出端点读取，不另建 POC
格式。

- [ ] **Step 7: 验证并提交**

Run: `cd pi-runtime && npm test && npm run typecheck`

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_internal_tools.py tests/agent_artifacts -q`

```bash
git add pi-runtime/src/extensions/internal-tools.ts pi-runtime/tests/internal-tools.test.ts \
  backend/app/pi_runtime_poc backend/tests/pi_runtime_poc/test_internal_tools.py
git commit -m "feat: bridge pi to trusted artifact tools"
```

### Task 6: 编写营销 Skills 与成功案例引用

**Files:**
- Create: `pi-runtime/skills/social-marketing-analyst/SKILL.md`
- Create: `pi-runtime/skills/brand-research-report/SKILL.md`
- Create: `pi-runtime/skills/brand-research-report/references/chatgpt-datatap-success.md`
- Create: `pi-runtime/skills/campaign-evaluation-report/SKILL.md`
- Create: `pi-runtime/skills/kol-selection-report/SKILL.md`
- Create: `pi-runtime/skills/artifact-drilldown/SKILL.md`
- Create: `pi-runtime/skills/marketing-strategy/SKILL.md`
- Create: `pi-runtime/tests/skills.test.ts`

**Interfaces:**
- Consumes: 六类 Artifact/Builder 契约与现有脱敏成功案例 `backend/app/agent_runtime/exemplars/brand_analysis_success_v1.*`。
- Produces: Pi 可自主发现的六个 Agent Skills；`skill_manifest` 中的 name/version/digest。

- [ ] **Step 1: 写 Skill 合规和反硬编码测试**

测试 frontmatter name 与目录一致、description 存在、引用文件存在；扫描禁止出现真实 token、
固定瑞幸日期/数值、固定“先调用 X 再调用 Y”工具序列；每个报告 Skill 必须包含 Evidence、
partial、Builder feedback、禁止编造、何时澄清和何时完成。

- [ ] **Step 2: 运行测试确认红灯**

Run: `cd pi-runtime && npm test -- skills.test.ts`

- [ ] **Step 3: 编写根 Skill**

明确只处理社媒营销；区分完整报告、钻取、策略咨询、模糊输入、非营销拒答；指导按需加载
专项 Skill，不直接写品牌/活动/达人关键词路由表。

- [ ] **Step 4: 编写三个报告 Skill**

分别列出业务问题、所需范围、证据覆盖、Builder 调用、缺失数据处理、Excel/BI 同版完成
条件。KOL Skill 保留严格缺失即 0、跨平台 Top20、效果与匹配度 70 + 价格效率 30 的既有
确定性评分契约，但评分计算仍由 Builder 完成。

- [ ] **Step 5: 编写钻取与策略 Skill**

要求优先读取用户引用的 Artifact Version 和历史 Evidence；只有 freshness/scope 变化或证据
不足才调用 DataTap；普通钻取不自动发布新报告。

- [ ] **Step 6: 生成成功案例引用**

从已有成功案例提炼：确认时间/平台/品牌范围、检查工具 Schema、分阶段取证、验证空值与
口径、用 Builder gaps 反馈补查、数据受限时透明收尾。删除品牌、日期、数值、密钥和固定
工具调用顺序。

- [ ] **Step 7: 验证并提交**

Run: `cd pi-runtime && npm test -- skills.test.ts`

```bash
git add pi-runtime/skills pi-runtime/tests/skills.test.ts
git commit -m "feat: add versioned social marketing pi skills"
```

### Task 7: 实现 Pi Run 适配、事件映射和 Attempt 收口

**Files:**
- Create: `backend/app/pi_runtime_poc/runner.py`
- Modify: `backend/app/agent_runtime/events.py`
- Test: `backend/tests/pi_runtime_poc/test_runner.py`
- Test: `backend/tests/pi_runtime_poc/test_event_mapping.py`

**Interfaces:**
- Consumes: `PiRpcClient`、AgentRunRepository、AgentEventStream、Task 2 token。
- Produces: `PiPocRunner.run(run_id)`；Pi event → 现有 `thinking.*`、`tool.*`、`artifact.*`、`message.completed`、`run.*` 映射；Attempt 级状态。

- [ ] **Step 1: 写事件映射失败测试**

覆盖 thinking delta 默认可折叠契约、assistant text delta/完成、tool start/end、artifact publish、
Pi error、取消、50 决策、30 分钟超时；原始事件写 Step 审计，产品事件 payload 不含 secret。

- [ ] **Step 2: 写 Run 终态测试**

三个报告发布成功 → completed；部分报告发布/数据受限 → completed_with_warnings；澄清 →
paused/clarification 且无 Artifact；非营销回复 → completed；进程崩溃 → failed；用户取消 →
cancelled。每个 Run 只能有一个终态事件。

- [ ] **Step 3: 运行测试确认红灯**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_runner.py tests/pi_runtime_poc/test_event_mapping.py -q`

- [ ] **Step 4: 实现上下文重建**

从 MySQL 生成受控启动消息：当前用户问题、近期对话、session summary、用户显式引用 Version、
Artifact 摘要、Evidence 索引、日期与时区。Prompt 不做业务意图路由，只说明可信工具、
安全边界、完成协议和 Skill 自主加载。

- [ ] **Step 5: 实现 Run/Attempt 生命周期**

领取 Run 租约、创建 Attempt、签发临时 token、启动 Pi、累计 decision_count、持久化事件；
完成时销毁进程与临时 agent dir。POC 基础设施故障不自动重启，保留完整证据供比较；方案 B
才实现一次 Attempt 恢复。

- [ ] **Step 6: 实现安全收口**

只有已发布 Artifact 和完成正文才能进入完成态；未发布 Draft 保留诊断但不当正式报告。
使用现有 `AgentEventStream.settle_terminal` 保证状态与终态事件原子。调用 abort 后等待短暂
宽限，再显式 terminate/kill 当前精确 PID，不使用进程名通配。

- [ ] **Step 7: 验证并提交**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc -q`

Run: `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc app/agent_runtime/events.py`

```bash
git add backend/app/pi_runtime_poc backend/app/agent_runtime/events.py \
  backend/tests/pi_runtime_poc/test_runner.py backend/tests/pi_runtime_poc/test_event_mapping.py
git commit -m "feat: run pi rpc through durable agent lifecycle"
```

### Task 8: 建立六场景对比 Harness 与自动验收

**Files:**
- Create: `backend/fixtures/pi_runtime_poc/cases.json`
- Create: `backend/app/pi_runtime_poc/comparison.py`
- Create: `backend/scripts/run_pi_runtime_poc.py`
- Create: `backend/scripts/run_pi_runtime_poc.sh`
- Create: `backend/tests/pi_runtime_poc/test_comparison.py`
- Create: `backend/tests/integration/test_pi_runtime_poc_real.py`
- Create: `docs/qa/pi-runtime-poc-rounds.md`
- Create: `outputs/pi-runtime-poc/.gitkeep`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: current Runtime runner、`PiPocRunner`、三个 Artifact exporters。
- Produces: `RuntimeCaseExecutor.execute(case: PocCase) -> PocCaseResult` 协议、
  `CurrentRuntimeCaseExecutor`（通过现有 `AgentRunExecutor.process_run` 执行）和
  `PiRuntimeCaseExecutor`（调用 `PiPocRunner.run`）；
  `run_pi_runtime_poc.py --case <id|all> --runtime current|pi|both`；append-only JSON result；Gate 汇总。

- [ ] **Step 1: 写比较器失败测试**

测试硬门槛：三个报告 published、Excel 可打开且绑定精确 Version、BI payload 同 Version、
所有 narrative 数字可由 lineage 支持、无 secret、clarify/offtopic/drilldown 正确；测试对比目标
计算数据覆盖、MCP 参数有效率、产物完整性和人工可读性待评分字段。

- [ ] **Step 2: 运行测试确认红灯**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_comparison.py -q`

- [ ] **Step 3: 定义六个版本化案例**

每例固定 `case_id`、用户问题、前置历史/Artifact、日期锚点、expected_behavior、
required_artifact_type。三个主报告分别品牌、活动、达人；钻取引用一个已发布 Version；模糊
输入缺少决定性范围；非营销输入使用明确非营销问题。fixture 不含供应商返回和密钥。

- [ ] **Step 4: 实现统一执行协议**

```python
class RuntimeCaseExecutor(Protocol):
    async def execute(self, case: PocCase) -> PocCaseResult: ...


@dataclass(frozen=True)
class PocCaseResult:
    case_id: str
    runtime: Literal["current", "pi"]
    run_id: str
    outcome: str
    artifact_versions: tuple[str, ...]
    evidence_ids: tuple[str, ...]
    metrics: dict[str, float | int | bool | None]
    diagnostic_path: str
```

两种实现都先用同一个 `PocCaseFactory` 创建隔离 Session/Message/Run，再分别交给既有
`AgentRunExecutor.process_run` 或 `PiPocRunner.run`；比较器不得直接调用私有测试 helper。

- [ ] **Step 5: 实现公平执行**

`both` 为每个案例创建隔离 Session；奇数案例 current→pi，偶数 pi→current；两侧共享同一
解析后的 runtime config snapshot。结果写 `outputs/pi-runtime-poc/<round>/<case>/<runtime>.json`
和汇总 `summary.json`，已有 round 不覆盖。

- [ ] **Step 6: 实现真实服务安全脚本**

Shell 脚本必须检查 `APP_ENV=test`、`MYSQL_DATABASE=kol_insight_pi_poc`、
`PI_RUNTIME_POC_ENABLED=true`，运行 Alembic 到当前 head，再显式设置
`RUN_REAL_SERVICES=1`。任何一项不符退出非零；不打印 env 或 DSN。

- [ ] **Step 7: 实现 Gate 报告**

`summary.json` 给出每个硬门槛 pass/fail、两 Runtime 指标和失败证据路径；
`docs/qa/pi-runtime-poc-rounds.md` 每轮追加命令、模型、Pi 版本、case 结果、供应商异常和结论。
“人工可读性”必须由评审者填 1–5 分并签名，脚本不能假装自动判断。

- [ ] **Step 8: 运行非真实完整回归**

Run: `cd pi-runtime && npm test && npm run typecheck`

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc tests/agent_runtime tests/agent_artifacts -q`

Run: `cd backend && .venv/bin/ruff check app tests scripts/run_pi_runtime_poc.py`

Expected: 全部通过，真实服务测试默认 skip。

- [ ] **Step 9: Commit**

```bash
git add backend/fixtures/pi_runtime_poc backend/app/pi_runtime_poc/comparison.py \
  backend/scripts/run_pi_runtime_poc.py backend/scripts/run_pi_runtime_poc.sh \
  backend/tests/pi_runtime_poc/test_comparison.py \
  backend/tests/integration/test_pi_runtime_poc_real.py docs/qa/pi-runtime-poc-rounds.md \
  outputs/pi-runtime-poc/.gitkeep .gitignore
git commit -m "test: add pi runtime comparison gate"
```

### Task 8A: 修复 Current 基线前置数据并解除 Task 9 预检阻断

> 2026-08-07 修订：原 Task 9 在任何模型/MCP 调用前停止。该记录应归类为
> `Gate A BLOCKED / NOT RUN`，不是 Pi 效果 Gate 的 FAIL。本任务完成后才能重新执行 Task 9。

**Files:**
- Modify: `backend/app/pi_runtime_poc/comparison.py`
- Modify: `backend/scripts/run_pi_runtime_poc.py`
- Test: `backend/tests/pi_runtime_poc/test_comparison.py`
- Modify: `docs/qa/pi-runtime-poc-rounds.md`
- Modify: `changelog/2026-08-07.md`

**Interfaces:**
- Consumes: 既有 `Wallet`、`UserChannelPermission`、`IdentityService.default_channels`、
  `Settings.tencent_plan_model` 和未修改的 `AgentMcpAccounting`。
- Produces: `PocCaseFactory(..., model_name: str, current_wallet_balance: int = 10_000)`；
  Current 案例具备一次性测试钱包，Pi 案例无钱包；两侧具备相同默认渠道权限和真实模型快照。

- [ ] **Step 1: 先把历史预检记录更正为阻断而非效果失败**

在 `docs/qa/pi-runtime-poc-rounds.md` 末尾追加“设计方复核”章节，不改写或删除原始记录：明确
当时没有调用模型、没有运行六案例、没有创建真实 round，所以结论重分类为
`Gate A BLOCKED / NOT RUN`；根因是计划把“积分不参与评价”错误扩大成了“Current 基线也
禁止原生计费”。保留 `cd4d2d3` 和 `15b2503` 作为审计证据。

- [ ] **Step 2: 写 Current/Pi 前置数据差异的失败测试**

```python
async def test_case_factory_keeps_current_billing_and_pi_wallet_free(db_session_factory):
    case = PocCase(
        case_id="brand-research-v1",
        user_question="分析最近一个月的品牌声量和情感",
        date_anchor="2026-08-07",
        expected_behavior="report",
        required_artifact_type="brand_report_v3",
    )
    factory = PocCaseFactory(
        db_session_factory,
        round_id="round-restart",
        model_name="deepseek-v4-pro",
        current_wallet_balance=10_000,
    )
    current_run_id = await factory.create(case, "current")
    pi_run_id = await factory.create(case, "pi")

    async with db_session_factory() as db:
        current = await db.get(AgentRun, current_run_id)
        pi = await db.get(AgentRun, pi_run_id)
        current_wallet = await db.get(Wallet, current.user_id)
        pi_wallet = await db.get(Wallet, pi.user_id)
        current_channels = set(
            await db.scalars(
                select(UserChannelPermission.channel).where(
                    UserChannelPermission.user_id == current.user_id
                )
            )
        )
        pi_channels = set(
            await db.scalars(
                select(UserChannelPermission.channel).where(
                    UserChannelPermission.user_id == pi.user_id
                )
            )
        )

    assert current_wallet.balance == 10_000
    assert current_wallet.reserved == 0
    assert pi_wallet is None
    assert current_channels == pi_channels == set(IdentityService.default_channels)
    assert current.profile_version == pi.profile_version == "v1"
    assert current.model == pi.model == "deepseek-v4-pro"
```

- [ ] **Step 3: 写 Gate 不使用积分作比较指标的失败测试**

构造一对 `PocCaseResult`：Current metrics 包含 `points_settled=30`，Pi 包含
`points_settled=0`，其他硬门槛和三个效果指标满足通过；断言 `assess_gate_a` 不读取
`points_settled`，结果仍由 coverage、MCP 参数有效率、Artifact 完整性和人工可读性决定。

- [ ] **Step 4: 运行聚焦测试确认红灯**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_comparison.py -q`

Expected: factory 不接受 `model_name/current_wallet_balance`，且没有 Wallet/渠道权限。

- [ ] **Step 5: 最小修复 POC fixture，不修改 Current 计费代码**

`PocCaseFactory.create` 必须：

- 为两种 runtime 都创建 `IdentityService.default_channels` 对应的启用权限行；
- 只在 `runtime == "current"` 时创建 `Wallet(balance=10_000,reserved=0,version=0)`；
- Pi 用户不创建 Wallet；
- 两侧 Run 的 `profile_version` 均为 `"v1"`；
- 两侧 Run 的 `model` 均为构造器传入的真实 `settings.tencent_plan_model`；
- `prompt_snapshot_json.pi_runtime_poc` 记录 `billing_mode`：Current 为
  `"native_isolated_wallet"`，Pi 为 `"disabled"`，但不记录钱包余额或流水。

禁止修改 `AgentMcpAccounting`、`WalletService`、`AgentMcpTool` 或加入 bypass/mock 分支。

- [ ] **Step 6: 把 Current 钱包用量作为诊断而非 Gate 指标**

`_collect_case_result` 可在 Current 结果 metrics 中写 `points_settled` 和 `points_reserved`，
Pi 写 0；`assess_gate_a` 不得把它们加入 hard checks 或 improved metrics。Current 因余额不足
结束仍按原业务 outcome 失败，但 10,000 余额应使该环境条件不可达。

- [ ] **Step 7: 修正 CLI factory 装配**

```python
factory = PocCaseFactory(
    SessionFactory,
    round_id=round_id,
    model_name=settings.tencent_plan_model,
    current_wallet_balance=10_000,
)
```

同时增加启动前断言：数据库仍精确为 `kol_insight_pi_poc`，不能通过环境变量把测试钱包写到
开发库或测试库。

- [ ] **Step 8: 运行恢复前验证**

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc/test_comparison.py tests/agent_runtime/tools/test_mcp.py -q`

Run: `cd backend && .venv/bin/pytest tests/pi_runtime_poc -q`

Run: `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc scripts/run_pi_runtime_poc.py`

Expected: 全部通过；没有真实模型/MCP 调用；`git diff` 不含任何密钥。

- [ ] **Step 9: Commit 并停在 Task 9 真实调用前**

```bash
git add backend/app/pi_runtime_poc/comparison.py backend/scripts/run_pi_runtime_poc.py \
  backend/tests/pi_runtime_poc/test_comparison.py docs/qa/pi-runtime-poc-rounds.md \
  changelog/2026-08-07.md docs/superpowers/specs/2026-08-07-pi-agent-runtime-integration-design.md \
  docs/superpowers/plans/2026-08-07-pi-rpc-poc.md \
  docs/superpowers/prompts/2026-08-07-pi-rpc-poc-codex-prompt.md \
  docs/superpowers/prompts/2026-08-07-pi-rpc-poc-task9-restart-prompt.md
git commit -m "fix: unblock isolated current runtime poc baseline"
```

### Task 8B: 修复 DataTap 配置桥接并延后 round 创建

> 2026-08-07 修订：Task 8A 后的启动尝试在任一案例前被 Shell → Settings 配置名称不一致阻断。
> `AGENT_RUNS=0`、`TOOL_CALLS=0`，没有真实模型/MCP/Pi 调用，因此不消耗唯一真实 round 授权。

**Files:**
- Create: `backend/scripts/pi_runtime_poc_env.sh`
- Modify: `backend/scripts/run_pi_runtime_poc.sh`
- Modify: `backend/scripts/run_pi_runtime_poc.py`
- Create: `backend/tests/pi_runtime_poc/test_task8b_bridge.py`
- Modify: `docs/qa/pi-runtime-poc-rounds.md`
- Modify: `changelog/2026-08-07.md`

**Interfaces:**
- Consumes: 外部进程临时 `DATATAP_MCP_ENDPOINTS_JSON`。
- Produces: Shell 显式导出的 `DATATAP_MCP_URLS`，由 `Settings.datatap_mcp_urls` 解析；Pi client
  factory 只从已校验 Settings 重建 `DATATAP_MCP_ENDPOINTS_JSON`。

- [ ] **Step 1: 先写失败回归测试**

覆盖两项行为：

1. 外部 `DATATAP_MCP_ENDPOINTS_JSON` 经 Shell 规范化后同时可供 Python Settings 的
   `DATATAP_MCP_URLS` 读取，且 Pi factory 从该 Settings 重建 endpoint JSON；测试只用
   `datatap.example.test` 伪地址与测试 token。
2. `build_real_pi_client_factory` 等本地配置/资源预检失败时，CLI 不得调用 `begin_round`，因而
   不创建 `<round_id>/` 目录。

- [ ] **Step 2: 最小修复 Shell → Settings → Pi 链路**

新增可 source 的 Shell helper：外部 mapping 优先，并显式同步到 `DATATAP_MCP_URLS`；启动脚本
加载主工作树 `.env` 后调用 helper，要求两个变量均非空。不得输出或持久化 mapping、endpoint 或
token。Python 不读取未校验的 Shell JSON；保持 `build_real_pi_client_factory` 从
`settings.datatap_mcp_urls` 序列化 Pi 环境变量的现有边界。

- [ ] **Step 3: 延后真实 round 目录创建**

CLI 可以先计算 `round_dir` 路径以装配 executor，但只能在 `refresh_approved_datatap_tools`、
Current executor 和 Pi executor（含 Settings 的四服务校验）全部成功后调用 `begin_round`。任何
预检失败不得留下空 round 目录；不删除历史空目录或覆盖历史输出。

- [ ] **Step 4: 运行恢复前验证**

Run: `cd backend && .venv/bin/pytest -q tests/pi_runtime_poc/test_comparison.py tests/pi_runtime_poc/test_task8b_bridge.py tests/agent_runtime/tools/test_mcp.py`

Run: `cd backend && .venv/bin/pytest -q tests/pi_runtime_poc`

Run: `cd backend && .venv/bin/ruff check app/pi_runtime_poc tests/pi_runtime_poc scripts/run_pi_runtime_poc.py`

Run: `cd backend && bash -n scripts/run_pi_runtime_poc.sh scripts/pi_runtime_poc_env.sh`

Expected: 全部通过；不调用真实模型/MCP；预检失败不创建 round；`git diff` 无密钥。

- [ ] **Step 5: Commit 并停在真实 Task 9 前**

```bash
git add backend/scripts/pi_runtime_poc_env.sh backend/scripts/run_pi_runtime_poc.sh \
  backend/scripts/run_pi_runtime_poc.py backend/tests/pi_runtime_poc/test_task8b_bridge.py \
  docs/superpowers/plans/2026-08-07-pi-rpc-poc.md docs/qa/pi-runtime-poc-rounds.md \
  changelog/2026-08-07.md
git commit -m "fix: bridge datatap poc settings configuration"
```

### Task 9: 执行真实 POC、审查结果并作 Gate A 决策

**Files:**
- Modify: `docs/qa/pi-runtime-poc-rounds.md`
- Runtime output only: `outputs/pi-runtime-poc/<round>/...`
- Modify: `changelog/2026-08-07.md`

**Interfaces:**
- Consumes: Task 8 真实服务脚本和六个 cases。
- Produces: 一轮不可覆盖的真实对比证据；明确 `Gate A PASS` 或 `Gate A FAIL`。

- [ ] **Step 1: 确认安全前置条件**

确认工作树、数据库名、迁移 head、当前模型快照、DataTap endpoint、输出目录和没有运行中的
POC。只显示 key 是否存在，不显示值。额外确认 Task 8A 已提交：Current fixture 有一次性
POC 钱包和默认渠道权限，Pi fixture 没有钱包，两侧 `profile_version="v1"` 且 model 与
`settings.tencent_plan_model` 相同；另确认 Task 8B 已提交、Shell 已将外部 mapping 规范化为
`DATATAP_MCP_URLS`，且预检失败不会创建 round。不得以 2026-08-07 的旧预检记录代替真实 round。

- [ ] **Step 2: 运行一轮真实对比**

Run: `cd backend && bash scripts/run_pi_runtime_poc.sh --case all --runtime both`

Expected: 六例均产生 current/pi 结果；供应商真实异常原样记录，不通过重复运行掩盖。

- [ ] **Step 3: 检查三个 Excel**

使用 openpyxl 做非视觉结构检查：工作簿可打开、必需 Sheet、关键单元格数据、图表对象数量、
公式/数值类型、Artifact id/version 元数据一致。不要使用 LibreOffice，不做截图或视觉审核。

- [ ] **Step 4: 人工评审分析可读性**

在不查看 Runtime 标签的情况下，对 current/pi 输出按事实性、洞察、可执行性、限制披露各
1–5 分；记录评分者与理由。不能修改模型输出后再评分。

- [ ] **Step 5: 运行全量回归**

Run: `cd backend && .venv/bin/pytest -q`

Run: `cd backend && .venv/bin/ruff check app tests`

Run: `cd pi-runtime && npm test && npm run typecheck`

Run: `npm run test && npm run lint && npm run build`

- [ ] **Step 6: 作 Gate 决策并更新 changelog**

只有真实六场景至少实际启动并形成 round 后，才允许写 `Gate A PASS` 或 `Gate A FAIL`。
仅当设计 §8.3 的所有硬门槛通过且三个对比目标至少两项优于 current，写 `Gate A PASS`；
否则写 `Gate A FAIL`，逐项记录根因，不启动方案 B。若仍在真实调用前被配置/基础设施阻断，
只能写 `Gate A BLOCKED / NOT RUN`。当天 changelog 按背景/改动/验证/遗留
结构记录，并保留：`Pi 链路稳定后提醒用户评估 Marketing MCP Gateway`。

- [ ] **Step 7: Commit**

只提交源代码、fixture、QA 摘要和安全的汇总；若 `outputs/` 含真实原始供应商数据则按
`.gitignore` 保持未跟踪，不强行提交。

```bash
git add docs/qa/pi-runtime-poc-rounds.md changelog/2026-08-07.md
git commit -m "docs: record pi rpc poc gate result"
```

---

## Gate A 之后的实施路径

Gate A PASS 后停止开发并进行一次架构复审，以已锁定 Pi SDK/Extension API 和真实 POC 数据
重新生成方案 B 的详细 writing-plans 文档。方案 B 按以下独立 Gate 顺序实施：

1. B1：租户、License、Runtime 配置与密钥加密的数据模型/API。
2. B2：Node.js Pi Agent Gateway SDK 服务与共享 Worker 池。
3. B3：持久队列、公平调度、会话互斥、draining 和一次 Attempt 恢复。
4. B4：透明 License/积分预留结算、用量与成本审计。
5. B5：Pi 事件到现有 SSE 的生产适配、取消和断线续传。
6. B6：六模块管理端和只读 Run 诊断页。
7. B7：租户级 `runtime_backend=current|pi` 灰度、回滚和真实多租户 UAT。
8. B8：稳定一个发布周期后提醒用户评估方案 C；未获明确批准不得实施。

不提前把方案 B 写成依赖猜测的代码任务：Task 1 的 Pi 包/API 探针和 Gate A 的进程、事件、
MCP Extension 实测结果是方案 B 文件级接口的必要输入。
